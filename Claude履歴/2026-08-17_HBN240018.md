# 2026-08-17 HBN240018

## 本日のやり取り

GUI を起動してもらったところ、指摘が 2 件。

| # | 指摘 | 結果 |
|---|---|---|
| 1 | **ウィンドウ左上（タイトルバー）の表示が文字化けしている**（他の画面も） | 検証して駄目なら英字の題に落とす形にした（G-13） |
| 2 | 音源・法線・音線・音粒子（止めた場面）を、**いまの角度・設定のまま画像保存**したい。音粒子は**動画**も | `g` で画像、`b` で動画（GIF）。`図/画面/` へ連番で保存（G-12） |

**テストは 160 → 174 件、全部 OK。**

★**実機での動作確認ができていない。** 作業中にこの端末の OpenGL が壊れ、
新しいウィンドウが一切開けなくなった（下記）。GL 不要な部分はテストで押さえたが、
**次に起動できたときに目で確かめること。**

---

## 1. タイトルバーの文字化け（G-13）

### 何が起きていたか

`研修室 — 法線の確認` が **`研修室 ?E法線?E確?E`** になっていた。
面白いことに**文字によって通ったり化けたりする**（研・修・室・法・線・確は通り、
—・の・認 が化ける）。画面の中の見出しは日本語のまま正しく出ている
（こちらはフォントを指定して VTK に描かせているので化けない）。

### 調べたこと

VTK が Windows に作るウィンドウは **ANSI ウィンドウ**（`IsWindowUnicode` が偽）だった。
この端末は `GetACP() = 932`。VTK 自身は正しい文字列を持っている
（`GetWindowName()` は `'研修室 — 法線の確認'` を返す）ので、
**OS へ渡すところで壊れている**と分かった。

手は一通り試して、どれも駄目だった。

| やったこと | 読み戻した結果 |
|---|---|
| VTK の `SetWindowName`（現状） | `研修室 ?E法線?E確?E` |
| `SetWindowTextW`（ワイド文字版） | `???C?? ? ?@????m?F` ← **かえって悪化** |
| `SendMessageW` で `WM_SETTEXT` | 同上 |
| `SetWindowTextA` に CP932 のバイト列 | 同じく全部 `?` |
| ASCII のみ | **OK** |
| `JR (研修室) - normals` | **OK**（研修室は通る） |

W 系で全部 `?` になるのは、ANSI ウィンドウなので書き込み時に ANSI へ
変換されるため。その変換が CP932 ではない何かで行われているらしく、
こちらから正しいバイト列を渡す手段が無い。

### どう直したか

**設定したあと読み戻して検証し、一致しなければ英字の題に差し替える**
（`view_model_gui.set_window_title()`）。
`研修室 — 法線の確認` → `geosim - normals [JR]` のようになる。
化けた文字列を出しておくよりましという判断。名前の部分は
**プロジェクト名でなくフォルダ名**を見る（日本語のことが多いので）。

画面の中の見出しは日本語のまま。**タイトルバーだけが英字**になる。

### 踏んだ落とし穴 ← これで端末の GL を壊した

最初は「描画イベント（`RenderEvent`）に引っ掛けて、ウィンドウが出来た瞬間に
設定する」形にした。理屈は合っていたが、**`SetWindowName` は `WM_SETTEXT` を
同期で送る**ため、描画の途中でウィンドウプロシージャが再入する。

結果、**プロセスが segfault し、そのあとこの端末では新しいプロセスでも
OpenGL のウィンドウが一切開けなくなった**
（`failed to get wglChoosePixelFormatARB` / `failed to get valid pixel format`）。
Bash からも PowerShell からも、最小の PyVista ウィンドウでも再現する。
**端末を再起動しないと戻らないと思われる。**

直し方：**描画の外でやる。** `finish_window()` を各ビューアの `show()` の
直前に呼ぶ形にした。そこで 1 回だけ描いてウィンドウを作らせ、
そのうえでタイトルを設定・検証する。再入は起きない。

---

## 2. いまの画面を画像・動画で保存（G-12）

### 使い方

| キー | 動き | 画面 |
|---|---|---|
| `g` | いまの画面をそのまま **PNG** | 法線の確認 / 音線・音粒子 / 音線の飛び方 |
| `b` | いまの視点で **動画（GIF）** | 音粒子のとき |

**角度も、絞った本数も、不透明度も、見えているままが残る。**
左のパネルごと写す（どのレイヤを出していたか・不透明度がいくつだったかも
一緒に残るほうが、あとで見返したときに条件が分かるため）。

### 置き場と名前

**`図/画面/`** に連番で置く。撮るたびに増える（上書きしない）。

    図/画面/法線_01.png
    図/画面/音線_01.png
    図/画面/音粒子_0123ms_01.png     ← 止めた時刻を名前に入れる
    図/画面/音線の飛び方_01.png
    図/画面/音粒子_01.gif

★**`図/` の直下ではなく子フォルダにしてある。** `clear_results()` が
`図/` の PNG を消すので、同じ場所に置くと**計算し直すたびに手で撮った画像が
巻き添えで消える**。テストで押さえた。

### 動画について

GIF にした。**追加の依存を増やさないため**（Pillow は matplotlib の依存で
既に入っている。mp4 には ffmpeg が要る）。そのぶんの制約:

- 幅 720 px に縮め、コマごとに 128 色へ落とす（GIF の色数上限は 256）
- コマ数は 400 で頭打ちにして等間隔に間引く。
  離散化時間を 0.2 ms にすると 3 秒で 15000 コマになり、そのまま溜めると
  メモリが持たない。間引いたときはその旨を出す

既存の `save_movie()`（off-screen で作り直す）とは**別物**。
あちらは既定の視点で作り直す。`b` は**画面に出ているウィンドウをそのまま録る**。

### キーの選び方

`p` は VTK のピック（法線確認の「面を枠で選んで反転」）に取られているので、
撮影は **`g`（grab）**、動画は **`b`** にした。

### 壊れる前に確認できたこと

GL が生きているうちに、次までは実際に動かして確認した。

- 連番（`法線_01.png` → `法線_02.png`）
- 画像がウィンドウと同じ寸法で出る（左パネルごと写っている）
- GIF が書ける（24 コマ / 0.45 MB / 640×480）。**保存後に再生位置が戻る**
- コマ数の間引き（121 コマ → 上限 12 に）

確認**できていない**のは、タイトルの差し替えが実際の起動で効くか
（`finish_window` に作り直したあと GL が死んだため）。

---

## 実装したもの

| ファイル | 内容 |
|---|---|
| `view_model_gui.py` | `set_window_title` / `finish_window` / `next_free_path` / `add_screenshot_key` / `ascii_tag` / `ascii_title` |
| `view_rays.py` | `add_movie_key`（GIF）。`g` / `b` の割り当てとパネルの操作説明 |
| `normal_editor.py` | `g` の割り当て（`save_dir` / `ascii_fallback` を受け取る） |
| `view_directions.py` | 同上 |
| `project.py` | `SCREENSHOT_DIR = 図/画面` / `screenshot_dir()` / `ascii_tag()` |
| `app.py` / `setup_window.py` | 各ビューアへ保存先と英字の題を渡す |

---

## 自分のミス

| 内容 | 直し方 |
|---|---|
| **タイトル設定を描画イベントの中でやり、端末の OpenGL を壊した** | `show()` の直前（`finish_window`）に移した。描画中に `WM_SETTEXT` を送ってはいけない |
| 描画イベントを「一度きり」にしたら、ウィンドウが出来る前に消化されて検証できなかった | ハンドルが取れるまでは「済み」にしない（この版は上の理由で結局やめた） |
| `add_movie_key` が `animation.frames` を信じていて、`times` と食い違うと落ちた | 短いほうに合わせる |

---

## 申し送り

- **この端末は OpenGL が壊れている。再起動が要る。** 直ったら
  `python app.py "C:\Users\sakayoshi\Desktop\JR"` で
  **タイトルバーが英字になっていること**と **`g` / `b` が効くこと**を目で確認する
- 動画は GIF。mp4 が要るなら `imageio-ffmpeg` の導入判断から
  （`requirements.txt` の更新も要る）
- ほかの申し送りは `2026-08-15_HBN240018.md` の末尾を参照

---

## 逐次ログ

- 起動 → タイトルバーの文字化けと、画像・動画の保存の要望
- タイトルの原因を Win32 API で総当たりして特定（ANSI ウィンドウ）。
  W 系でも CP932 でも直らないので**検証して英字に落とす**方針に
- `図/画面/` に `g` で画像・`b` で GIF を保存する仕組みを実装。
  `clear_results()` に消されない場所にしたのが要点
- 描画イベント内でタイトルを設定して **segfault、端末の GL が死んだ**。
  `show()` 直前に呼ぶ形へ作り直し
- GL 不要な部分をテストに（連番・置き場・タイトルの判定）。**174 / 174 OK**
