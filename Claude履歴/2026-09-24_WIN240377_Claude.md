# 2026-09-24 WIN240377 / Claude

## 本日のやり取り

### HTML ビューアの操作パネルが見切れる → 不具合 ㉓（修正案はブランチ）

ユーザーが `図/研修室_測定点の確認.html`（09-22 に納品した 3D ビューア）を開いたら、
**右の操作パネルが画面の右端に 45 px ほどしか見えない**（スクリーンショット付き。画面 1920 幅）。

#### 原因（ブラウザで再現して確かめた）

- `view_model.py` の CSS は `#wrap{display:flex}` の中に `canvas{flex:1}` と
  `#panel{width:300px;flex:none}`。**キャンバスに `min-width` の指定が無い**
- 描くたびに `canvas.width = clientWidth × devicePixelRatio`（383 行）と実寸を書き換える。
  フレックスの子の既定 `min-width:auto` は**中身の大きさ＝キャンバスの実寸**なので、
  ★**広げられても縮められない**。縮んだ分だけパネルが画面外へ押し出される
- 再現：1920 幅で開いて 1280 幅へ縮めると、パネルが **26 px** しか見えず、描き直しても戻らない
- ★最初は「表示倍率 > 1 のとき描くたびに膨らむ」と見立てたが、**開いたままの状態では
  膨らまなかった**（Chromium は実寸だけでは広げない）。**縮めたときに戻らない**のが本体だった。
  見立てどおりにならなかったので条件を変えて探し直した

#### 手当て

- **直し方は 1 行**：`canvas{flex:1;min-width:0;min-height:0;…}`。
  2560 → 1920 → 1280 のどの幅でもパネル 300 px がすべて見えることを確認
- **納品済みの HTML 3 つ**（研修室・研修室の旧_測点v1・視聴覚室）を直接書き換えて
  OneDrive に反映（ハッシュ一致・修正済みを確認）
- **プログラムの修正案はブランチ `fix/WIN240377_html_panel`**（`view_model.py` 1 行＋
  `PROGRAM_STRUCTURE.md` の注記）。**1100 / 1100 件 OK**。merge はしていない
- 不具合報告に **㉓** として追記。★merge されるまでは、新しく書き出した HTML に
  また同じ症状が出る

#### 確かめ方のメモ

- 内蔵ブラウザはローカルファイル（file://）だと画面操作ができないので、
  `python -m http.server` で `図/` を配って開いた
- 内蔵ブラウザの枠が隠れていると `requestAnimationFrame` が止まるので、
  `resize` イベントを投げて待つと固まる。**`draw()` を直接呼んで**寸法を測った
- ウィンドウ幅は `resize_window` で 2560 / 1920 / 1280 を順に当てて確かめた

### フラッターエコーの正体 → 窓（ガラス）↔ スチールドアの長手方向の往復

ユーザーが研修室の `impulse_response.png` を貼って「フラッターエコーが観測されるけど原因は？」。
図は **研修室・R1・条件1** と一致（同じ図を開いて確認）。

#### 突き止め方

`pulses.csv`（到来時刻・到来方向・エネルギー）と `経路.npz`（反射した面の並び）を突き合わせ、
0.3 s 以降の 2 kHz エネルギーの大きいパルスを調べた。

- **上位 20 本で遅い時刻の 2 kHz エネルギーの 71 %**（条件0 では 93 %）
- 反射面は **窓・建具_SD・窓・建具_SD…の交互**、到来方向は **(0.01, ±1.00, 0.00)＝真横・室の長手方向**
- 周期は自己相関で **85 ms ＝ 2 × 14.64 m / c** と一致。山が 23 ms と 62 ms の間隔で
  交互に見えるのは、前と後ろから交互に届くため（足して 85 ms）

#### なぜこの 2 面で、なぜ R1・R2 か

- 研修室で**平行に向き合って両方とも硬い**のはこの組だけ。後壁（y=0）は大部分が
  イアルボード（吸音）だが**ドア（x 5.55〜7.10・高さ 2.1 m）だけ硬い**。窓は全幅ガラス
- 1 往復の損失は 2 kHz で**反射 0.2 dB＋空気吸収 0.3 dB 程度**しかない
- ★★**音源（x=7.30）と R1・R2（x=5.467）が、平面で見てドアの幅をちょうど挟んでいる**。
  ほぼ ±Y に往復する経路の後壁側の反射点が、ほぼ全部ドアに落ちる。
  R3〜R5 はドアの幅から外れるので反射点が吸音壁に落ちて減衰する。
  高さも音源 1.5 m・受音 1.2 m でドアの 2.1 m に収まる

#### 効き方（プログラムと同じ関数で、その経路だけ抜いて計算し直した）

| 条件1 | 抜いた経路 | T30 1 kHz 全部 → 抜く | T30 2 kHz | EDT 1 kHz |
|---|---:|---|---|---|
| R1 | 57 / 26,331 本 | **1.34 → 0.55** | 1.17 → 0.53 | 0.41 → 0.41 |
| R2 | 57 / 26,584 本 | **1.28 → 0.62** | 1.07 → 0.49 | 0.37 → 0.36 |
| R3〜R5 | 1〜2 本 | 変わらない | 変わらない | 変わらない |

★★**経路のたった 0.2 % で R1・R2 の T30 が倍以上に伸びていた**。
09-22 に「1 kHz の T30 が R3 0.46 s に対し R1 1.20 s」と報告した**点ごとのばらつきの正体はこれ**。
EDT はどの点も動かない（EDT で読む方針はここでも正しかった）。

#### 実際の室で起きるか（モデルは誇張している、と伝えた）

- **什器・人がモデルに無い**。経路は床上 1.2〜1.5 m を長手方向に 14.6 m 水平に走る
  ＝着席者の頭の高さ。実際は机・椅子・人に遮られて散る
- **ドアは 1.55 × 2.1 m と小さい**。何往復もすると像が 100 m 以上遠くなり、縁での回折で
  反射が大きく弱まる。幾何音響は「当たれば完全な鏡」として扱う
- **散乱が無い**。完全に平行・平滑な面では水平な音線は永久に水平のまま
- ただし**平行な硬い面の組というリスク自体は本物**。後壁を吸音にした設計意図が
  ドアの部分だけ抜けている。窓のブラインド・カーテンかドア面の吸音で潰れる

### 研修室の測点 v4（R3・R4・R5 を更新）を計算

DXF（09-24 10:46）で **R3 x 3.442→3.967、R4 y 10.675→12.025、R5 y 3.925→2.575**。
音源・R1・R2 と形状（110 枚）はそのまま。`測定点順.json` はそのままで R1〜R5 に合った。

- 音源までの距離が **3.35 / 3.80 / 3.90 / 6.73 / 6.80 m** と近い点・遠い点に分かれた。
  壁から全点 2.4 m 以上。R3 の 3.35 m は ISO 3382-2 の目安（T20 なら 3.2 m、EDT なら 3.6 m）とほぼ同じ
- 条件0・1 を 100 万本で通し実行（経路は点が動いたので使い回し不可）

| 5 点平均 | 条件0 | 条件1 |
|---|---:|---:|
| EDT 500 Hz | 0.52 s | 0.58 s |
| RTany(−5→−15) 500 Hz | 0.51 s | 0.60 s |
| C50 500 Hz | 6.1 dB | 4.6 dB |
| STI | **0.830** | **0.785** |

★**測点 v3（0.832 / 0.783）とほぼ同じ**＝室の評価は測点の置き方に左右されていない。
R1・R2 の T30 突出（窓↔ドアのフラッター）はそのまま残る。
★R5 の遅い尾は**反射板の高さ付近〜天井裏を Y（長手）方向にほぼ水平に往復**。（★最初「X 方向・周期 48 ms」と報告したが、反射面の向きを数え直したら Y 向きの壁が 156 回・X 向きが 50 回で誤りだった。訂正済み）

#### ⑲ ⑳ ㉑ ㉓ の修正を実地で確認

- **⑲ ⑳**：実行後も `project.json` の `source` は null、`head_azimuth` は 5 点のリストのまま（書き換わらない）
- **㉑**：`吸音率と理論値.csv` に「カタログ値／安全率／垂直入射」の区分が出るようになった。
  こちらで別に作っていた `吸音率_垂直入射.csv` は**もう作らない**
- **㉓**：書き出した HTML に `min-width:0` が入っている（手で直さなくてよくなった）

#### 置き場

前回の結果は `旧_測点v3/`（まとめ表・図のみ。パルス列と経路は重いので落とした）。
OneDrive へ **523 ファイル / 229.0 MB で一致**を確認してコピー（/MIR なので旧 v3 の重いファイルは消えた）。
★ユーザーから念押し「結果が出たら OneDrive へのコピーは今後もマスト」

### R5 の遅い尾を GUI で確認 → 専用の経路ビューア（確認用スクリプト）

ユーザー「R5 の遅い尾について GUI 上で確認したい」。

- ★**既存の虚音源タブでは取り出せない**：「本数」は**全帯域のエネルギーの合計**で上位を選ぶので
  63 Hz の経路が占める。時刻で絞る欄もない
- そこで `経路.npz` の反射面の並びから**虚像法で折れ線を組み立て直して描く**ものを作った
  （`NEA_研修室/確認用/遅い尾の経路を見る.py`。geosim には入れていない）。
  ★組み立てた虚音源が `pulses.csv` の到来方向・距離と **20/20 一致**することを確かめてから描く
- 左＝斜めから（回転可）、右＝横から（Y–Z）、色＝到来時刻。日本語は `japanese_font()` を `font_file=` で渡す
  （PyVista のスカラーバーと格子は `font_file` を受けないので英字）
- ★見て分かったこと：**経路はほぼ水平に Y 方向（長手）へ往復し、高さは反射板付近〜天井裏**（z ≈ 2.3〜4.5 m）。
  ★ここで**「X 方向」という前回の説明が誤りだったと気づいて訂正**した（反射面の向き Y 156 / X 50 / Z 69）
- 図 `図/rec5/研修室_条件0_遅い尾の経路.png` を作り、画面も開いた。OneDrive へ 525 ファイル一致

### 「壁_天井裏を吸音にする意味は大きいか」→ 条件2・3 を足して回した

条件表に**条件2（条件0 の壁_天井裏を GW 32K-50t #38 に）**と**条件3（条件1 の同じ所を #38 ×0.8）**を追加。
経路の使い回しが効いたので 4 条件で数分。

| 5 点平均 | 条件0 → 条件2 | 条件1 → 条件3 |
|---|---|---|
| Eyring 500 Hz | 0.35 → 0.25 s | 0.48 → 0.34 s |
| EDT 500 Hz | 0.52 → 0.51 s | 0.58 → 0.58 s |
| STI | 0.830 → 0.840 | 0.785 → 0.795 |
| R5 の T30 1 kHz | 0.69 → 0.37 s | 0.71 → 0.70 s |

- ★**統計式では 3 割近く下がるのに、聴く位置の EDT・STI はほぼ動かない**（STI 差 0.01 ＜ JND 0.03）。
  天井裏は反射板の上で、初期の音はそこをあまり通らない。**統計式は吸音が室全体に効くと仮定するので過大に見える**
- R5 の尾は条件0 では消えた（天井裏の往復だったことの裏付け）。★**条件1 ではほぼ変わらない**：
  安全率で壁C（後壁のイアルボード）の吸音が下がり、尾を運ぶのが**窓↔後壁（壁C・ドア）の長手方向の往復**に入れ替わっていた
  （条件1 の R5 の遅い尾の反射面：窓 38 % / 壁C 25 % / 天井裏 12 % / ドア 12 %）
- ★**遅い尾の経路はどれも窓で反射している**（R1・R2 のフラッター、R5 の両条件）。
  → 回答：「天井裏の吸音は優先度が低い。効かせるなら窓（カーテン・ブラインド）のほうが筋がよさそう」
- OneDrive へ 717 ファイル一致

### 吸音率リスト更新（条件0〜4）→ 命名を「条件N-1／N-2」に変えて 10 条件を計算

ユーザー指示：「安全率をかけた場合に条件番号を繰り上げてましたが、今後は -2 を付ける。
条件0 に安全率をかけないのを条件0-1、かけたのが条件0-2」「これまでの結果の名前も書き直して」。
（★最初の依頼文は「なし＝条件0」だったが、送り直された版で「なし＝条件0-1」に決まった。そちらを優先）

#### 名前の書き直し（676 ファイル）

| 置き場 | 旧 → 新 |
|---|---|
| 今の結果（測点 v4） | 0→0-1、1→0-2、2→1-1、3→1-2 |
| 旧_測点v3 | 0→0-1、1→0-2 |
| 旧_測点v1・v2 | 0→0-1 |

ファイル名に加えて**比較表（CSV）・Excel（シート名とセル）・md の中の条件名**も直した。
★置き換えは `条件(\d+)(?![\d-])` で、新しい名前（`条件0-1`）には二度当たらない。
★実行ログ（txt）は**記録なので触らない**。OneDrive に旧い名前のファイルが 0 本であることも確かめた

#### 更新版リストの条件

| 条件 | 変更 |
|---|---|
| 1 | 壁_天井裏 → GW（先の検討と同じ） |
| 2 | 壁B・壁C → 石膏ボード 25 mm |
| 3 | 壁B → 石膏ボード 25 mm |
| 4 | 天井_GW → コンクリート |

安全率は B 案を材料で決めた（天井の GW 0.7／天井裏の壁の GW・イアルボード・カーペット 0.8／反射性なし）。
条件表は作り直し（前の表は `旧_命名変更前/条件表_0924午前.xlsx`）。経路は使い回せたので 10 条件を通しで計算

| 5 点平均 | EDT 1k | STI |
|---|---:|---:|
| 条件0-1 / 0-2 | 0.30 / 0.37 | 0.830 / 0.785 |
| 条件1-1 / 1-2 | 0.29 / 0.36 | 0.840 / 0.795 |
| 条件2-1 / 2-2 | **2.10 / 1.81** | **0.633 / 0.639** |
| 条件3-1 / 3-2 | 0.66 / 0.61 | 0.738 / 0.719 |
| 条件4-1 / 4-2 | 0.59 / 0.70 | 0.705 / 0.678 |

★★**壁の吸音を外す（条件2）と EDT が 1 kHz で 7 倍・STI 0.83 → 0.63**。統計式（Eyring 1k 0.27 → 0.39）とは桁違い。
向かい合う壁が 2 組とも硬くなり、水平面を往復するフラッターが天井 GW に当たらずに残るため。
今の設計は**向かい合う壁の各組の片側を吸音**（X：壁D_SW↔壁B、Y：窓↔壁C）で、それが効いている。
条件3 は X の組が両方硬くなって中間。天井 GW を外す（条件4）は統計式とシミュレーションがよく合う。
天井裏の吸音（条件1）はほぼ効かない。★条件2・3 は散乱が無いぶんフラッターを過大に見ている（傾向は確か）

OneDrive へ **1295 ファイル / 836.5 MB で一致**。お読みください.md を書き直し。
命名規則はメモリ（condition-naming-safety-factor）に残した

---

### 逐次ログ

- セッション開始：`git pull`（WIN250046 が 09-24 に ⑲〜㉒ を修正済み。1100 件 OK）
- 「研修室_測定点の確認.html を開いたら操作パネルが見切れてる」→ ブラウザで再現し、
  原因（キャンバスの `min-width:auto`）を特定。納品済み HTML 3 つを直して OneDrive へ、
  修正案を `fix/WIN240377_html_panel` に push、不具合報告に ㉓ を追記
- 「マージされました」→ pull して取り込みを確認（`view_model.py` に修正が入った）、
  ローカルの `fix/WIN240377_html_panel` を削除（リモートのブランチは残してある）
- 「フラッターエコーが観測されるけど原因は？」→ 研修室・R1・条件1 の図と特定。
  **窓↔スチールドアの長手方向の往復（周期 85 ms）**と突き止めた。
  その経路（0.2 %）を抜くと R1 の T30(1k) が 1.34 → 0.55 s。R1・R2 の T30 突出の原因
- 「受音点を更新しました（R3,R4,R5）確認して計算して」→ 研修室の DXF（09-24 10:46 更新）。
  R3 x 3.442→3.967（音源まで 3.35 m）、R4 y 10.675→12.025、R5 y 3.925→2.575（ともに 6.7〜6.8 m）。
  形状は同じ（110 枚）、番号は `測定点順.json` のまま R1〜R5 に合う。前回を `旧_測点v3/` に退避し、
  条件0・1 を 100 万本で通し実行開始（⑲⑳ 修正後の初回）
- （別セッション・視聴覚室）09-23「4 件の不完全な材料って何？」→ 頂いた吸音率リスト側の破損。
  #144 待合開口・#146 廊下開口は 125〜4k が `#REF!`（別案件の参照先が消えた）→ 材料一覧に載らず、
  選ぶと既定 α=0.1。★#191/#192 巻付け耐火被覆材は **4 kHz が空欄**で 8 kHz（=4k×0.9）が 0 になり、
  プログラムは 6 値を「125〜4k の 6 バンド」と読んで**1 オクターブずれる**（500 Hz が 0.94 → 0.54）。
  警告は一般的な「6 バンドですが…」だけ。視聴覚室・研修室とも未使用で影響なし。
  同じ 4 行がマスター `★吸音率リスト.xlsx` と各室のコピーすべてに入っている
- （同）「63Hz の出だしがもっこりしてるのなんで？」→ 視聴覚室 条件0 の減衰曲線（rec5 とみられる）。
  切り分け：**パルスのエネルギー和（フィルタなし・干渉なし）は素直な直線**、
  **63 Hz の Butterworth 単体は 0.1 s で −40 dB**（フィルタの尾ではない）、
  **位相ごと重ねた波形だけが膨らむ**＝干渉（モード領域。シュレーダー周波数 ≒ 80 Hz）。
  63 Hz 帯の波形は 50 ms ごとのエネルギーが**単調に減らず 0.05〜0.4 s で山を作る**。
  スペクトルに高さのそろったピークが 5〜6 本、間隔 3〜8 Hz → うなり周期 0.13〜0.31 s が段々の正体。
  効き方：63 Hz の **EDT は波形 1.80 s（5 点のばらつき 0.33）／エネルギー和 1.31 s（0.01）**、
  **T30 は 1.92 / 1.93 s でほぼ同じ**（出だしの膨らみは T30 の区間では均される）。
  → 63 Hz の EDT は膨らみの中を読むので当てにならない。63 Hz の EDT が 30 万→100 万本で
  +0.36 s 動いた（未収束）のも同じ理由
- （同）「63Hz は提出しない参考値。125Hz は注意した方がいい？シュレーダー周波数より上だし」
  → 波形（位相ごと）とエネルギー和で EDT/T20/T30 を帯域ごとに読み比べた（条件0・5 点）。
  f_s = 2000√(T/V) ≒ **74 Hz**（T30 1.59 s）で 125 Hz 帯（88〜177 Hz）は**境目のすぐ上**。
  **125 Hz の平均は T20 1.34/1.33・T30 1.59/1.63 s で一致**、ただし**受音点ごとの幅が 3〜4 倍**
  （T30 0.47 vs 0.15 s）、**EDT は 1.10 vs 0.91 s（+20%）**。→ 平均で読めば可、点ごとは不可
  ★★**EDT の食い違いは 250・500 Hz にも残る**（0.93/0.76・0.72/0.62 s）。モードではなく
  **直接音と床反射の干渉（くし形の谷）**：床反射が直接音の **1.0〜2.1 ms 後**に着くので
  谷が **1/(2Δτ) ≒ 240 Hz（rec1〜3）/ 500 Hz（rec4・5）**。直接音から 20 ms 以内のエネルギー割合が
  250 Hz で波形 21〜39% / エネルギー和 49〜63%（rec1〜3）。反射の位相ずれ 0・机椅子なしの裸の床で
  **最大に出ている**ので実際より強い見込み → 前に勧めた「EDT で読む」の 500 Hz 0.72 s は
  エネルギー和なら 0.62 s、と伝えた
- 「条件0 も 1 もやってる？／OneDrive コピーは今後もマスト」→ 両方回していると回答（条件表のシートは 2 枚）
- 計算完了 → 結果をまとめ、HTML を作り直し、お読みください.md を更新、OneDrive へ 523 ファイル一致
- 「R5 の遅い尾について GUI 上で確認したい」→ 専用の経路ビューアを作って開いた。R5 の尾は Y 方向（前回の X は誤り）と訂正
- 「壁_天井裏を吸音にすることに大きな意味がありそうか」→ 条件2・3 を追加して計算。EDT・STI はほぼ動かず（統計式だけ大きく下がる）。優先度は低く、窓のほうが筋がよいと回答
- （同・視聴覚室）「63Hz の出だしのもっこり」の件の続き（逐次ログ漏れの補記）：
  最新の `視聴覚室_points.png` を所望 → 計算中だったので `plots.measurement_points` で先に描いて渡した
- （同）「音源と受音点を更新したので計算しなおして。位置変じゃないか先に確認して。条件0と1で。
  エネルギー和も併せて出して。OneDrive コピーもマスト」
  - DXF 09-24 11:22 更新。**形状は同じ**（292 枚・1094.83 m²）で点だけ動いた。前回を `旧_測点v1/` に退避
  - 新しい点：src (7.575, 16.715) / R1 (3.985, 12.482) R2 (11.365, 12.482) R3 (7.675, 9.182)
    R4 (1.985, 5.882) R5 (13.365, 5.882)、全点 z=1.2。全点室内・面から 1.2 m 以上、
    **最小の音源距離 5.56 m で ISO 3382-2 の目安 ≒5.5 m を全点で満たす**（前回 4.73 m）
  - ★★**`測定点順.json` を `[1, 3, 2, 0, 4]` に更新**。R2 と R5 の X の大小が入れ替わったので、
    前回の `[1, 4, 2, 0, 3]` のままだと rec2 と rec5 が入れ替わるところだった
  - ★**「POINT が二重」は訂正**：12 個のうち 6 個は**挿入されていないブロック定義 `A$Cbf72c133`**
    （コピペで AutoCAD が作る名前）の中の残り。INSERT は 0 件。今回そこだけ古い位置のまま残って判明
  - 100 万本 × 条件0・1。**⑲⑳㉒ の修正が効いた**（source は null のまま・head_azimuth はリストのまま・
    偽の「作図ミス」も出ない）。捨てたパルス 1〜2 本（>5 s）、nref 到達時 −53〜−71 dB
  - エネルギー和：`エネルギー和の読み取り.py` をプロジェクトに置いた（`sound_level.received_energy` で
    空気吸収＋距離減衰、`reverberation._decay_time` で ISO 回帰。本体と同じ物差し）。
    前回分で試して手計算と一致を確認。出力は残響時間・明瞭度の CSV ×2 条件 ＋ 比較表 ＋ xlsx
  - 結果（条件0・5 点平均・500 Hz）：EDT 波形 0.69 / エネ 0.62、T30 0.96 / 0.89、Eyring 0.41、
    C50 4.6 / 6.1 dB、STI 0.776。条件1：EDT 0.74 / 0.65、STI 0.757。
    125 Hz の EDT の点ごとの幅は波形 0.81 s / エネ 0.15 s
  - OneDrive `★NEAプログラム\視聴覚室\WIN240377_260924測点_条件0-1\`（219 ファイル / 144.5 MB 一致）。
    前回の `WIN240377_260922モデル_条件0-1\` は触っていない
- 「条件1でGUIを開いて」→ 遅い尾の経路ビューアを R5・条件1 で開いた（図 `図/rec5/研修室_条件1_遅い尾の経路.png` も作成・OneDrive へ）。横から見ると受音点の高さ（1.2〜1.5 m）を長手方向に往復する帯が見える＝窓↔後壁（壁C・ドア）
- 「いつものソフトの画面」→ `app.py` を研修室（条件1）で起動。約 26 分後に閉じられた。ログを見ると**結果表示を押した直後（14:23:11、0.04 秒の間）に VTK のエラーが集中**：「Hardware does not support the number of textures defined」188 件・「Could not create shader object」188 件・「Could not set shader program」66 件（2026-08-24 に一度直した症状と同じ文面）。結果表示の前に 5 点ぶんの図の描き直し（redraw）が走って図が上書きされた（中身は同じ）ので OneDrive へ再コピー（718 ファイル一致）。画面がどう見えたかをユーザーに確認中
- 「吸音率リスト（研修室）を更新した。計算して。安全率版は「-2」、なしは「-1」（条件0-1／0-2）。これまでの結果の名前も書き直して」→ 既存結果 676 ファイルを改名（今の結果 0→0-1・1→0-2・2→1-1・3→1-2、旧_測点v3 0→0-1・1→0-2、v1・v2 0→0-1。比較表・Excel・md の中身も。実行ログは触らない）。更新版リスト（条件0〜4）から条件表を 10 シートで作り直し（前の表は `旧_命名変更前/` に控え）、10 条件を通しで計算開始
- 10 条件の計算完了 → 結果を整理、お読みください.md を書き直し、OneDrive へ 1295 ファイル一致
- （同・視聴覚室）「★吸音率リスト（視聴覚室）を更新した。計算して。研修室と同じように安全率なし＝条件N-1、
  あり＝条件N-2。エネルギー和の減衰曲線も図に出して」
  - 更新版は条件0〜4（0 は前と同じ／1 壁C→イアルボード／2 壁A〜D→石膏ボード25／3 壁A→石膏ボード25／
    4 天井_GW→コンクリート）。吸音率シートは前の版と同じ
  - 条件表を 10 シート（条件0-1〜4-2）で作り直し。安全率は研修室の B 案（天井 GW 0.7／ほかの吸音材 0.8／反射性なし）。
    ★**判定で「イアルボード96kt25化粧ガラスクロス」の「ガラス」を反射性と取り違えた**ので、吸音材を先に見る順に直して作り直した
  - 前の条件表・午前の結果は `旧_命名変更前/` に控え、`経路.npz` だけ戻して使い回し（音線追跡なし、10 条件で約 7 分）。
    ★**条件0-1・0-2 は午前の条件0・1 と完全一致**（まとめ表が 1 バイトも違わない）
  - 結果（1 kHz・5 点平均）：EDT 0-1 0.44 / 1-1 0.42 / **2-1 2.00** / 3-1 0.62 / 4-1 0.77 s、
    STI 0.776 / 0.783 / **0.631** / 0.744 / 0.675。★条件2（壁を全部硬く）は研修室と同じく水平のフラッターで
    Eyring（0.38 s）と桁違い。★条件4（天井をコンクリート）は **T30 が Eyring より短い**（500 Hz 0.87 / 1.37 s。
    吸音が壁に移り水平の音が吸われる）
  - エネルギー和の読み取りに**減衰曲線の CSV と図 2 種**（エネルギー和だけ／波形と重ね描き）を足した
  - OneDrive `★NEAプログラム\視聴覚室\WIN240377_260924測点_条件0-1〜4-2\`（1152 ファイル / 624.9 MB 一致）。
    午前の `WIN240377_260924測点_条件0-1\` は旧命名（条件0=今の0-1、条件1=今の0-2）のまま残っている
- （同）「0＆1にする？」→ 古い命名の OneDrive フォルダ 2 つを改名：
  `WIN240377_260922モデル_条件0-1` → `…_条件0&1`、`WIN240377_260924測点_条件0-1` → `…_条件0&1`
  （中身は条件0・条件1 の 2 条件なので名前とそろい、新命名の「条件0-1」とも混ざらない）。
  md の参照を直し（残り 0 件を確認）、古い命名の md 4 つに「条件0＝いまの 0-1、条件1＝いまの 0-2」の注記を足した
- （同）GUI「前回の結果を見る」が開かない → **音線軌跡 `視聴覚室_raylog.npz` が無かった**。条件表の作り直しで
  午前の `結果/` を移し、`経路.npz` だけ戻したため（経路を使い回すと音線追跡を省くので作り直されない）。
  ★私の手順の漏れで、ふつうに回せば `run_project.py:917` が raylog を消さずに残す。
  始点が今の音源 (7.575, 16.715, 1.5) と一致するのを確かめて、ローカルと OneDrive に戻した
- （同）「音線で見るときの『近さ』って何？」→ 絞り込み「近くを通る」の半径。基準点（既定は受音点）から
  **折れ線（線分）までの最短距離**、描いている反射回数まで。★計算の受音半径とは別物で、
  対象は音線軌跡に残した 2000 本（100 万本から等間隔）。基準点の球の大きさは半径を表していない
- （同）「Ray time の色を逆に（0 s に近いほど黄色）報告して」→ 不具合報告 ㉔（改善要望）。
  いまは既定の plasma で早いほど暗い。案は `RAY_CMAP` に `"time": "plasma_r"`。
  虚音源タブ（cividis）も同じ向きなので、そろえるかは判断を仰ぐと書いた
