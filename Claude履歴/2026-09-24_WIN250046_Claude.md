# 2026-09-24 WIN250046 / Claude

## 逐次ログ

- 「プルして，不具合報告確認して」→ `ff49a3d..4430bca` を早送りで取り込み
  （WIN240377 の 09-22 ぶん。研修室・視聴覚室の計算）。
  不具合報告に **⑲〜㉒** が追加、あわせて **⑱（安全率）が実案件で直っていることの確認**。
  4 件ともこの端末のコードで裏取りした。
  - ⑲ 音源が `project.json` に書き戻され DXF の更新が効かない … `run_project.py:963`
    で `project.source` を書き、966 で保存。`_sources()`（114 行）は `project.source`
    を DXF より優先する。★`run_conditions` の 472 行でも書き戻している
  - ⑳ `head_azimuth` のリストが 1 点ぶんに潰れる … 受音点ごとの子（850 行で数値を持つ）が
    `save_settings=save_settings`（229 / 264 行）のまま同じ `project.json` を保存する
  - ★★**⑲ と ⑳ は同じ根っこ**（受音点ごとの子プロジェクトが親の設定ファイルを上書きする）
  - ㉑ 垂直入射吸音率が結果に残らない（改善要望）… `吸音率と理論値.csv` は残響室法のみ
  - ㉒ T字接合を「作図ミス」と断じる … `read_dxffile.py:2051` が `model.is_closed`
    （`open_edges == 0`）と `open_edges` の本数で判定し、`free_edges` を見ていない
- 「⑲〜㉒まとめて対応して」→ 4 件とも修正（`f5fa378`）。**1100 / 1100 件 OK**
  - ⑲ ⑳：受音点ごとの子（`_run_one`）が `project.json` を書かないようにした。
    ★報告に無かった `run_conditions` の音源書き戻しも外した。
    ★過去に書き込まれた古い音源は残るので、実行の頭で `_warn_stale_points()` が
    DXF と突き合わせて知らせる（勝手に消さない）
  - ㉑：`吸音率と理論値.csv` に 4 区分（カタログ値／安全率／垂直入射／丸めた帯域）。
    ★「材料別の吸音率」はカタログ値ではなく**安全率と丸めを通したあとの乱入射値**だった
  - ㉒：★★**報告の直し方（`model.free_edges` に寄せる）をそのまま入れると逆に壊れた**。
    `free_edges` は囲まれているときしか数えておらず、開いたモデルでは `[]`＝「数えていない」。
    寄せると開いたモデルに「面は閉じています」と言う（既存テストが落ちて気づいた）。
    `free_edges` を常に数えるようにしてから判定を寄せた（`open_edges.py` と同じ数）

## 本日のやり取り（WIN250046 / 2026-09-24）

### ① プルと不具合報告 ⑲〜㉒ の確認

WIN240377 の 09-22 ぶん（研修室・視聴覚室の計算）を取り込み、⑲〜㉒ を裏取りした。
あわせて ⑱（安全率）が実案件で直っていることの確認も届いていた
（研修室の条件 1 で STI 0.832 → 0.783 と効いている）。

### ② ⑲〜㉒ の修正（`f5fa378`）

| | 内容 | 直し方 |
|---|---|---|
| ⑲ | DXF で音源を動かしても古い音源のまま | 子が `project.json` を書かない＋古い値の見張り |
| ⑳ | `head_azimuth` のリストが潰れる | ⑲ と同じ根っこ。同じ修正で解消 |
| ㉑ | 垂直入射吸音率が結果に残らない | `吸音率と理論値.csv` に 4 区分 |
| ㉒ | T字接合を「作図ミス」と断じる | 自由端だけで判定＋`free_edges` を常に数える |

★★**学んだこと（2 つ）**

1. **⑲ ⑳ は同じ根っこ**。「子プロジェクトが親の設定ファイルを上書きする」形は
   ③（一括実行が条件シートを書き換える）とも同じ。**設定を書くのは親が 1 回だけ**
2. **㉒ は報告の直し方のままだと逆に壊れた**。`model.free_edges == []` が
   「自由端が無い」ではなく「数えていない」を意味していた。
   ★**既定値の空リストを「無い」と読んではいけない**。既存テスト（test2.dxf）が
   落ちてくれたので気づけた

★次に研修室・視聴覚室を回すと、**`project.json` に残っている古い音源について
「DXF と違います」と出る**はず。`"source"` を `null` に戻せば DXF から取る。

---

### ③ 再プル：不具合報告 ㉓（HTML ビューアの操作パネル）

- 「再度プルしてきて，HTMLの話でてきてない？」→ `e637173..1d9aa47` を取り込み。
  **不具合報告 ㉓**（`view_model.py` の HTML で右の操作パネルが画面外へ押し出される）と
  修正案ブランチ **`fix/WIN240377_html_panel`**（CSS 1 行 ＋ PROGRAM_STRUCTURE の注記）が届いていた
- 原因の説明は正しい：キャンバスはフレックスの子で既定の `min-width:auto`＝**中身の大きさ**、
  その中身の大きさを**描くたびに実寸へ書き換える**ので、広げられても縮められない
- ★**ブラウザで実際に再現して確かめた**（test.dxf から修正前・修正後の HTML を作り、
  アプリ内ブラウザで幅を変えて `#panel` の見えている幅を測った）
  - 修正前：1920 幅でもキャンバスが 2000 px に膨らみ、**パネルの見える幅 0 px**
  - 修正後：2560 → 1920 → 1280 のどれでも **パネル 300 px がすべて見える**
    （キャンバスは 2260 → 1620 → 980 と正しく縮む）
  - ★スクラッチパッドの HTML は静的表示になるので、一時的にプロジェクト内へ置いて開き、
    確認後に消した
- マージはまだしていない（ユーザーの判断待ち）
- 「マージして」→ `fix/WIN240377_html_panel` をマージ。**1100 / 1100 件 OK**。
  不具合報告 ㉓ に対応済みと追記。★これで新しく書き出す HTML には症状が出ない
- 「HTML のモデルで三角形要素の表示はやめてほしい（計算をこのモデルで行っていると勘違いする）」
  → `view_model.build_payload` を本計算と同じ `coplanar_patches` の**パッチ外周だけ**描く形に。
  法線矢印もパッチごとに 1 本、レイヤ欄・概要も面（パッチ）数に。ブラウザで test / test2 を確認。
  テスト [61] 追加、**1106 / 1106 件 OK**。コミット・push 済み
- 「GUI の結果画面も三角形の表示になってないか確認して」→ 辺は消えていたが
  **法線の矢印が三角形ごと**（test2 の床に 7 本）、パネルに「三角形 N 枚」。
  `build_plotter` を計算と同じパッチ（外周・矢印とも）に、`face_editor` は面グループごとに 1 本
  （グループ内で向き・色が違えば別に立てる）。オフスクリーン描画で前後を確認。**1111 / 1111 件 OK**
- 「計算上，法線はしっかり一本に集約されていますか？」→ はい。`PatchArrays` は代表面（若い番号）の
  法線を `patch_normal` とし、`hit_id` も代表面を返すので反射・鏡像・エネルギーとも 1 本。
  実測（階段教室 779 三角形 → 229 パッチ）で代表法線とのずれ最大 0.0000°・平面距離 0.0002 mm
- 「集約した面（多頂点）から法線を出し直すのは難しいか。ずれが変わらなければそのままで良い」→
  難しくない（三角形の外積の和＝Newell 法）。階段教室で試算：代表面の法線との差 最大 0.000005°、
  頂点の平面距離 0.00023 mm で代表面のまま（0.0002 mm）と同じ → **変更しない**ことを提案
- 「ねじれのある面は集約しない想定（向き同一・同一平面・隣接・同レイヤ）。そうなっているか」→
  4 条件はあるが、★**同一平面の判定が「隣の三角形どうし」**（`coplanar_groups` の塗りつぶし）なので
  **ゆるい曲面・ねじれが連鎖して 1 面になりうる**（試作：全体で 2° 曲がる 80 三角形が 1 面。
  グループの最初の平面と比べる版なら 14 面）。距離も相手の重心 1 点しか見ていない。
  実モデル（test / test2 / 五角形 / 階段教室）では試作版と**計算のパッチは完全に同じ**（階段教室 229）。
  直すか判断待ち。レイヤ条件は `Mesh.material`（materials.json で面ごとに貼ると材料名）
- 「今は問題無さそうだけど今後問題になりそう。対応方法を考えるので保留事項として残して」→
  TODO.md「判断待ち」に **B-23** として記録（現象・試作結果・既存モデルへの影響なし・直すときの注意）
