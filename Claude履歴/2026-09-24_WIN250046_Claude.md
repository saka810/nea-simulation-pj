# 2026-09-24 WIN250046 / Claude

## 逐次ログ

- 「プルして，不具合報告確認して」→ `ff49a3d..4430bca` を早送りで取り込み
  （WIN240377 の 09-22 ぶん。研修室・視聴覚室の計算）。
  不具合報告に **⑲〜㉒** が追加、あわせて **⑱（安全率）が実案件で直っていることの確認**。
  4 件ともこの端末のコードで裏取りした。
  - ⑲ 音源が `project.json` に書き戻され DXF の更新が効かない … `run_project.py:963`
    で `project.source` を書き、966 で保存。`_sources()`（114 行）は `project.source`
    を DXF より優先する。★`run_conditions` の 472 行でも書き戻している
  - ⑳ `head_azimuth` のリストが 1 点ぶんに潰れる … 受音点ごとの子（850 行で数値を持つ）が
    `save_settings=save_settings`（229 / 264 行）のまま同じ `project.json` を保存する
  - ★★**⑲ と ⑳ は同じ根っこ**（受音点ごとの子プロジェクトが親の設定ファイルを上書きする）
  - ㉑ 垂直入射吸音率が結果に残らない（改善要望）… `吸音率と理論値.csv` は残響室法のみ
  - ㉒ T字接合を「作図ミス」と断じる … `read_dxffile.py:2051` が `model.is_closed`
    （`open_edges == 0`）と `open_edges` の本数で判定し、`free_edges` を見ていない
