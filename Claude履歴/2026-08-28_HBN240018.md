# 2026-08-28（HBN240018）

## 本日のやり取り

### 階段教室のモデルが読めない → ACIS の変換器を作った（TODO B-22 完了）

`Desktop\階段教室\260828-3_階段教室.dxf` を渡され、「読み取れる？」
「閉じた形状になっていますか？」「反射板が浮いている想定」と聞かれた。

**最初の答え**：読めない。中身が全部 ACIS だった。

| セクション | 中身 |
| --- | --- |
| ENTITIES | REGION 179 / 3DSOLID 7 / INSERT 22 / LINE 5 / HATCH 3 ほか |
| BLOCKS | REGION 168（どの INSERT からも呼ばれていない＝編集のバックアップ） |

### ユーザー指摘「REGION で読めたはずでは無いですか？」

記録を確認すると、**読めたことは一度も無い**。
`docs/DXFデータの作り方.md` に REGION が出てくるのは**平面性チェックの裏技**
としてで、そこに「REGION は本ツールでは読めない」と書いてある。
TODO **B-22 は未着手**のままだった。

ただ B-22 に「`accoreconsole` で面に分解する変換を**プログラム側から呼ぶ**のが
現実的」と書いてあり、**この端末に accoreconsole が入っていた**ので、その場で作った。

### `dxf_faces.py`（新設）

`python dxf_faces.py <DXF>` → `<名前>_faces.dxf`

AutoCAD の中で `EXPLODE` すると、REGION は**輪郭の LINE**に、3DSOLID は
**面の REGION**に分解される。それを LISP で書き出させ、Python 側で輪の順に
並べ直して**閉じた 3D ポリライン**の DXF にする。

**決めごと**

- **元のファイルは触らない**（控えを作ってそちらを開かせる）
- **レイヤ名は保つ**（吸音材の割り当てに使うので、崩れると意味が無い）
- 円弧・スプライン・**穴あき（輪が 2 つ以上）**・閉じなかった輪郭は
  **数えて知らせる**（黙って捨てない）
- 面が 1 枚も読めず ACIS だけがあるときは、**読み込み側もこの手順を出す**

**踏んだ落とし穴**

1. ★★**accoreconsole ではメッシュ化できない**（画面が無いため）。
   `MESHSMOOTH` は「認識された数: 186」と出るのに MESH を 1 つも作らず、
   `CONVTOMESH` は**そもそも無い**。使えるのは `EXPLODE` だけだった
2. ★★**`EXPLODE` は選択を空行で閉じる**（`(command "_.EXPLODE" o "")`）。
   閉じないとコマンドが開いたままになり、**次の行を選択として食べて止まる**。
   無いコマンドに `(command … "")` を渡したときも同じで、2 回固まらせた
3. REGION を分解して**また REGION が出てくる**ことがある。最初その 2 枚を
   「直線でない」として落としていた（面が 2 枚欠けた）。親と同じ待ち行列に
   戻すようにしたら 220 → 222 面になり、**容積が 918 → 3134 m³ に変わった**
   （欠けていたのが室を閉じる壁だった）

### 階段教室を読んだ結果

- **222 面 → 三角形 692 枚**、レイヤ **11 種**すべて保持
- 寸法 **21.4 × 26.0 × 9.0 m**、表面積 **2945 m²**、容積 **約 3134 m³**（目安）
- ★**閉じていない**：開いた辺 353 本、うち**自由端 27 本・126.4 m**
- ★**反射板は浮いている**（想定どおり）。シェル 2〜8 の **7 個が独立した
  閉じた塊**（13.4 × 2.26 × 0.575 m）。本体のシェルとつながっていない
- 要確認：巻き順が一貫していない（289/692 枚を自動で反転）／同じ位置に
  重なる面 2 組／面積 1 mm² 未満の面 2 枚／三角形の最小角の中央値 9.7°・
  1°未満 101 枚（**手すり 159 枚**が主因）／穴あき 1 枚（外周だけ使った）

### テスト

**[48] ACIS を面に直す**を追加（13 項目）。731 → **744 件**。
GUI の 2 ブロック（[42][43]）は昨日と同じく**この端末では走らせられない**
（RDP 越しで OpenGL の窓が取れない）。残り **667 / 667 OK**。

---

## 逐次ログ

- 階段教室の DXF を確認。全部 ACIS（REGION 179 / 3DSOLID 7）で読めないと報告。
  レイヤ構成（階段・バルコニー／建具／手すり／1F・2F 壁／垂れ壁／天井／
  反射板／床）を提示。
- 「REGION で読めたはずでは？」との指摘。記録を確認して、読めたことは無く
  B-22 が未着手であることを説明。accoreconsole があったので変換器を作った。
- `dxf_faces.py` 新設。階段教室を 222 面に変換して読み込み、閉じ具合・
  反射板の浮き・三角形の品質を確認。テスト [48] 追加、資料を更新。
