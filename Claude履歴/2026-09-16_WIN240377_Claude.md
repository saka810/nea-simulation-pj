# 2026-09-16 WIN240377_Claude

## 本日のやり取り

階段教室を**音源 2 点で計算し直した**。⑨ の修正は入っていたが、
**古い `project.json` の `source` が効いていて 1 回無駄打ちした**（⑩ として報告）。

### ① 修正版を取り込んだ

`git pull` で WIN250046 の `5c05164`（不具合 ⑨ の対応）が入った。

- `geosim/source_mix.py`（510 行）… 音源ごとに計算し、合成の仕方を選ぶ
- `geosim/point_order.py`（327 行）… 測定点の並び替え（`rec1` を R1 に合わせる）
- `run_project.py` +194 / `project.py` +116 / `setup_window.py` / `app.py` ほか
- **テスト 925 / 925 件 OK**（881 → 925）
- PR #1（POINT の引き継ぎ）も `267580c` でマージ済み

設定に **「音源が複数のとき」**（`source_combination`）が増えた。
すべて / 個別のみ / 個別＋平均 / 個別＋重ね合わせ（時間差あり・なし）。

### ② ★1 回無駄打ちした（不具合 ⑩）

新しいコードで回したのに `結果/src1` `src2` が作られず、**また S1 だけ**だった。
**5 時間を無駄にした。**

原因は `project.json` の `"source": [15.0, 1.0, 1.5]`。
`_sources()` は `project.source` を最優先するので、**1 点返して終わっていた**。
その値は `run_project.py:404` が**計算のたびに書き戻していた**もの。
つまり**1 度でも計算したプロジェクトは音源 1 点に固定され、⑨ を入れても直らない**。

`"source": null` に戻し、**回す前に音源の数を確かめてから**再実行した。

```
音源の数: 2
  src1 = [15.0, 1.0, 1.5]
  src2 = [6.0, 1.0, 1.5]
```

★**今後は長い計算の前に必ずこの確認をする。**

### ③ 2 音源の結果（音線 100 万本 / 受音球 2.5 m / 最大反射 120 回 / 応答 5.0 s）

構成は `結果/src1` `src2` `合成_平均` `合成_重ね合わせ` `合成_重ね合わせ_時間差なし`。
`測定点.csv` に `src1からの距離_m` `src2からの距離_m` が両方入った。

**5 点平均（合成_平均）**

| 指標 | 63 | 125 | 250 | 500 | 1k | 2k | 4k | 8k |
|---|---|---|---|---|---|---|---|---|
| EDT [s] | 1.57 | 1.41 | 1.18 | 0.87 | 0.95 | 0.89 | 0.79 | 0.60 |
| T20 [s] | 1.50 | 1.23 | 1.46 | 1.33 | 1.32 | 1.21 | 1.03 | 0.74 |
| T30 [s] | 1.61 | 1.32 | 1.60 | 1.51 | 1.53 | 1.42 | 1.15 | 0.78 |
| 理論 E-K | 2.28 | 1.12 | 0.71 | 0.48 | 0.45 | 0.45 | 0.43 | 0.38 |
| C50 [dB] | −7.3 | −0.1 | 0.1 | 3.8 | 3.6 | 3.6 | 4.4 | 6.5 |

**STI**：src1 0.670 / src2 0.674 / **合成_平均 0.672（良 good）**／
重ね合わせ 0.668（時間差あり）・0.671（時間差なし）。ばらつき 0.033。

- ★**src1 の値は単一音源で回した 100 万本と完全に一致**
  （EDT 0.8775 / T20 1.3554 / T30 1.5420）。新しいコードが従来と同じ答えを出す確認になった
- ★**S1 と S2 の差が小さい**（T30 500 Hz で 1.542 対 1.487）。配置がほぼ左右対称なので妥当
- ★**平均すると受音点間のばらつきが減る**（T30 500 Hz で 0.23/0.31 → 0.18）。
  音源位置を変えて平均する ISO 3382 の効き目がそのまま出ている
- ★**T20・T30 は相変わらず理論値の 3 倍**で、ξ も 10 を超えるバンドが多い。
  音源を 2 点にしても**二段階減衰は解消しない**（室の性質なので当然）

**使える指標**：EDT・C50・D50・STI（収束している）
**使えない指標**：T20・T30（二段階減衰。1 本の直線で読めない）

---

### 逐次ログ

- 「修正版あがってるはずなのでプルして」→ `5c05164` ほかを取り込み、テスト 925 件 OK。
  `source_mix.py` / `point_order.py` の中身と新しい設定項目を確認して報告
- 「おススメに従います」→ 200 万本（単一音源）を止め、2 音源で回し直し
- ★ところが `project.json` の `source` が効いて**また単一音源**（5 時間の無駄）。
  原因を特定して `null` に戻し、**音源が 2 点と認識されることを確認してから**再実行
- 2 音源・100 万本の結果を取得。src1 が従来の単一音源の結果と一致することも確認
- 不具合 ⑩ を報告書に追記して push
