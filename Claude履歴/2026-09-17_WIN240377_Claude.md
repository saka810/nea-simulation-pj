# 2026-09-17 WIN240377_Claude

## 本日のやり取り

階段教室の**吸音条件を変えて回した**（条件0 → 条件1。PHP を 0.8 がけ）。
回している間に不具合を 2 件見つけた（⑭ ⑮）。

### ① 吸音条件を変えて回した（条件0 / 条件1）

> 吸音条件はこのシートの「条件1」です。PHPシリーズの吸音が 0.8 がけになっているはずです。
> 今までのは「条件0」とします。残響室法なので，垂直入射に変えてくださいね。

材料は `4_吸音率\★吸音率リスト.xlsx`。**条件0 は従来の「現状」と 23 層すべて一致**していた。
条件1 との差は **PHP 6 層だけ**で、比はきっちり 0.800。

**やったこと**

- 条件表の吸音率シートに `ピンホールパネル（AS0mm）80%開口` を追加（材料番号 12、**種類は残響室法のまま**）
- シート `現状` → `条件0` に改名、`条件1` を追加（PHP 6 層の材料番号を 12 に）
- 計算済みの結果・図 **391 件**を `階段教室_現状_*` → `階段教室_条件0_*` に改名（計算はやり直さない）
- 9/15 の単一音源のときの `図/recN/` も `図/旧_単一音源S1のみ_260915/` へ退避

★**垂直入射への変換はプログラムに任せた**（値を手で変換しない）。
安全率をカタログ値に掛けてから変換する約束と順番が合う。

| PHP 6 層 | 63 | 125 | 250 | 500 | 1k | 2k | 4k | 8k |
|---|---|---|---|---|---|---|---|---|
| 条件0 残響室法 | 0.030 | 0.060 | 0.230 | 0.700 | 1.000 | 1.000 | 0.890 | 0.801 |
| 条件0 **垂直入射** | 0.015 | 0.032 | 0.135 | 0.533 | 0.951 | 0.951 | 0.785 | 0.653 |
| 条件1 残響室法 | 0.024 | 0.048 | 0.184 | 0.560 | 0.800 | 0.800 | 0.712 | 0.641 |
| 条件1 **垂直入射** | 0.012 | 0.025 | 0.105 | 0.393 | 0.652 | 0.652 | 0.547 | 0.471 |

★**変換後の比は 0.8 にならない**（0.69〜0.80）。Paris の式が非線形なため。
1k・2k で比が小さいのは、**条件0 側が上限 0.951 で頭打ち**になっているから。

### ② 結果（5 点平均・合成_平均）

★**経路キャッシュが効いて 4 分 35 秒で終わった**（前回は音線追跡込みで 2.5 時間）。
吸音は経路の幾何に効かないので、`energy_from_geometry()` だけで済む。

| 指標 | 63 | 125 | 250 | 500 | 1k | 2k | 4k | 8k |
|---|---|---|---|---|---|---|---|---|
| EDT 条件0 | 1.57 | 1.41 | 1.18 | 0.87 | 0.95 | 0.89 | 0.79 | 0.60 |
| EDT 条件1 | 1.57 | 1.42 | 1.21 | 0.91 | 0.98 | 0.91 | 0.82 | 0.62 |
| T30 条件0 | 1.61 | 1.32 | 1.60 | 1.51 | 1.53 | 1.42 | 1.15 | 0.78 |
| T30 条件1 | 1.62 | 1.32 | 1.60 | 1.51 | 1.52 | 1.41 | 1.14 | 0.79 |
| C50 条件0 | −7.29 | −0.06 | 0.13 | 3.82 | 3.56 | 3.57 | 4.37 | 6.47 |
| C50 条件1 | −7.30 | −0.11 | −0.02 | 3.55 | 3.18 | 3.29 | 3.98 | 6.15 |

**STI**：0.672 → **0.664**（−0.008）。5 棚とも同じ下がり方。評価は「良 (good)」のまま。
**等価吸音面積 A（500 Hz）**：965.1 → 926.3 m²（−4.0%）。平均吸音率 0.318 → 0.306。

- ★**効きが小さい**。PHP 6 層は合計 277 m² で、総表面積 3032 m² の 9%。
  吸音を支配しているのは **GW 647 m²（α_n 0.951）**なので、
  PHP を 2 割落としても全体の吸音力は 4% しか減らない
- ★**EDT は素直に動く**（+0.04 s / 500 Hz）が、**T20・T30 はほぼ動かない**。
  二段階減衰の後半（硬い面だけの空間）を読んでいるため、PHP の増減が効かない。
  ⑦（9/16）の「使える指標 / 使えない指標」の切り分けがここでも裏付けられた

### ③ 回している間に不具合を 2 件見つけた（⑭ ⑮）

- **⑭ 条件を変えて回すと前の条件の図が全部消える**（`clear_results` が PNG を
  名前も見ずに消す）。実測で**条件0 の PNG 110 枚が消えた**。
  ★回す前に `図/` を控えに退避しておいたので戻せた。**一括実行だと最後の条件の図しか残らない**はず
- **⑮ 音源を合成するとき見出しの行も数値として平均**していて、
  `10^(8000/10)` でオーバーフローの警告が毎回出る。
  出力は正しい（dB の行だけ差し替えるので壊れた見出しは捨てられる）が、
  本物の異常が埋もれる

---

### 逐次ログ

- 「吸音条件を変更して解析を回してほしい」→ ★吸音率リスト.xlsx の条件0 が従来の「現状」と
  一致することを確認。条件表に材料 12（PHP 80%開口）と条件1 シートを追加、現状→条件0 に改名
- 計算済み 391 件を `条件0` の名前に改名。`図/recN/`（9/15 の単一音源分）も退避
- ★回す前の確認（音源 2 点・`source` が null・音線本数が条件0 と同じ）を通してから実行
- ★`clear_results` が図を全部消すことに実行前に気づき、`図/` を退避してから回した（⑭）
- 4 分 35 秒で完了（経路キャッシュ）。条件0 との比較を取得し、OneDrive へ反映

### ④ 条件2 も回した（PHP → 3M 吸音パネル）

条件2 は 0.8 がけではなく**別材料**（`3M吸音パネル（AS0mm）80%開口率`）。
材料番号 13 として吸音率シートに追加し、条件2 シートを作って回した（1 分 49 秒）。

| 指標（5 点平均） | 500 | 1k | 2k | 4k |
|---|---|---|---|---|
| EDT 条件0 | 0.87 | 0.95 | 0.89 | 0.79 |
| EDT 条件1 | 0.91 | 0.98 | 0.91 | 0.82 |
| EDT 条件2 | 0.87 | **1.10** | **1.16** | **1.07** |
| C50 条件0 | 3.82 | 3.56 | 3.57 | 4.37 |
| C50 条件2 | 3.77 | **2.29** | **1.81** | **2.34** |

**STI：条件0 0.672 / 条件1 0.664 / 条件2 0.634**

★**条件2 は中音域では条件0 と同等以上だが、1 kHz 以上で明らかに悪い。**
3M パネルは 500 Hz に山のある共鳴型で、PHP が持っていた高域の吸音
（α_n 0.95 → 0.65）が無くなる。子音を運ぶ帯域なので STI と C50 に効く。

### ⑤ 高速化の提案を出した（`評価レポート/2026-09-17_高速化の提案_…`）

条件2 の実行を工程ごとに計測（109.2 秒）。

| 工程 | 回数 | 合計 s | 割合 |
|---|---:|---:|---:|
| ① DXF を読む | **17** | 35.45 | **32.5%** |
| ⑬ 音源の合成 | 1 | 25.23 | 23.1% |
| ⑥ インパルス応答 | 15 | 12.07 | 11.0% |

★**1 条件で DXF を 17 回読んでいる**（うち 10 回は音源 × 受音点ごと）。
`read_model` の重い工程（三角形分割・同一平面グループ・容積・開いた辺・
作図ミスの確認）は**すべて材料に依らない**。パッチの併合だけが材料に依るが
0.04 秒/回。提案①（モデルを 1 回だけ読む）で 1 条件あたり −33 秒。

### ⑥ STI の暗騒音

★**`noise_level_db` だけ入れても効かない**（`source_power_db` と両方そろって
いるときだけ。`sound_level.speech_transmission_index`）。

計算済みのパルス列から、受音点の音声を 65 dB(A) に合わせて感度を見た。

| 暗騒音 | dB(A) | SNR(1k) | STI 条件0 | STI 条件2 |
|---|---:|---:|---:|---:|
| 騒音なし | − | − | 0.672 | 0.634 |
| NC-30 | 39.8 | 27.4 | 0.647 | 0.608 |
| NC-35 | 44.2 | 22.4 | 0.626 | 0.588 |
| NC-40 | 49.0 | 17.4 | 0.584 | 0.550 |

★**下がり方は条件に依らずほぼ同じ**（NC-30 で −0.025、NC-35 で −0.046）。
条件の優劣を比べる目的なら暗騒音は入れても入れなくても順位は変わらない。

---

### 逐次ログ（続き）

- 「⑭⑮ コミットプッシュ」→ 前ターンで push 済み（`a3ed3ed`）と確認。
  別セッションの `aa07a01` が入って行番号がずれていたので引用箇所を修正
- 「材料だけ変わる場合の高速化を提案して」→ 条件2 を計測付きで回し、
  実測をもとに提案を書いて push（`0b84f49`）
- 「条件2 もよろしく」→ 材料 13 を追加して実行。OneDrive へ反映（条件2 296 件）
- 「STI の暗騒音はどんな値が妥当か」→ PWL も要ることを指摘し、NC 曲線で感度を提示

### ⑦ STI の音声レベルとスペクトル（規格の原本で確認）

★**原本を確認した**：`OneDrive\…_技術_基準・指針\IEC\STI_iec60268-16{ed5.0}b.pdf`
（**IEC 60268-16:2020 ＝ ed5**）。PDF を開く手段が無かったので、
**プロジェクトの venv は汚さず**スクラッチに `pypdf` を入れて読んだ。

**① 65 dB(A) は規格の値ではない**（Annex J、normative）

- **J.3** … PA が無い場合（生声・置き型音源）は **1 m で 60 dB(A)**。
  声を張る条件（Lombard 効果）を模擬するなら **70 dB(A)**
- **J.3 冒頭** … 「For measurements on a PA system, the corrected (real) speech
  level is the level actually obtained from the system…at a specified reference
  position」＝**拡声は「その設備が実際に出すレベル」を使う**。固定値の規定は無い
- **Table D.4（Scenario 4, lecture）** … 受聴点は
  **「想定される最悪の席を必ず含む」**こと

→ 65 dB(A) は 60（通常の生声）と 70（声を張る）の間の**実務の慣用値**。
拡声なので、規格に沿うなら**設計 SPL を決めてそれを使う**のが正しい。

**② ★音声スペクトルは ed4 と ed5 で違う**

最初 ed4 の値（125:62.9 / 250:62.9 / 500:59.2 …）を使っていた。
ed5 の **Table A.4**（A 特性音声レベルに対する相対値 [dB]）は

| 125 | 250 | 500 | 1k | 2k | 4k | 8k | A 特性 |
|---|---|---|---|---|---|---|---|
| −2.5 | +0.5 | 0 | −6 | −12 | −18 | −24 | 0,0 |

規格自身が "The male spectrum **has changed in this edition**" と注記している。
検算：A 特性合計 = +0.02 dB（規格の 0,0 と一致）。

### ⑧ フラット vs 音声スペクトル（拡声・条件0/1/2）

受音点の音声レベル 65 dB(A)（5 点平均・2 台合計）。

| 条件 | 音源スペクトル | 騒音なし | NC-30 | NC-35 | NC-40 |
|---|---|---|---|---|---|
| 条件0 | フラット | 0.672 | 0.647 | 0.626 | 0.584 |
| 条件0 | **音声(A.4)** | 0.672 | 0.627 | **0.588** | 0.522 |
| 条件1 | フラット | 0.664 | 0.640 | 0.620 | 0.579 |
| 条件1 | **音声(A.4)** | 0.664 | 0.621 | **0.582** | 0.518 |
| 条件2 | フラット | 0.634 | 0.608 | 0.588 | 0.550 |
| 条件2 | **音声(A.4)** | 0.634 | 0.598 | **0.565** | 0.509 |

★**フラットは STI を 0.02〜0.04 甘く見る。**理由はバンド別 SNR（NC-35）。

| | 500 | 1k | 2k | 4k | 8k |
|---|---|---|---|---|---|
| フラット | 18.5 | 22.4 | 24.3 | 24.8 | 24.3 |
| 音声(A.4) | 24.9 | 22.8 | 18.7 | **13.2** | **6.7** |

実際の音声は高域が −18 / −24 dB 落ちるので、同じ暗騒音でも高域の SNR が
11〜18 dB 悪い。**拡声の検討はフラットではなく Table A.4 で。**

**音声レベルの効き**（NC-35・音声スペクトル）

| 条件 | 60 dB(A) | 65 dB(A) | 70 dB(A) |
|---|---|---|---|
| 条件0 | 0.521 | 0.588 | 0.628 |
| 条件1 | 0.516 | 0.582 | 0.622 |
| 条件2 | 0.507 | 0.565 | 0.599 |

★★**NC-35・65 dB(A)・実スペクトルだと、条件0 でも STI 0.588 で
「可 (fair)」に落ちる**（良 (good) は 0.60 以上）。0.60 を確保するには
**拡声レベルを 66〜67 dB(A) 以上**にするか、**暗騒音を NC-30**（条件0 で 0.627）。

---

### 逐次ログ（続き）

- 「65 dB(A) に指標はあるか／講堂なので NC-35／拡声・置き型スピーカ／
  フラット vs 実スペクトルを条件0/1/2 で」→ IEC 60268-16:2020 の原本
  （Annex J・Table A.4・Table D.4）を確認。**使っていた音声スペクトルが ed4 の
  ものだった**ので ed5 の値に直して再計算。フラットは STI を 0.02〜0.04 甘く見る

---

## 2026-09-18（追記）

### ⑨ 条件3〜10 を回した／比較表とモデル諸元

★吸音率リスト.xlsx に条件3〜10 が追加されたので取り込んだ。
材料を **6 種追加**（コンクリート打ち放し面 / パーティールーム / 食堂 /
低周波吸音材（広帯域） / 同 +GW32K25x2 / ピンホールパネル96k（AS300）80%開口率）。
既存 13 種は元リストと**値が完全一致**。全 11 条件 × 22 レイヤを検算して食い違い 0。

★**計算結果が「見当たらない」と言われた。原因は OneDrive へのコピー忘れ**
（ローカルには全部あった）。反映して各条件 296 件になった。

**モデル諸元**（条件に依らない）

| | |
|---|---|
| 容積 | **3,490.4 m³**（法線・発散定理。自由端 0 本なので信用できる） |
| 総表面積 | **3,031.89 m²** |
| 外形 | 21.44 × 25.96 × 9.00 m / 4V/S = 4.60 m |
| メッシュ | 三角形 779 枚・同一平面パッチ 223 枚・画層 22 種 |

★**`吸音率と理論値.csv` に総表面積はあるが容積が無い**（理論残響は容積で決まるのに
結果から追えない）。`結果/階段教室_モデル諸元.csv` を別に作った。**本体への要望候補**。

**11 条件の比較**（5 点平均・合成_平均）

| 条件 | A₅₀₀ | ᾱ₅₀₀ | EDT₅₀₀ | EDT₂ₖ | T30₅₀₀ | C50₁ₖ | STI 静 | STI NC-35 |
|---|---|---|---|---|---|---|---|---|
| 条件0 | 965 | 0.318 | 0.87 | 0.89 | 1.51 | 3.56 | 0.672 | 0.588 |
| 条件1 | 926 | 0.306 | 0.91 | 0.91 | 1.51 | 3.18 | 0.664 | 0.582 |
| 条件2 | 993 | 0.327 | 0.87 | 1.16 | 1.51 | 2.29 | 0.634 | 0.565 |
| 条件6 | **324** | **0.107** | **1.53** | **1.48** | 1.69 | **−1.27** | 0.549 | 0.486 |
| 条件10 | 960 | 0.317 | 0.86 | 0.91 | 1.51 | 3.22 | 0.667 | 0.586 |

- ★**条件6（GW → コンクリート）だけが別次元で悪い**。A が −66%。
  GW 647 m² が吸音の 2/3 を担っていることが数字で出た
- ★**条件8・9・10（低周波吸音材）は条件1 とほぼ同じ**。中高域の α が GW と同等なため。
  ただし **250 Hz・4k で残響室法の値が 1 を超えて上限 0.951 に丸められている**ので、
  **低域の改善効果はこの計算では出せていない**
- ★**T30 は条件6 以外すべて 1.50〜1.51 s で動かない**（二段階減衰）。
  EDT・C50・STI で判断する
- ★★**NC-35・65 dB(A) では 11 条件すべて「可 (fair)」**（最良の条件0 で 0.588）。
  材料だけでは 0.60 に届かない

出力：`結果/階段教室_条件比較.xlsx`（7 シート）と `結果/階段教室_モデル諸元.csv`。

### ⑩ ⑪⑫ がマージされた／⑭ の修正案を出した

- `6c4d622` で**私のブランチ `fix/WIN240377_source_shelf` がマージ**され、
  さらに `1172119` で開発者側が**同型を 3 か所**直していた。
  「前回の結果を見る」も虚音源のタブも回避策なしで使える
- **⑭ の修正案を `fix/WIN240377_figure_clear`（`5add9e7`）で出した**。
  `Project.owns_figure()` を足し、CSV 側と同じ「対象室名＋条件名」で
  **いまの条件の図だけ**消す。実案件と同じ 121 枚で検証（11 枚だけ消えて 110 枚残る）。
  テスト 1001 / 1001 件 OK（996 → 1001。⑭ のテストを 5 件追加）

---

### 逐次ログ（2026-09-18）

- 「吸音条件の表を更新したから計算して」→ 条件3〜10 を取り込み（材料 6 種追加）、
  全条件の検算後に 8 条件を実行（合計 36 分）。★⑭ を避けるため図を 1 条件ごとに退避
- 「容積と表面積は？」→ 3,490.4 m³ / 3,031.89 m²。画層別面積も提示
- 「結果が見当たらない／比較表も／諸元も入れて」→ **OneDrive へのコピー忘れ**が原因。
  反映したうえで `階段教室_条件比較.xlsx` と `階段教室_モデル諸元.csv` を作成
- 「GUI 開いて（条件1 で）」→ ⑪⑫ がマージ済みなのを確認して起動
- 「⑭ の修正案ブランチして」→ `fix/WIN240377_figure_clear` を作成・push、報告書に追記

### ⑪ ⑭ をマージ、⑮ の修正案も出した（2026-09-18）

- ★**ユーザー指示で ⑭ を main へマージ**（`627ff9a`。`--no-ff`）。
  マージ後の main でテスト 1001 / 1001 件 OK。GUI を条件1 で開き直した
  （`owns_figure` の検算：条件1 の図 → 消す / 条件0 の図・測定点図 → 残す）
- **⑮ の修正案を `fix/WIN240377_mix_header`（`c943b0c`）で出した**。
  `_average_rows(tables, energy=…)` が**行ごとの真偽のリスト**も取れるようにして、
  表を丸ごと 2 通り平均してから差し替えるのをやめた。
  実データ 220 表で**直す前と完全一致**、`-W error::RuntimeWarning` でも止まらない。
  テスト 1006 / 1006 件 OK（1001 → 1006。⑮ のテストを 5 件追加）

- 「合成で見出しを数値平均ってなんだっけ」→ 再現して説明。
  見出しの `63, 125, …, 8000` まで数値として平均され、エネルギー平均だと
  `10^(8000/10)` がオーバーフローして `4000` `8000` が `inf` になる。
  出力は正しい（dB の行だけ差し替えるので壊れた見出しは捨てられる）
- 「マージして開きなおして」→ ⑭ をマージして GUI 再起動
- 「⑮ のブランチ出して」→ `fix/WIN240377_mix_header` を作成・push、報告書に追記

### ⑫ 条件11・12 を追加して回した（2026-09-18）

材料を 2 種追加（**20 `ｶﾞﾗｽ`** / **21 `開口（反射無し）`**）。13 条件 × 22 レイヤを
検算して食い違い 0。2 条件で 214 秒（各 107 秒）。

| 条件 | 条件0 からの差 |
|---|---|
| 条件11 | 条件1 ＋ `ガラス` を **`ｶﾞﾗｽ`**（低域の吸音が小さい別材料）に |
| 条件12 | 条件1 ＋ `開口_2F大` を **`開口（反射無し）`**（α=1.0）に |

★★**`開口（反射無し）` は反射無しにならない。**残響室法 1.0 は垂直入射へ直すと
**上限 0.951 で頭打ち**になる。さらに根本的に、局所反応モデルの反射率は
`R(θ) = (z cosθ − 1)/(z cosθ + 1)` で**斜めに入るほど |R| が 1 に近づく**ので、
**吸音率をどう指定しても「音が出ていって戻らない」は表せない**。
条件12 は「よく吸う開口」であって「開口」ではない。
本当に必要なら**音線をその面で打ち切る**扱いが要る（要望候補）。

★`ｶﾞﾗｽ`（半角カナ）と `ガラス はめころし` は別材料。名前が紛らわしく
取り違えやすいので、`ガラス_厚6mm` のような区別できる名前を勧めた。

**13 条件の比較**（5 点平均・合成_平均）

| 条件 | A₅₀₀ | EDT₁₂₅ | EDT₅₀₀ | T30₅₀₀ | C50₁ₖ | STI 静 | STI NC-35 |
|---|---|---|---|---|---|---|---|
| 条件0 | 965 | 1.41 | 0.87 | 1.51 | 3.56 | 0.672 | 0.588 |
| 条件6 | 324 | 1.69 | 1.53 | 1.69 | −1.27 | 0.549 | 0.486 |
| 条件11 | 923 | **1.59** | 0.92 | **1.55** | 3.18 | 0.662 | 0.581 |
| **条件12** | 960 | 1.36 | **0.84** | **1.44** | **3.65** | **0.677** | **0.590** |

- ★**条件12 が全条件で最良**（STI 0.677 / NC-35 で 0.590）。開口をよく吸わせると
  T30 も 1.51 → 1.44 と**唯一はっきり動いた**。開口が二段階減衰の後半に
  効いていることの裏付け。ただし上限 0.951 で頭打ちなので**これが上限ケース**
- ★**条件11 は低域が伸びる**（EDT 125 Hz が 1.42 → 1.59）。`ｶﾞﾗｽ` の
  低域の吸音が 0.18 → 0.04 と小さいため。ガラス 298 m² は面積が大きいので効く
- ★**NC-35・65 dB(A) では条件12 でも 0.590** で、依然 0.60（良）に届かない

★**⑭ の修正が実地で効いた**：実行ログが「消されていた図を **0 枚** 戻しました」。
直す前なら 1 条件ごとに 110 枚ずつ消えていた。

### 逐次ログ（続き）

- 「⑬ 面数の列足して／容積も行を足して／ブランチ出して」→ ⑬ は**既に main に
  入っていた**（`COLUMN_PATCHES`。`layer_patch_counts` まで実装済み）。
  容積は `feat/WIN240377_room_volume`（`ee5cc43`）で出した（区分「室の諸元」）
- 「条件11 と 12 を足したので計算して」→ 材料 2 種を追加して実行。
  `開口（反射無し）` が反射無しにならないことと、`ｶﾞﾗｽ` の紛らわしさを報告

### ⑬ 正面ガラスを分けた／条件13・14（2026-09-18 夕）

**レイヤを分けたい** → 方法は 2 つあり、**条件ごとに材料を変えたいなら CAD で画層を分ける**
（`materials.json` の面ごと割り当ては**材料名を直接持つ**ので全条件で固定になる）。

対象は **モデル Y = −0.736 m（CAD Y = +0.736）の面**。34 枚 / 135.97 m²。
★分ける前に**パッチの切れ目が動くか**を確かめた（動かなければ経路キャッシュが使える）。
1 回目の CAD 修正では**極小の細片 4 枚（計 0.0068 m²）が ガラス に取り残され**、
パッチが 223 → 224 に割れてキャッシュが無効になると分かったので、CAD で直してもらった。
2 回目で取り残しゼロ・**パッチ 223 で並びまで同一**になった。

### ⑭ ★DXF を作り直すと音源・受音点の並びが変わる（不具合 ⑯）

画層名だけ変えた DXF で **src1/src2 が入れ替わり、受音点も R1,R2,R5,R3,R4 の順**になった
（三角形は頂点の並び順まで完全一致）。経路キャッシュの指紋が合わず**音線追跡から
やり直し**になり、**50 分回してから気づいて中止**した。

```
[経路] 保存した経路は使えません: 音源の位置が違います
       （source: 保存 [15.0, 1.0, 1.5] / いま [6.0, 1.0, 1.5]）
```

回避は `測定点順.json`（`point_order.py`。**音源にも使える**）。
`sources=[1,0] / receivers=[0,1,3,4,2]` で src1=S1・rec1=R1 に戻した。報告書 ⑯ に追記。

★1 回目の DXF では **R4 の X が 13165 → 13500 に戻っていた**（9/15 の指示ぶんが
巻き戻っていた）。回す前に当初指示と突き合わせて気づけた。CAD で修正済み。

### ⑮ 条件1・13・14 の結果

★**条件1 のパルス列は以前と完全一致**（差 0.000e+00・全 14 列）＝物理計算は同じ。
指標だけ EDT 0.003 s・C50 0.04 dB ほど動いたが、これは **`83fbc03`
（周波数バンドを IEC 61260-1 のベース10 に揃える）**の影響で、規格に沿う方向の改善。
→ ★**条件0・2〜12 は古いコードのままなので、条件をまたぐ比較には回し直しが要る**。

| 条件 | PHP | ガラス＋正面ガラス | 開口_2F大 | STI 静 | STI NC-35 |
|---|---|---|---|---|---|
| 条件1 | ピンホール80% | はめころし | 開口 | 0.664 | 0.582 |
| **条件13** | ピンホール80% | **ｶﾞﾗｽ** | **反射無し** | **0.674** | **0.589** |
| 条件14 | **3M** | ｶﾞﾗｽ | 反射無し | 0.642 | 0.572 |

- **条件13 が最良**。開口をよく吸わせた効果が主（条件12 と同じ傾向）
- **条件14 は 500 Hz で最良（C50 4.31 dB）なのに 1 kHz 以上で悪化**（C50 2.1〜2.6 dB）。
  3M パネルは 500 Hz に山のある共鳴型で高域を吸わない＝子音の帯域が落ちる
- 低域は 3 条件とも悪化（`ｶﾞﾗｽ` の 125 Hz が 0.18 → 0.04。ガラス 298 m² は面積が大きい）

**減衰曲線**（`図/階段教室_減衰曲線比較_1-13-14.png`。横軸 3 s・縦軸 0〜−60 dB・全 8 帯域）
- ★3 条件とも **−30 dB 付近で折れる二段階減衰**がはっきり見える。
  T30 が材料を変えても動かない理由がそのまま絵になっている
- 63 Hz だけ別格に長い（条件13・14 は 2.5 s でも −60 dB に届かない）
- 条件14 は 4k・8k が中域と束になる（高域を吸わない）

### ⑯ OneDrive への反映は「言われなくても必須」

> もちろん，言われなくても移すのは必須です。（2026-09-18 ユーザー指摘）

★**成果物ができた時点で毎回移す。「反映しますか？」と聞かない。**
対象は結果・図だけでなく **DXF・条件表・project.json・測定点順.json** まで含める
（`測定点順.json` が無いと他端末で src1/rec1 の名前がずれる）。記憶にも書いた。

置き場はモデルごとに分けた（前のフォルダは**上書きしていない**）。

    WIN240377_260914モデル_音源S1S2\        前のモデル。13 条件が揃った状態で温存
    WIN240377_260918モデル_正面ガラス分離\   今回。このフォルダについて.txt に世代を明記

---

### 逐次ログ（続き）

- 「レイヤーを分けたい／できる？」→ 方法 2 つと経路キャッシュへの影響を説明。
  対象面を特定し、**分ける前にパッチが動かないことを確認**
- 「CAD で直した」→ 1 回目は細片 4 枚の取り残しでパッチが割れると判明、再修正を依頼。
  2 回目で完全一致
- 「★吸音率リストもレイヤー行増やせる？」→ 13 シートに挿入（★数式だったので
  248 個を振り直し）。その後ユーザーが自分で書き直したので、そちらを正として読み込み
- 「条件1，13，14 について計算して」→ ★DXF の並び替えと R4 の位置ずれを発見。
  R4 は CAD で修正、並びは 測定点順.json で復元してから実行（5 分半）
- 「残響曲線を見たい」→ 3 条件 × 全帯域の減衰曲線を作図
- 「言われなくても移すのは必須」→ OneDrive へ反映し、記憶を更新
