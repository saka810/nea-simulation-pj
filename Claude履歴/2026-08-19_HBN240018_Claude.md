# 2026-08-19 HBN240018

## 本日のやり取り

`C:\Users\sakayoshi\Desktop\ModelTest\ModelTest.dwg`（新形式のモデル）が読めるかの検証。
**結論：そのままでは読めない（幾何が 3DSOLID 1 個）。面に変換すれば完全に読める。**

---

## 1. 渡されたモデルの中身

`.dwg`（AC1032 = AutoCAD 2018 形式）なので、まず DXF に変換して中身を見た。
変換は端末に入っている **AutoCAD 2025 の `accoreconsole.exe`**（GUI 無しのコマンド実行）。

| 項目 | 中身 |
|---|---|
| 単位 | `$INSUNITS=4`（mm） |
| 範囲 | X 0〜16145 / Y 0〜17950 / Z 0〜3000 mm（= 16.1 × 17.95 × 3.0 m） |
| 幾何 | **`3DSOLID` が 1 個だけ**（レイヤ `01_建具高さ`） |
| 音源 | `src` レイヤの `POINT` 1 個（7322.6, 14471.2, 1500.0） |
| 受音点 | `rec` レイヤの `POINT` 1 個（5663.6, 9122.0, 1200.0） |
| レイヤ | `0` / `01_建具高さ` / `src` / `rec` の 4 つだけ |

ユーザーの言う「ブロックで出来ている」は DXF 上は **BLOCK/INSERT ではなく
ACIS ソリッド（3DSOLID）**だった。BLOCKS セクションには `*Model_Space` 等の
既定のものしか入っていない。

## 2. そのまま読ませた結果

```
[read_dxffile] 三角形メッシュ: 0 枚
[read_dxffile] 読み飛ばし: {'非対応エンティティ': {'3DSOLID': 1}}
[read_dxffile] 音源: [[7.3226, 14.4712, 1.5]]
[read_dxffile] 受音点: [[5.6636, 9.122, 1.2]]
```

`read_dxffile` が読めるのは面（`3DFACE` / ポリフェイスメッシュ / 閉じたポリライン）だけ。
3DSOLID は **境界表現（B-rep）の ACIS データ**で、DXF 上は暗号化されたバイナリなので
自前パーサでは中身に手が届かない。**src / rec は正しく読めている。**

## 3. 面に変換して読ませた

AutoCAD 側で三角形に割ってから 3DFACE として書き直した。

- `CONVTOMESH`（ソリッド→メッシュ）は **accoreconsole には無い**（GUI 版のみ）。
- 代わりに **`STLOUT`** でソリッドを三角形化 → ASCII STL（68 三角形）。
- STL には レイヤが無いので、元 DXF から レイヤ名（`01_建具高さ`）・`$INSUNITS`・
  src/rec の POINT を引き継いで DXF を組み立てた（変換スクリプトは scratchpad に置いた。
  リポジトリに入れるかは**未確認**）。

読み込み結果：

```
三角形メッシュ: 68 枚
単位: 1 CAD単位 = 0.001 m（$INSUNITS=4）
レイヤ別の枚数: {'01_建具高さ': 68}
寸法: [16.145, 17.95, 3.0] m (min=[0.0, 0.0, 0.0])
シェル: 1 個 / 閉 / 体積 770.3284 m³ / 法線=outward → inward へ自動反転
面の元になったエンティティ: {'3DFACE': 68}
[check] 作図チェック: 問題は見つかりませんでした
```

- 音源・受音点とも **囲まれ度 1.0**（室内にある）。
- 総面積 **724.09 m²**、体積 **770.328 m³**。
- AutoCAD の `MASSPROP` で測ったソリッドの体積は **7.7033E+11 mm³ = 770.33 m³**、
  境界ボックスも一致。**STL の三角形分割は（面が全部平面なので）誤差なし。**

## 4. 積み残し

- **吸音面の設定**。全 68 面が同一レイヤ `01_建具高さ` なので、面ごとの吸音率を
  レイヤで振り分けられない。今回は「そこまでは不要」との指示。
  やるなら「法線の向き・座標で面を分類する」か「変換時にレイヤを振る」仕組みが要る。
- 変換スクリプト（STL→面 DXF）をリポジトリに入れるかは未決。
- 実際に音線追跡まで通してはいない（読み込みの検証まで）。

---

## 逐次ログ

- `ModelTest.dwg` が読めるか検証依頼 → accoreconsole で DXF 化し中身を確認。
  幾何は 3DSOLID 1 個で、現行リーダーでは面 0 枚。src/rec は読めた。
- STLOUT で 68 三角形に分割 → レイヤ・単位・src/rec を引き継いで面 DXF を生成 →
  68 面・閉じたシェル・体積 770.33 m³ で読み込み成功。MASSPROP と一致することも確認。
- 法線確認画面で ModelTest を表示（`ModelTest_PJ` を新規作成）。68 面すべて緑（内向き）。
  `q` で保存せず終了。cp932 コンソールだと `体積…m³` の print で落ちるので
  `PYTHONIOENCODING=utf-8` を付けて起動した（要修正）。
- 次の依頼：**面に吸音材を割り当てる**仕組みの検討。あわせて
  「選んでいる面が分かりづらい」「一括選択して法線反転・吸音材設定をしたい」。
  吸音材リストは当面 `C:\Users\sakayoshi\Desktop\JR\absorption.csv`（以前のテストモデルのもの）。
  → 調査：68 三角形は**同一平面で連結した 16 グループ**にまとまる（＝元ソリッドの面そのもの）。
- 実装（コミット 774c702）：面に吸音材を割り当てる仕組み一式。
  `coplanar_groups`（68三角形→16面グループ）／`read_model(face_materials=...)`／
  `materials.json`／`normal_editor.py`→`face_editor.py` 改名と「選ぶ→適用」への変更。
  テスト 208→222 件 全部 OK。ModelTest に床=タイルカーペット・天井=天井吸音・
  壁=壁吸音材 を割り当てて統計残響まで確認（T30 Sabine 2.62〜0.22 s、容積 770.33 m³）。
- ついでに cp932 コンソールで `体積…m³` の print が落ちる件を修正（m3 に変更）。
- 追加検証：`視聴覚室_Test.dxf`（面を **REGION** で作ったモデル）。
  **REGION も 3DSOLID と同じ `AcDbModelerGeometry`（ACIS）なので直接は読めない。**
  そのままだと面 0 枚（REGION 42 + 3DSOLID 2 + LINE 8 を読み飛ばし）。src 1 / rec 5 は読めた。
  - 変換：accoreconsole で ①ソリッドを面（REGION）に分解 → ②REGION を 1 つずつ分解し、
    出てきた線を `F<番号>#<元レイヤ>` レイヤへ退避（`(ssget "_P")` で取れる）→ ③DXFOUT。
    Python 側で輪をつないで `read_dxffile.triangulate_polygon` で三角形化し 3DFACE に。
    140 面 → 340 三角形、失敗ゼロ。曲線は無し（全部直線）。
    ※ `_.EXPLODE` は `(command "_.EXPLODE" ss "")` でも 1 個しか処理しない。
      ename を先にリストに集めて 1 つずつ回す必要がある。`_.JOIN` は効かなかった。
  - **モデルの作りが ModelTest と違う**：室を高さで 3 つの帯に切り、
    `0-2100`（壁＝25 リージョン。扉・窓で分割）／`2100-3000`・`3000-4500`（ソリッド）
    ＋ `床`(Z=0) ／ `天井`(**Z=2100**) ／ `扉` 13 面 ／ `窓` 2 面。
    平面は ModelTest と同一（256.78 m²）、高さ 4.5 m。
  - **帯の境目に内部の水平面が二重にできる**：Z=2.1 m と Z=3.0 m にそれぞれ 513.55 m²。
    重なった面 24 組（48 枚）を落とすと 292 枚・1094.83 m² になる。
  - Z=2.965〜2.990 に **25 mm 厚・910 mm 幅・14.4 m 長の板が 11 枚**（レイヤ `2100-3000`）。
    吸音バッフルらしい。Y 方向に約 1.6 m ピッチ。
  - **要確認**：`天井` レイヤの面が Z=2.1 m にあり、実際の室天井（Z=4.5 m）は
    `3000-4500` レイヤの面になっている。帯の切り方をどう解釈するかはユーザー判断待ち。
  - **ユーザー回答**：帯の解釈は **CAD 側を直すので待ち**。
    Z=2.97 m の 11 枚は「**反射板や家具**。宙に浮いた障害物で、外向き法線・中は空洞」。
  - 浮いた障害物の法線を確認：`orient_normals='auto'`（既定）で
    **132 枚中 132 枚が外向き＝空気側**になる。`inward` も同じ。
    `shells` は 44/132 しか合わない（変換で作った巻き順が一貫していないため
    `cad` へフォールバックする仕様どおりの挙動）。**既定のままでよい。**
- **モデル作り直し版を検証（13:42 更新）。指摘 3 点すべて解消。**
  ソリッドが無くなり全部 REGION（136 個）。`反射板` レイヤ新設（66 リージョン＝11 枚×6 面）。
  天井が Z=4.5 m へ。帯の境目の内部水平面（重複 24 組）は消えた。
  - 変換：同じ手順で 136 リージョン → **292 三角形、失敗ゼロ**。
    `視聴覚室_Test_faces.dxf` ／ プロジェクトは `視聴覚室_PJ`。
  - レイヤ別面積：0-2100 94.66 ／ 2100-3000 63.16 ／ 3000-4500 105.27 ／ 反射板 265.47 ／
    天井 256.78 ／ 床 256.78 ／ 扉 37.45 ／ 窓 15.27 ＝ **合計 1094.83 m²**。
    面グループ 87 個。寸法 16.145 × 17.95 × 4.5 m。
  - **健全性**：壁+扉+窓 315.810 m² が周長×高さ（70.180 × 4.5）と**誤差ゼロで一致**
    → 面の抜けなし。音源・受音点 5 点すべて囲まれ度 1.000。
    反射板 132 枚すべて外向き（空気側）。
  - 残る警告 2 件は無害：開いた辺 70 本は**T 字接合**（帯と開口で分割しているため）、
    巻き順の不一致は変換がリージョンの輪の向きを使うため。既定の `auto` は巻き順に依存しない。
  - **容積は自動算出できない**（閉と判定されないため）。
    床面積×高さ − 反射板 = **1154.42 m³** を project.json に明示した。
  - **次に要るもの**：レイヤ→材料の対応。いまは `扉`・`窓` だけ名前が一致し、
    他 6 レイヤは既定 0.1。`project.json` の `assignment` に書く仕組みはあるが
    **入力 UI が無い**（TODO G-9b）。材料の指定待ち。

---

## 注目したい音線だけを残す（G-16）

**「音線・音粒子ともに、この経路もっとみたい、この辺りの粒子もっとみたい、
この方向に飛ぶ音線・粒子に注目したい。そんな機能を追加できないですかね？」**

できた。3 つとも**「表示する音線の添字を絞る」**話なので、既存の
`RayDisplay` / `ParticleAnimation`（どちらも「表示する添字」を受ける作り）に
そのまま乗った。

### 操作

**`p` で 3D 上の点を拾う**（VTK 標準のピックキー。壁でも音線でもよい）。
拾った点に**黄色い球**が出るので、どこを基準にしているかが見える。
そのあと `k` で種類を切り替え、範囲は左パネルのスライダ。`0` で解除。

| 種類 | 何が残るか | スライダ |
|---|---|---|
| 絞り込みなし | 候補すべて | — |
| 基準点の近くを通る | 折れ線が基準点から半径以内を通った音線 | 近さ [m] |
| この方向に飛ぶ | 音源から見て基準点の向きへ出た音線（円錐） | 方向の半角 [°] |
| いちばん近い 1 本だけ | 1 本（反射面の並びをコンソールに出す） | — |

パネルに「該当 67 / 1801 本」のように出るので、絞れているかがすぐ分かる。

### 作り

- **`geosim/ray_filter.py`（新設）** … 画面に触らない純粋な計算。
  `near_point` / `in_direction` / `nearest_ray` / `through_face` / `describe_ray`。
  テストは GL 不要（18 件）
- **`view_rays.RayFocus`** … その結果を `RayDisplay.set_pool()` と
  `ParticleAnimation.set_focus()` に渡し直すだけ

### 判断したこと

**方向は「出射方向」で見る**（`RayLog.directions`）。途中の向きで見ると反射のたびに
向きが変わり、**どの音線も条件に引っかかって絞り込みにならない**。

**「近くを通る」は描いている反射回数までで測る。** ここが実際に問題になった。
50 回も反射を追うと、どの音線も室内を回って基準点の近くを通ってしまう。

| 反射何回まで見るか | 半径 0.5 m 以内（2000 本中） |
|---|---|
| 1 回 | 9 本 |
| 4 回（既定の描画範囲） | 67 本 |
| 20 回 | 326 本 |
| 全部（51 回） | **741 本** ← 絞れていない |

描画範囲（`RayDisplay.max_reflection`）に合わせるようにした。

**距離は線分まで測る**（節点までではない）。節点だけ見ると、長い区間の途中を
通っている音線を拾い落とす。

**粒子はトポロジを変えずに隠す。** `_shown` を書き換えてエネルギーを NaN にし
`nan_opacity=0` で消す（フレームごとにジオメトリを組み直さないための既存の約束に従った）。

### 踏んだバグ（実ウィンドウで発見）

**絞り込みを緩めても音線の描画本数が戻らなかった。**
候補が 8 本に減ると `count` も 8 に下がり、247 本に戻しても 8 本のままだった。
**スライダで指定された本数（`wanted`）と実際に描いた本数（`count`）を分けて**解決。

    近さ 0.2 m → 該当   8 本 / 描画  8 本
    近さ 1.0 m → 該当 247 本 / 描画 60 本（スライダの指定に戻る）

### 検証

- **立体角の期待値と一致**：半角 5° で 4 本（期待 4）、15° で 34 本（期待 34）、
  30° で 131 本（期待 134）
- 線分までの距離を解析解と突き合わせ（手で作った 3 本の軌跡）
- 実ウィンドウで 4 種類の切り替えと本数の追随を確認し、画像も撮った

**テストは 222 → 240 件、全部 OK。**

---

## 終了時の segfault（視聴覚室で絞り込みを試したあと）

絞り込みを触ってもらったところ、**アプリの終了時に segfault**（exit 139）した。

### 分かっていること

- **落ちたのは全部終わったあと。** ログの最後は
  `[app] 閉じました`（条件入力を閉じた）で、そのあとプロセス終了時に落ちている
- **データは無傷。** 図 9 枚が 19:36 に生成済み、結果 CSV も無事
- GL のエラー（`Could not set shader program` が大量、
  `Error binding ndCoords to VAO`、`vtkOpenGLRenderWindow.cxx:1545`）が
  **すべて 319.5 秒＝終了時刻に固まっている**。
  つまり **OpenGL の文脈が消えたあとに VTK が描こうとした**形
- 5 分ほど操作したあとの出来事。絞り込みを使った痕跡はログに残っていない
  （`p` を押しても mode が変わるだけで print しないので、使ったかどうかは判別できない）

### 再現できていない

同じ手順を 3 通り試したが**どれも exit 0**だった。

| 試した内容 | 結果 |
|---|---|
| ウィンドウを 2 回開いて閉じる（絞り込みなし） | 正常終了 |
| 同じ（絞り込みあり＝`enable_point_picking` を有効に） | 正常終了 |
| `run_animation` の対話ループ ＋ スライダ操作の連打 ＋ 絞り込みの切り替え | 正常終了 |

tkinter を先に開いて閉じる順序（`app.py` と同じ）も含めて再現しなかった。
**GL ドライバの状態にも依るらしい**（8/17 にも同じ端末で GL が一時的に死んでいる）。

### やったこと（落ちる余地を減らす処置）

根治の確認は取れていないので「対策」ではなく「余地を減らす」処置として入れた。

1. **`run_animation` はウィンドウが無くなったら 1 コマも描かずに抜ける。**
   `_closed` が立つのが遅れることがあるので `plotter.iren` まで見る。
   閉じられたあとに描き続けると、文脈が消えた状態でシェーダを組み直そうとする
2. **`view_model_gui.release_window()` を新設。** 閉じる前に、こちらが握っている
   actor の参照（`RayDisplay.actors` / `ParticleAnimation.actor` / `RayFocus.marker` /
   パネルのウィジェット）を外してから閉じる。
   VTK の GL 資源は文脈が生きているうちに解放しないといけないが、
   Python の後片付けは順序が決まっていないため
3. **`app.py` は終了直前に `pv.close_all()`** を呼ぶ（`finally` で必ず通す）

### 申し送り

- **また落ちたら、何をしていたか教えてほしい。**
  特に「ウィンドウの × で閉じたか / `q` で閉じたか」「最大化したか」
  「数値入力（`t`）を使ったか」が手がかりになる
- 落ちても**計算結果と図は残っている**（保存はすべて終わったあとなので）

---

## 絞り込みをスライダで指定できるようにした（G-16b）

**「スライダーの方では無く、モデル上で `p` を押して、という操作が難しいです。
どの方向へ、というのも平面方向と縦方向のスライダーを用意するなどで、
指定出来た方がやり易いかもしれないです。」**

そのとおりに変えた。**クリックは任意**になり、スライダとキーだけで使える。

| 決めるもの | どう決めるか |
|---|---|
| 種類 | `k`（なし → 近くを通る → この方向に飛ぶ → 1 本だけ） |
| 基準点 | `j`（**受音点** → 音源 → 拾った点）。**既定は受音点** |
| 方向 | **「方位角 [°]」「仰角 [°]」スライダ** |
| 範囲 | 「近さ [m]」「方向の半角 [°]」スライダ |

- **方向の約束は `project.head_azimuth` と同じ**にした（0° = +X で反時計回り、
  仰角 0° が水平・+90° が真上）。バラバラだと混乱するため
- **方向は音源から伸びる黄色い矢印**で描く。スライダを動かすと向きが変わるので、
  何を指定しているかが見える
- 方向の初期値は**音源から受音点を見る向き**（視聴覚室では方位 212° / 仰角 -4°）
- `p` を使ったときは、方向モードなら**スライダの値もその向きに合わせる**
  （拾う操作とスライダで状態が食い違わないように）

### 動作確認（視聴覚室）

    k → 近くを通る（既定=受音点）  該当   23 / 1064 本
    近さ 1.0 m                    該当   98 本
    j → 音源                      該当 1064 本（音源はどの音線も通るので当然）
    k → この方向に飛ぶ             該当   25 本（方位 212° 仰角 -4°）
    方位角 90°（+Y）               該当   29 本
    仰角 +60°（上向き）            該当   14 本
    半角 40°                      該当  166 本
    k → 1 本だけ                  該当    1 本

### パネルに入りきらなくなった（副作用と、その扱い）

スライダが 9 本になり、レイヤ 8 つの視聴覚室で**下が 458 px はみ出した**。
操作説明ごと画面外に出るので放置できない。

**スクロール（PageUp/PageDown）を作ったが取り下げた。**
VTK のウィジェットはテキストと違い、置き直しを飛ばすと古い位置に残る。
範囲外を隠す処理（`representation.SetVisibility(0)`。`SetEnabled(0)` では消えないことも
実測で確認）を入れても、**チェックボックスが 4 行ぶんずれて文字に重なる**症状が残り、
最小の例では再現しなかった。原因を詰めるより引き返す判断をした。

代わりに**載せる内容を削った**。

- 「音線の情報」はコンソールに出しているのでパネルから外した（約 90 px）
- スライダのバー高さを 34 → 28 px に詰めた（9 本で約 54 px）
- → **458 → 268 px** に減った
- それでも入りきらないときは**コンソールに操作説明を出す**。
  パネルの高さ＝ウィンドウの高さなので、縦に広げれば入る

**テストは 240 → 253 件、全部 OK**（方位角・仰角の変換と往復を追加）。

---

## 一律 α=0.2 での試算と、容積・総表面積の算出（夕方）

### 一律 α=0.2 の計算（視聴覚室_PJ）

材料の割り当てを決める前に、ひとまず計算を通したいとのことで
`absorption_uniform02.csv`（全 8 レイヤ 0.2、`# kind: normal`）を作って回した。
**受音点は 5 点 → 中央 1 点（受音点3 [7.5, 10.45, 1.2]）に絞った**
（受音点ごとに音線追跡をやり直すので時間が 1/5 になる。約 12 分）。
音線 200,000 本 / 最大反射 60 回 / 受音球 1.0 m / 打ち切り 2.0 s。

| | 125 | 250 | 500 | 1000 | 2000 | 4000 Hz |
|---|---|---|---|---|---|---|
| EDT | 0.474 | 0.538 | 0.563 | 0.464 | 0.480 | 0.445 |
| T30 | 0.671 | 0.636 | 0.638 | 0.668 | 0.569 | 0.525 |
| Eyring-Knudsen | 0.434 | 0.433 | 0.431 | 0.429 | 0.422 | 0.398 |
| 比 T30/E-K | 1.548 | 1.469 | 1.480 | 1.556 | 1.348 | 1.318 |

C50 4.9〜9.0 dB / C80 9.7〜12.5 dB / D50 0.75〜0.89 / Ts 24〜32 ms。
受音した経路 1,247 本。

**T30 はまだ信用しない。** 曲率 11〜31% で 2 段減衰。原因は 2 つあり性質が違う。
- **計算側の取りこぼし**：`pulses.png` の経路数の時間分布が 0.15 s でピークを打って
  減る（拡散音場なら t² で増えるはず）。反射回数は足りている（nref=60 到達時点で
  −97.1 dB）。効くのは**音線数と受音球の半径**
- **室の性質**：反射板 11 枚が全表面積の 24%（265 m²）を占め、天井との間に
  半分ふさがれた空間を作る。連成空間なので**2 段減衰が出るのが正しい**

→ ①を潰さないと②を評価できない。音線 1,000,000 本・受音球 1.5 m を提案（未実施）。

### 音線・音粒子の可視化

`view_rays.py` で確認。音線 2000 本（元 200000 本を 100 本おき）/ 受音 1064 本。
※ **バックグラウンド実行では作業ディレクトリが戻る**ので、`../.venv/Scripts/python`
のような相対パスだと起動に失敗する（何度か空振りした）。絶対パスを使うこと。

### 容積・総表面積（G-16 / G-17。コミット 9dc16e0）

「音源等を入れずに容積と総表面積を出したい。どの面を拾い、どの領域を容積として
拾ったか確認したい」との依頼。**不具合が 1 件見つかった。**

- `signed_volume()` は頂点の巻き順から体積を出すので、**巻き順が一貫した閉曲面**
  でしか成り立たない。リージョン変換したモデルや、壁を高さの帯・開口で分割した
  モデル（T 字接合で開いた辺が出る）では合わない。
  **反射板 1 枚の体積が 0.1090 m³（正解 0.3270 m³）**、室全体は `None` だった
- `volume_from_normals()` を新設。発散定理 `V=(1/3)∮r·n dA` を**法線**で評価。
  巻き順に依存せず、**家具・反射板の体積も自動で引かれる**。穴があると誤るので
  `encloses_point()` で確かめてから採用。巻き順の値との食い違いも知らせる
- `surface_area()` / `open_edge_segments()` を追加。`DxfModel` に `surface_area` と
  `layer_areas` を持たせ、`summary()` に総表面積・レイヤ別面積・容積・平均自由行程
- **`python read_dxffile.py 室.dxf`** で幾何だけ確認できる CLI を付けた
- `face_editor` に**「容積の拾い方」表示**（`m` の 3 段目）。外殻＝青／内側の物体＝橙／
  内側の開いた板＝灰／**開いた辺＝赤い線**。赤い線が壁の継ぎ目と床際に沿って走るのが
  見えるので、視聴覚室の「開いた辺 70 本」は穴ではなく T 字接合だと判断できた

**視聴覚室：総表面積 1094.829 m² / 容積 1152.269 m³**（平均自由行程 4.210 m）。
「床面積 256.776 × 高さ 4.5 − 反射板 3.224」= 1152.268 m³ と一致。
ModelTest は 770.328 m³ で AutoCAD の `MASSPROP`（770.33 m³）と一致。
`project.json` の手入力の容積（誤った 1154.42）は消して自動算出に任せた。

テスト 222 → 264 件、全部 OK。

### 積み残し

- **レイヤ→材料の対応**（`assignment`）の入力 UI が無い（G-9b）。材料の指定待ち
- 音線数を上げた再計算（T30 を信用できる形にする）
- 反射板ありなしの比較（連成空間の効きの切り分け）

### 階段教室モデル（`C:\Users\sakayoshi\Desktop\JR\階段教室.dxf`）

前段の「容積と総表面積を出したい」依頼は、このモデルを指していた（パスが渡っていなかった）。

- 中身は **3DSOLID 1 個だけ**（レイヤ `06_ルーバー上_6000-7500`）。
  レイヤ定義には 6 つの高さ帯（`01_1F建具高さ_0-2100` 〜 `06_ルーバー上_6000-7500`）が
  あるが**実体はこの 1 個のみ**。帯ごとの分割はまだされていない。音源・受音点は無し。
- 変換：68 面 → **272 三角形**、曲面ゼロ。段床の階段が正しく出た。
  変換で 2 点直した（どちらもこのモデルで初めて出た）。
  - **端点の突き合わせ許容差**：ACIS から分解した線の端点は `-21142.00000014645` の
    ように末尾が揺れる。1 nm（6 桁）では閉じる輪が閉じず 8 面が失敗 → **1 µm（3 桁）**に
    したら 7 面が救われた
  - **穴の開いた面**：Z=0 の床が階段席の部分（10.94 × 9.35 m）だけ矩形に抜けていた。
    既存の `triangulate_polygon` は穴を扱えず、穴を橋でつなぐ手も**幅ゼロの切れ込みで
    耳刈り法が破綻**した。→ **スラブ分割**に変更（全頂点の高さで横に切り、帯ごとに
    台形を拾う）。任意の単純多角形で厳密。面積が「外周 − 穴」になるか毎回照合する
- **結果：総表面積 2231.162 m² / 容積 3557.322 m³ / 平均自由行程 6.378 m**
  （寸法 21.4365 × 26.235 × 9.0 m）。
  **AutoCAD の `MASSPROP` は 3.5573E+12 mm³ = 3557.3 m³ で一致。**
  一致することは「面に穴が無い」ことの裏付けにもなる（穴があれば発散定理が破れる）。
- `[check]` の「開いた辺 16 本」は**すべて Z=0（床）で、スラブ分割が作った T 字接合**。
  壁側の 1 本の長い辺に対し床側が 3 本に割れているだけで、穴ではない。
  巻き順の不一致も変換由来。容積は法線から出しているので影響しない。
- プロジェクトは `JR\階段教室_PJ`。

**提案（未決）**：これで変換したモデルが 3 つ目（DWG/3DSOLID・REGION・穴つき面）。
変換スクリプトは scratchpad にあるだけで中身も育っている（許容差・穴のスラブ分割・
面積照合）。`geosim/import_acis.py` として入れ、AutoCAD スクリプト生成から DXF 出力まで
1 コマンドにまとめ、テストも付けたい（TODO B-20）。→ **ユーザー判断待ち**

### 三角形分割の質と T 字接合（G-18。コミット b7d0301）

ユーザー指摘 2 件。どちらも実測で答えた。

**「T 字接合は不都合が生じるか」→ 実測では問題にならない。**
開いた辺 1 本ずつについて「同じ直線上の他の開いた辺で覆われているか」を調べた
（覆われていれば面は連続していて辺の分け方だけが違う）。
階段教室 16 本・視聴覚室 70 本・ModelTest 0 本、**すべて本当の隙間は 0 本**。
容積が MASSPROP と一致することも watertight の裏付け。
実際に効くのは①容積が出せない（→法線から出す、対処済み）
②`shells` モードが使えない（→既定 `auto` は巻き順に依存しないので影響なし）だけ。

**「1 面なのに細い三角形で 10 分割以上されて無駄」→ 指摘は正しかった。**
- **枚数は減らせない**。n 角形は必ず n-2 枚で、これが理論的な最小。
  階段教室で 27〜34 枚の面は実際に 29〜36 個の角を持つ（段床の階段状の輪郭）。
  68 面のうち 52 面は 1〜2 枚で済んでいる
- **形が悪かった**。`_ear_clip` が「最初に見つかった耳」を切っており、走査が毎回
  同じ側から始まるので実質**扇状分割**になっていた（ユーザーの推測どおり）
  → `triangle_min_angle()` を新設し、**最小角がいちばん大きい耳**を切るようにした。
  四角形の対角線も両方使えるときは形の良いほうを選ぶ
  - 10:1 の細長い 12 角形：最小角 1.53° → **14.76°**
  - 階段教室 272 枚：中央値 9.2° → **11.6°** / 1°未満 24 → **16 枚**
- **残るスリバーはモデル側の形状**。視聴覚室の反射板は 1 枚 14.375 × 0.024 m
  （縦横比 610:1）で、2 枚に割れば必ず 0.09°。分割では直せない。
  視聴覚室が改善しなかった（中央値 8.4° のまま）のはこのため
- `DxfModel.triangle_quality`（最小角の中央値・最小・1°未満/5°未満の枚数）を追加

**「扁平な面で、プログラム側が頂点を足して分割を工夫できるか」→ 可能だが勧めない。**
制約付きドロネー＋細分（Ruppert）で最小角 20° を保証できるが、
1. **枚数が爆発**：反射板の面は 2 枚 → 1198 枚。視聴覚室全体で 292 → 約 26,600 枚（91 倍）。
   音線追跡は面数に比例するので現実的でない（F-1 / F-2 が未着手）
2. **実害がほぼ無い**：頂点を倍精度の丸め幅だけ揺らしたときの法線の振れは
   縦横比 610:1 で **2.2e-11 度**、100 万:1 でも 2.7e-08 度。音響的に無意味な量
3. **総辺長が増える**：近接判定のあいまいさは辺の長さに比例するので逆効果になりうる
4. 音響的には 1 枚の平面をどう切っても物理は変わらない（同じ平面・法線・材料）

頂点追加が要るのは**拡散反射（散乱）・音響ラジオシティ・面ごとの入射エネルギー分布**を
やるとき。着手時に合わせて考える。

**代案（提案・未決）**：枚数を減らしたいなら逆向きに、
**面グループ（同一平面パッチ）単位で当たり判定する**手がある。
`coplanar_groups()` で 1 枚の壁は既に把握できているので、
①音線とパッチの平面の交点を 1 回求める ②多角形の内外判定 とすれば
面 1 枚あたり 1 回で済む（階段教室 272 回 → 68 回）。面の内部に辺が無くなるので
近接判定のあいまいさも消える。F-2（空間分割）と組み合わせると効きそう。

### 同一平面パッチ単位の交差判定（F-4。コミット 705ccf6）

ユーザー指摘：「三角形に割っているのはねじれのない面を定義するためで、
ねじれの無い平面なら多頂点要素でよいのでは？ 三角形の法線が一致していれば
ねじれ無しと判定できるか？」→ **その判定で成り立つ**（三角形分割の隣接は木状に
つながるので、辺を共有する 2 枚の法線が一致すれば連鎖して全頂点が同一平面）。
実装は平面からの距離で見るようにした（mm 単位でしきい値を決められる）。

- `mesh_method.PatchArrays` を新設。平面との交点を 1 回求め、外周多角形の内側かを
  **交差数の偶奇**で判定。外周は輪に並べ替えず辺の集まりのまま扱うので穴もそのまま
- `hit_id` は代表の三角形インデックスを返すので**下流は変更不要**
- パッチの条件は 同一平面＋辺で連結＋**同じ材料**＋同じ法線の向き。
  材料で割らないと吸音率が引けない（視聴覚室で 18 パッチが混ざっていた）
- **同一平面のしきい値は編集画面より厳しく**（0.1°/0.1mm）。編集画面の 1°/1mm だと
  代表面の平面から 3.5 mm ずれた三角形まで入り、交点距離が 3.15 mm 動いた
- `collision_arrays()` を入口にし、音線追跡とバックトレースが必ず同じ入れ物を使う

| モデル | 三角形 → パッチ | 判定の速度 |
|---|---|---|
| ModelTest | 68 → 16 | 3.0 倍 |
| 階段教室 | 272 → 70 | 2.6 倍 |
| 視聴覚室 | 292 → 114 | 2.3 倍 |

いずれも三角形版と**距離・交点がビット単位で一致**。

**★速度以上に効いたのは取りこぼしていた経路が戻ったこと。**

    視聴覚室 / 音線 30,000 本 / 最大反射 40 回
      三角形  62.4 秒  経路 14633 本 → 受音  314 本  エネルギー 193.94
      パッチ  31.2 秒  経路 14234 本 → 受音  703 本  エネルギー 253.59

バックトレースは「記録した反射面の並びを再現できるか」で経路を検証するが、
三角形単位だと**同じ壁の隣の三角形に当たっただけで却下**していた。
鏡像は平面で決まるのでこれは取りこぼし。**受音経路が 2.2 倍**になった。
前に報告した「後期の経路が薄く T30 が信用できない」問題にも効くはず。

### 「開いた辺」の 2 種類と、設定画面の総表面積（コミット b721d0b）

`uncovered_open_edges()` を新設。開いた辺を「同じ直線上の他の開いた辺に覆われて
いるか」で分ける。覆われていれば **T 字接合**（面は閉じている）、
覆われていなければ**本当の自由端**（宙に浮いた片面の板・面の抜け）。
発散定理の容積は閉曲面でしか成り立たないので、自由端があれば `volume_note` と
`check_model` が「目安」と知らせる。

- 階段教室 16 本・視聴覚室 70 本・ModelTest 0 本 → **自由端ゼロ**（全部 T 字接合）
- 研修室（板の寄せ集めモデル）→ 136 本中 **14 本・計 32.6 m が自由端**。
  容積 371.892 m³ は目安（従来の床面積×高さ 372.961 と 0.3% 差）

`setup_window.estimate_volume()` が (容積, 総表面積, 説明) を返すようにし、
容積の自動算出ダイアログに**総表面積と平均自由行程 4V/S を参考表示**する
（ユーザー要望）。

### 視聴覚室（`JR\視聴覚室.dxf`）

`ModelTest\視聴覚室_Test.dxf` と**バイト単位で同一**（移動・改名のみ）。
JR 側に `視聴覚室_faces.dxf` を作り直した。

    総表面積 1094.829 m2 / 容積 1152.269 m3 / 平均自由行程 4.210 m
    寸法 16.145 x 17.95 x 4.5 m / 三角形 292 枚 / 自由端 0 本

参考（3 モデル）

| | 総表面積 | 容積 | 平均自由行程 |
|---|---|---|---|
| 視聴覚室 | 1094.829 m² | 1152.269 m³ | 4.210 m |
| 階段教室 | 2231.162 m² | 3557.322 m³ | 6.378 m |
| 研修室 | 386.768 m² | 371.892 m³（目安） | 3.846 m |

テスト 270 → 284 件、全部 OK。

### リージョン＋ソリッド混合モデルと、研修室の計算（2026-08-20）

**混合モデル（`JR\視聴覚室_リージョンソリッド混合.dxf`）**
部屋を REGION（70）、反射板を 3DSOLID（11）で作ったもの。
別ソフトで法線の向きの都合が良いから、というユーザーの判断。
→ **問題なく読める。**変換手順が最初にソリッドを面に分解するため。
全リージョン版と**小数点以下まで完全に一致**（面数 292 / 総表面積 1094.828798 m² /
容積 1152.269158 m³ / 判定パッチ 114 / レイヤ別面積すべて一致）。
反射板 132 枚の法線も全部空気側。**この作り方を続けて問題ない。**

**研修室（`JR\研修室.dxf`、REGION 23 + 3DSOLID 5）**
- 吸音材リストを 21 材料・**8 バンド（63〜8k）**に更新、レイヤ名も ID に合わせて改名。
  9 レイヤすべて材料が引けた
- 53 面 → 110 三角形 / 判定パッチ 48 / 寸法 8.5 × 14.64 × 4.5 m
  **総表面積 580.771 m² / 容積 556.325 m³ / 平均自由行程 3.832 m / 自由端 0 本**
- 反射板 5 枚（0.91 × 13.3 × 0.025 m）→ 内側の物体として認識、法線は空気側
- 計算：音線 200,000 本 / 最大反射 **150 回**（63 Hz は平均α 0.060 で 35 dB に 131 反射要る）
  / 受音球 1.0 m / 打ち切り 3.0 s。**受音経路 8,521 本**（パッチ化の効果が大きい）

| | 63 | 125 | 250 | 500 | 1k | 2k | 4k | 8k |
|---|---|---|---|---|---|---|---|---|
| EDT | 1.390 | 1.079 | 0.949 | 0.541 | 0.311 | 0.307 | 0.306 | 0.284 |
| T30 | 1.388 | 1.049 | 1.053 | 0.715 | 0.487 | 0.403 | 0.397 | 0.382 |
| Eyring-Knudsen | 2.474 | 1.151 | 0.885 | 0.390 | 0.236 | 0.244 | 0.235 | 0.233 |

**減衰は全バンドで 2 段。**傾きを分けて測ると

| | 早い部分 (-5→-25dB) | 遅い部分 (-35→-60dB) | Eyring-Knudsen |
|---|---|---|---|
| 63 Hz | 1.04 s | **2.53 s** | 2.47 s |
| 500 Hz | 0.68 s | 1.62 s | 0.39 s |
| 1k Hz | 0.43 s | 0.75 s | 0.24 s |

63 Hz は**遅い部分が統計値とほぼ一致**（2.53 対 2.47）。高域は遅い部分が統計値の
2〜3.5 倍。吸音の分布が極端に不均一（天井吸音 GW と壁ピンホールで 191 m² 対
反射板・間仕切り・窓で 193 m²）＋天井近くの反射板 5 枚が吸音天井を遮るため、
**2 段減衰が出るのが物理的に正しい**状況。Sabine / Eyring は拡散音場前提なので
この形は表せない。**知覚に効くのは EDT。**

積み残し：受音点は 1 点のまま（増やした分が保存されていなかった）。
max_time=3.0 s を超えたパルス 38 本（最大 5.19 s、−70 dB 以下）を捨てている。
吸音率リストの ID 17/18/19 が同名「GW 32K-50t(GC厚手)」で、名前で引くと衝突する。

**不具合修正**：統計残響式が `is_closed` で使用可否を判定していたため、この研修室で
「形状が閉じていないので容積が決まりません」と出て計算が飛んでいた。
`model.volume` があるかで判定するように変更。
