# 2026-09-21 WIN250046 / Claude

## 本日のやり取り

### 散乱（拡散反射）の導入方針をパワポにまとめた（`27890dc`）

ユーザー依頼：

> 拡散係数の導入について，やり方をまとめて。**まだプログラムとしては導入しないで**。
> 導入の可能性のあるやり方の説明や，やり方による違い（計算コスト，計算精度，
> 拡散係数の設定難易度，モデル化の難易度等々）を，パワポにまとめて。
> また，拡散係数の妥当な数値設定についてもまとめて。

★**コードには何も入れていない。**`評価レポート/2026-09-21_散乱の導入方針_WIN250046_Claude.pptx`（18 枚）。

#### 中身の骨子

- **なぜ要るのか**：階段教室の実測（T30 が理論の 3.35 倍／ξ 10〜25／曲率 10% 超）。
  ★等価吸音面積をそろえて配置だけ変える実験で再現できているので、
  **室の性質ではなくモデルの限界**だと言い切れる
- ★★**いまの作りと、散乱がそのままでは入らない理由**（この資料の核心）。
  3 段目の**虚音源バックトレースが鏡像で検算する**ので、
  **散乱した音線は 1 本残らず却下される**。反射の式を 1 行変える話ではなく、
  計算の骨組みを変える話になる
- **用語の整理**：ご指示の「拡散係数」は、シミュレーションで使う量としては
  **散乱係数 s（ISO 17497-1）**。**拡散係数 d（ISO 17497-2 / AES-4id）は別物**で
  拡散体の性能比較に使うもの。資料は以後 s で統一した
- **方式 A〜E**：ベクトル法（VBS）／確率的散乱（モンテカルロ）／2 次音源（diffuse rain）／
  放射伝達（radiosity・ART）／幾何でつくる
- **比較表**：計算コスト・精度・s の設定難易度・モデル化の手間・**既存コードへの影響**・
  **位相（複素和）の維持**・市販ソフトでの採用
- ★★**このプログラム固有の論点：位相**。インパルス応答は位相を持つ複素和（査読論文 式(2)）
  なのに、散乱音には決まった位相が無い。**「散乱したかどうか」で切り分ける**提案
  （鏡面は複素和のまま・散乱はエネルギー）にした。何次反射までで切るより素直
- **ロードマップ**：段 0 物差しを控える → 段 1 B+C → 段 2 条件表に散乱率の列 → 段 3 校正
- **s の決め方**：凹凸の深さと波長／面の大きさ（縁の効果）／★**二重に数えない**。
  材料別・周波数別の目安表を付けた。★**s = 0 にしない**（0.05〜0.1 を下限）
- **階段教室への当てはめ**：形が作り込まれているので s は小さめ（反射板・段床 0.05〜0.10）。
  ★**未作図の客席は 0.6〜0.7**。いちばん効くのは反射板の隙間（結合空間の「のど」）
- **検証の基準**：曲率・ξ・T20/T30 の差が縮むこと。
  ★**EDT・C50・STI と逆二乗則は動かないこと**（動いたら入れ方を間違えている）

#### 作り方で踏んだこと

- ★この端末には **node も LibreOffice も python-pptx も無い**。
  **プロジェクトの `.venv` は汚さず**、scratchpad に使い捨ての venv を作って
  `python-pptx` を入れた（`requirements.txt` は変えていない）
- ★★**画像で確かめられない**（LibreOffice が無いので PDF に出せない）ので、
  **文字幅と行高から「枠に入りきるか」を見積もる点検スクリプト**を書いて全ページ通した。
  全角 1em・半角 0.52em・行高 1.4em で数えて、あふれ 10 箇所・余白不足を洗い出して直した
- ★Bash のヒアドキュメントが `\n` を実際の改行に潰すので、
  置換で `chr(92) + "n"` と組み立てる必要があった（前にも踏んだ）

---

### 逐次ログ

- 「拡散係数の導入について、やり方をまとめて。まだ導入はしないで。パワポに」
  → 18 枚の検討資料を作成（`評価レポート/2026-09-21_散乱の導入方針_WIN250046_Claude.pptx`）。
  ★コードは 1 行も触っていない。作成環境は scratchpad の使い捨て venv

### 他ソフトの手法比較を追加（`4cbe96c`。18 → 22 枚）

ユーザー依頼「他のソフトの手法の比較も一緒にパワポに説明入れて。同じようにやり方の違いとかも」。

足した 4 枚（p12〜p15）:

- **他のソフトはどうしているか（一覧表）** … ODEON / CATT-Acoustic / RAVEN /
  EASE・AURA / Olive Tree Lab / Treble を「早期反射・後期残響・散乱の入れ方・回折・
  散乱係数の入力」の 5 列で並べた。
  ★**どのソフトも「散乱を入れずに後期残響を出す」ことはしていない**
- **主要な 3 本を詳しく** … ODEON（反射次数 TO で切替・VBS・diffuse rain・
  面の大きさから縁の散乱を自動補正）／CATT-Acoustic（コーン追跡・ランダム化した尾・
  用途でアルゴリズムを選ぶ）／RAVEN（虚音源に可聴判定・確率的な後期・
  ★ISO 17497-1 の測定法を作った研究室）
- ★★**やり方の違いは 4 つの分かれ目に集約される**（この枚がいちばん効くと思う）
  1. 早期と後期の境目（反射次数で切る／★散乱したかで切る）
  2. 散乱の表し方（向きを混ぜる VBS ／くじ／エネルギー分割）
  3. 受音の拾い方（受音球で待つ／★2 次音源で直接送る）
  4. 回折（散乱係数に押し込める／陽に解く／低域は波動解法に任せる）
- ★★**このプログラムはいまどこにいるか** … **早期も後期も虚音源で厳密に解く形は
  市販には無い**。だから初期反射の指標（EDT・C50・STI）は強く、後期残響だけが
  構造的に外れる。**いま T20 / T30 が合わないのは実装の誤りではなく、
  まだ持っていない機能のため**、という位置づけを明示した

★各社の内部仕様は版で変わるので、**公開資料・論文から読み取れる範囲の整理**である旨を
副題に明記した（断定しない）。

### 逐次ログ（追記）

- 「他のソフトの手法の比較も一緒に」→ 4 枚追加（一覧表／主要 3 本／4 つの分かれ目／
  自分たちの位置づけ）。★あふれ点検を通して 22 枚すべて枠に収まることを確認
