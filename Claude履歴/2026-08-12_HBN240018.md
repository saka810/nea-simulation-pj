# 2026-08-12（HBN240018）

## 本日のやり取り

- 別プロジェクト（OneDrive 上）の `CLAUDE.md` と同じ要領で、このリポジトリでも作業履歴を
  逐次残す仕組みを整備した。OneDrive のような自動同期がないため、履歴の受け渡しは
  **Git（origin = saka810/nea-simulation-pj）** で行うルールとした。
- 作成物：
  - `CLAUDE.md`（新規）… 履歴記録ルール（逐次ログ＋要約整理）、Git 同期の手順、
    リポジトリの目的・構成・作業上の注意。
  - `Claude履歴\`（新規）… 本ファイル。`YYYY-MM-DD_PC名.md` 形式。

## 逐次ログ

- 2026-08-12 別プロジェクトの `CLAUDE.md` を読み、同等のルールを本リポジトリ向けに作成。
  OneDrive 同期がない点を踏まえ、セッション開始時 `git pull` ／区切りごとに履歴を
  独立コミットして push する運用を明記。`Claude履歴\` を新設し本ファイルを作成。
- 2026-08-12 履歴中の「京大フォルダ」という具体名を「別プロジェクト」に修正（依頼による）。
