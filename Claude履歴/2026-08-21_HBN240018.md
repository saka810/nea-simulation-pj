# 2026-08-21 HBN240018

## 本日のやり取り

前日から続けて、**実案件 2 室（研修室・視聴覚室）の計算**と、**計算の高速化**。
高速化は通しで **10.7 倍**になり、あわせてチーム共有用の説明資料を作った。
後半は**結果の見せ方と出力の追加**を一気に進めた（全測定点のまとめ表・結果フォルダの構成・
ファイル名に対象室と条件名・吸音率と理論値を 1 ファイルに・**材料条件表**・
**Excel 出力**・**STI**・**帯域別の音圧レベル**）。いずれもユーザー要望。

---

## 1. モデルの更新に追随（リージョン＋ソリッド混合）

- `JR\視聴覚室_リージョンソリッド混合.dxf`（部屋=REGION 70 / 反射板=3DSOLID 11）
  → **問題なく読める。**全リージョン版と**小数点以下まで完全一致**
  （面 292 / 総表面積 1094.828798 m² / 容積 1152.269158 m³ / パッチ 114）。
  反射板 132 枚の法線も全部空気側。**この作り方を続けて問題ない**
- `JR\研修室.dxf`（REGION 23 + 3DSOLID 5）。吸音材リストを 21 材料・**8 バンド**に更新、
  レイヤ名も ID に合わせて改名 → 9 レイヤすべて材料が引けた
- 両モデルとも受音点 5 点に

**不具合修正**：統計残響式が `is_closed` で使用可否を判定していたため、T 字接合のある
モデル（研修室）で「形状が閉じていないので容積が決まりません」と出て計算が飛んでいた。
`model.volume` があるかで判定するように変更。

---

## 2. 高速化（F-4 / F-5 / F-6 / F-7a）

ユーザー指摘 3 つがそれぞれ効いた。

### ① 同一平面パッチ単位の交差判定（F-4、前日 705ccf6）

> 三角形要素で分割しているのは、ねじれのない面を定義する必要があるため。
> ねじれの無い平面であれば、多頂点要素でも良いのでは？

そのとおりだった。判定 **2.3〜3.0 倍**。★**受音経路が 2.2 倍**になった副産物のほうが大きい
（三角形単位だと「同じ壁の隣の三角形に当たっただけ」でバックトレースが却下していた）。

### ② バックトレースの配列演算化（F-5）

全経路を同時に、反射回数 k を大きいほうから 1 段ずつ下ろす。
「長さ L の経路は k = L のときに入る」と考えると動いている経路は常に同じ k にいる。
**28.7 倍**（31.54 秒 → 1.10 秒）。1 本ずつの `backtrace_path` は参照実装として残す。

### ③ 音線追跡を受音点間で共有（F-6）

> 一回の音線追跡で受音判定はできないですか？

コードを読むと**追跡は受音点に依存していなかった**（受音しても音線は打ち切られない）。
受音球を複数置いて同時に判定するようにした。

    研修室 / 音線 6,000 本 / 160 反射 / 受音点 5 点
      旧 142.0 秒 → 新 13.2 秒（10.7 倍）。全 5 点で結果は完全一致

### ④ 複数音源の追跡（F-7a）

音線は音源から出るので共有できないが、**音源ごとの音線を 1 本の配列に並べて追跡**。
戻り値は `[音源][受音点]`。パイプライン統合（F-7b）は設計まで。

段階別の内訳が変わった：以前は 追跡 34%（×受音点数）／バックトレース 65%（×受音点数）。
いまは追跡は 1 回で共有、バックトレースは 28.7 倍。

---

## 3. 実案件の計算結果（5 受音点・反射 300 回・打ち切り 6 秒・8 バンド）

### 研修室（8.5 × 14.64 × 4.5 m / 556.3 m³ / 580.8 m²）

| | 63 | 125 | 250 | 500 | 1k | 2k | 4k | 8k |
|---|---|---|---|---|---|---|---|---|
| T30 平均 | 1.282 | 1.027 | 0.864 | 0.592 | 0.299 | 0.225 | 0.233 | 0.265 |
| T30 の幅 | 0.230 | 0.183 | 0.069 | 0.069 | 0.114 | 0.078 | 0.081 | 0.087 |
| EDT 平均 | 1.333 | 1.044 | 0.759 | 0.467 | 0.285 | 0.293 | 0.279 | 0.261 |
| Eyring-Kn. | 1.831 | 0.862 | 0.389 | 0.251 | 0.198 | 0.211 | 0.193 | 0.185 |

C50 平均 0.16 / 1.18 / 2.82 / 6.58 / 15.00 / 15.00 / 15.34 / 15.22 dB
D50 平均 0.51〜0.97。受音経路 8,825〜9,161 本。

### 視聴覚室（16.1 × 18.0 × 4.5 m / 1152.3 m³ / 1094.8 m²）

| | 63 | 125 | 250 | 500 | 1k | 2k | 4k | 8k |
|---|---|---|---|---|---|---|---|---|
| T30 平均 | 1.360 | 1.190 | 0.950 | 0.749 | 0.603 | 0.537 | 0.477 | 0.407 |
| T30 の幅 | 0.408 | 0.183 | 0.115 | 0.092 | 0.142 | 0.081 | 0.157 | 0.018 |
| EDT 平均 | 1.511 | 1.138 | 0.903 | 0.656 | 0.349 | 0.354 | 0.352 | 0.326 |
| Eyring-Kn. | 2.038 | 0.945 | 0.455 | 0.286 | 0.213 | 0.227 | 0.214 | 0.201 |

C50 平均 −1.40 / 1.10 / 2.07 / 5.12 / 10.42 / 10.53 / 10.78 / 12.45 dB
D50 平均 0.42〜0.94。受音経路 3,242〜3,553 本。

### 読み方

- **中高域は非常に明瞭**（1 kHz 以上で研修室 C50 = 15 dB / D50 = 0.97）
- **低域が突出**（63〜250 Hz の T30 が 1 kHz 以上の 3〜5 倍。63 Hz の C50 が 0.2 / −1.4 dB）
- **T30 が統計値の 2 倍前後**なのは減衰が 2 段になっているため。吸音の分布が極端に
  不均一（吸音天井・壁 対 反射板・窓）＋天井近くの反射板が吸音天井を遮る。
  Sabine / Eyring は拡散音場前提なのでこの形は表せない。**知覚に効くのは EDT**

### ★条件の収束を確認した

反射 160 回・打ち切り 2 秒の結果と、300 回・6 秒の結果が**実質同一**（差は小数第 3 位以下）。
受音経路は 4% 増えたが、増えたのは T30 の測定区間より後ろだった
（63 Hz が −35 dB に到達するのは 0.73〜0.74 秒）。**2 秒版で既に収束していた。**
2 秒版は `バックアップ_2秒打ち切り_0821_0703\` に残してある。

実所要時間：研修室 約 12 分 ／ 視聴覚室 約 27 分（前回は 160 回・2 秒で 1 時間 45 分）。

---

## 4. その他

- **顔の向きを受音点ごとに持てるようにした**。`head_azimuth` が数値でもリストでも読める。
  面の確認画面は `w` で調整する受音点を切り替え（矢印は全点に描き、調整中だけ濃い黄色）。
  **後から直すのは `run_project.py --redraw`**（音線追跡をやり直さず図だけ描き直す）
- 面の確認画面のパネルの並びを直した（材料 19 種で向きのスライダが画面外に出ていた）。
  **長い一覧は最後に置く**
- 設定画面の容積自動算出に**総表面積と平均自由行程を参考表示**
- `docs/高速化の説明.md` を新設（チーム共有用）。Web 版も公開

## 5. 結果の見せ方を整えた（G-21 / G-22 / G-23 / G-24）

すべてユーザー要望。**計算はやり直していない**（既存の結果を移行しただけ）。

### G-21 全測定点のまとめ表（`summary.py` 新設）

> 全測定点の結果を 1 つのファイルで見たい。残響時間については理論値も一緒に。

`結果/<室>_まとめ_残響時間.csv` … rec1〜recN の EDT / T20 / T30 ＋ **平均** ＋
**ばらつき（最大−最小）** ＋ **理論値 3 式**（Sabine / Eyring / Eyring-Knudsen）。
`結果/<室>_まとめ_明瞭度.csv` … C50 / C80 / D50 / Ts で同じ形。

1 列目が受音点・2 列目が項目・3 列目以降が周波数（**周波数は横**の共通ルール）。
受音点ごとの CSV はそのまま残す。計算後に自動で作られ、
作り直しだけなら `python summary.py <プロジェクト>`。

### G-22 結果フォルダの構成を統一

> rec1 だけが「結果」というフォルダ名になっているのが気になります。
> どちらかというと、結果フォルダの中に Rec1〜のフォルダが出来るものでは？

そのとおりで不揃いだった（1 点目だけ `結果/` 直下、2 点目以降が `rec2/結果/`）。

    結果/                 ← 受音点に依らないもの（室の吸音と理論値・音線軌跡・まとめ表）
      rec1/ … rec5/       ← 受音点ごと（pulses / ir / rt / decay / clarity）
    図/
      rec1/ … rec5/
      画面/               手で撮った画像（計算し直しても消えない）

以前は統計残響式・材料別面積・音線軌跡が受音点ごとに 5 重複していた。
受音点ごとに作られていた `project.json` も廃止（条件は 1 つだけ）。
実装は `Project.receiver_index` を立てると `result_path` / `figure_path` が
自動で振り分ける形にしたので、呼び出し側は変えていない。

移行済み：研修室_PJ（70 ファイル）／視聴覚室_PJ（70）／ModelTest の視聴覚室_PJ（14）。
バックアップフォルダは触っていない。**古い置き方の結果も読める**ようにしてある。

### G-23 結果ファイル名に「対象室＋条件名」

> 対象室および条件名をファイル名に入れるようにするので、それを結果ファイルの名前にも。

プロジェクト名（`Project.name`）を頭に付ける（`研修室_条件A_rt.csv`）。**図にも付く**。
報告書やメールでフォルダの外に出ると `rt.csv` ではどの室・どの条件か分からないため。

- 条件入力に**「対象室・条件名」欄を新設**（空欄ならフォルダ名）。
  ファイル名に使えない文字と空白は `_` に置き換える（`safe_name`）
- 書くのは `result_path()` / `figure_path()`、**読むのは `existing_result_path()`**。
  頭なしの昔のファイルも探すので、作り直す前のプロジェクトもそのまま開ける
- `clear_results()` は昔の名前も消す（残っていると今回の結果と並んでしまう）
- ★`_sub_project()` が `name` に「受音点N」を足していたのをやめた。
  足すと**ファイル名が受音点ごとに変わる**。番号は `receiver_index` が持つ

### G-24 `rt_statistical.csv` ＋ `surface.csv` → `<室>_吸音率と理論値.csv`

> rt_statistical と surface は一緒のファイルにして欲しい。
> かつ、surface に平均吸音率を算出して出しておいて欲しい。
> 表の順番のイメージ：材料毎の吸音率／平均吸音率／残響時間理論値

どちらも受音点に依らない室の性質で、見るときは続けて追うので 1 枚にした。
**並び順はユーザー指定のまま。**

    区分,項目,面積_m2,63,125,…
    材料別の吸音率,11__研修室_壁_吸音,67.1625,0.05,0.09,0.36,0.77,…
    平均吸音率,平均吸音率,580.7705,0.0805,0.1632,0.3260,0.4579,…   ← ᾱ=ΣSᵢαᵢ/S
    平均吸音率,等価吸音面積_m2,,46.78,94.79,189.36,265.94,…        ← A=S・ᾱ
    平均吸音率,空気吸収_4mV_m2,,0.077,0.264,0.710,1.345,…
    残響時間理論値,sabine_s,,1.912,0.943,0.472,0.336,…
    残響時間理論値,eyring_s,,1.833,0.864,0.390,0.251,…
    残響時間理論値,eyring_knudsen_s,,1.831,0.862,0.389,0.251,…

- **平均吸音率は新しく出したもの**（面積で重み付けした ᾱ）。面積の欄は総表面積
- **等価吸音面積 `A = S・ᾱ` も並べた**。理論値は `T = 24 ln10・V/(c・A)` なので、
  ᾱ → A → T がこの表の中でつながる
- 書くのは `project.write_room_csv()`、読むのは `read_room_csv()`。
  **分かれていた頃の 2 ファイルも読める**（`_load_legacy_room`）

### ついでに直した不具合

- `has_results()` が `結果/rec1/` を見ていなかった（置き場を変えたときの取り残し）。
  結果があるのに条件入力が「計算結果あり」を出さず、「前回の結果を見る」も塞がれていた
- `load_results()` の明瞭度が `結果/` 直下を見ていた（いまは `結果/recN/`）

テストは 296 → **323 件**、全部通っている。

---

## 6. 依頼 4 件（材料条件表・Excel・STI・音圧レベル）

### 依頼① 材料条件表（G-25）

> 壁の吸音材条件を変える場合に、CAD データを変えて、それをまた読み込んでもらって、
> っていうのが大変。レイヤー名と壁材料の一覧表（条件表）を作って、
> それを読み込ませる形式に変えたい。ソフト上で吸音材かえた場合に、
> その記録を反映しておけば、見返しやすいとも思いました。

`プロジェクトフォルダ/材料条件表.csv` を新設（`condition_table.py`）。

    区分,レイヤー名,材料名,面数,面積_m2,63,125,…
    レイヤ,09__研修室_床,ﾀｲﾙｶｰﾍﾟｯﾄ(GA-100)6.5t,4,123.964,0.005,0.010,…
    面ごとの指定,11_吸音板,11_吸音板,12,67.163,0.026,0.049,…

- 書き換えるのは**材料名の列だけ**。空欄なら従来どおりレイヤー名（先頭の番号）で引く
- 面数・面積・吸音率は参考で毎回書き直す。`#` 行は覚え書き（対象室・モデル・吸音率の種類）
- ★**利用者が書いた材料名は絶対に上書きしない。**消えたレイヤも
  `（モデルに無し）` として残す（前の設定を見返せるように）
- `face_editor` で面に貼った材料は区分 `面ごとの指定` に**記録**される
- 入口は条件入力の「材料条件表を開く」（Excel で開く）と
  `python condition_table.py <フォルダ>`
- `project.json` の `assignment` より条件表が優先。**G-9b のレイヤ割り当ては解決**

研修室（9 レイヤ）・視聴覚室（8 レイヤ）ぶんを生成済み。材料名は自動で埋まっている。

### 依頼② CSV と Excel の役割分担（G-26）

> CSV の方が、逐次読み込む際に都合が良いですか？ .xlsx でまとめた方が、
> グラフを作ってもらうのも、テンプレに当てこむだけ、というのが出来るかなと。
> ソフトで読み込む用のデータと、体裁を整える用のファイルを分けても良いかも。

**分けるのが答え**として実装（`workbook.py`）。

| | CSV | Excel |
|---|---|---|
| 誰が読むか | プログラム（`--redraw`・まとめ表・他ソフト） | 人（報告書・打ち合わせ） |
| 良いところ | 1 行ずつ読める・差分が見える・文字コードだけで開ける | 1 枚で全部見える・**グラフ付き**・体裁を作れる |

`結果/<室>_結果一式.xlsx` に 7 シート（概要・残響時間・明瞭度・音圧レベル・STI・
吸音率と理論値・材料条件表）＋ネイティブのグラフ 5 枚。
まとめ表の CSV を並べ直すだけなので**計算はやり直さない**。

`--template 雛形.xlsx` で**会社の雛形の書式・グラフ・ロゴを残したまま値だけ入れる**。
書き出しは必ず A1 からにしてあるので雛形のセル参照がずれない。
openpyxl を追加（Excel だけに要る。CSV は無くても書ける）。

### 依頼③ STI（G-27）

IEC 60268-16。**インパルス応答を帯域分割して積分せず、パルス列から直接
変調伝達関数 m(F) を出す**（フィルタの遅れや裾の影響を受けない。幾何音響では普通この形）。

    m(F) = |Σ E_i e^{-j2πF t_i}| / Σ E_i
    実効SNR = 10log10(m/(1-m)) を ±15 dB に丸める → TI → MTI → STI

- 男声・女声の両方を出す（女声は 125 Hz の寄与ゼロ）。評価区分（優/良/可/不可/劣）も
- 背景騒音と聴覚マスキングは **PWL と騒音レベルの両方があるときだけ**効かせる
- **6 バンド（125〜4k）では 8 kHz が無い**ので重みを外して正規化し直し警告する
- 検算：指数減衰の MTF が解析解 `1/√(1+(2πFT/13.8)²)` と 1e-6 で一致。
  T=1 s の STI が文献値 0.59 と一致

**研修室の STI 平均 0.852（優）／ばらつき 0.047**（rec1〜rec5 で 0.827〜0.874）。
C50 が 15 dB 出ていたのと整合する。

### 依頼④ 帯域別の音圧レベル（G-28）

> 無響室の計算で逆二乗がどれくらい成り立っているかを確認したりしたい。
> 点音源の PWL を与えれば絶対値も、未入力の場合は相対値でも良い。

    E_i = A_i · exp(-m·d_i) / (4π d_i²)      ← パルス列には距離減衰が入っていない
    Lp  = Lw + 10log10(Σ E_i) + 10log10(ρc/400)

★**逆二乗則が実装の物差しになった。**反射面が無ければパルスは直接音 1 本だけで
`Σ E_i = 1/(4πd²)` なので `Lp = Lw − 20log10 d − 11` に**厳密に一致**する
（実測の差 1e-14 dB、距離 2 倍で 6.02 dB 減）。
`spl.csv` / `spl.png` / まとめ表に**自由音場との差**を必ず並べてあり、
0 dB なら逆二乗どおり、外れた分が室の効きになる。

- PWL 未入力なら **Lw = 0 dB（1 pW 基準）として相対値**。出力にどちらか書く
- 成分に分けて出す（直接音 / 反射音 / 初期 50 ms / 後期 / A 特性）
- `atmosphere` に湿り空気の密度を追加（20℃ で ρ=1.200、ρc=412.5 → 補正 +0.13 dB）

研修室（5 点）の実測：**直接音は全点で自由音場の理論値と一致**、
自由音場との差は 63 Hz で +9.7〜+15.2 dB、1 kHz で +3.4〜+7.0 dB。
音源距離 3.0 / 4.3 / 6.0 m。

### まとめ表と表の形

- まとめ表を 4 枚に（**音圧レベル**と **STI** を追加）。
  音圧レベルは 3 列目に**音源距離**を入れてそのまま逆二乗の確認に使える。
  平均は**エネルギー平均**で取る（dB をそのまま平均しない）
- `table.py` に**区分付きの表**を追加（1 列目 区分 / 2 列目 項目 /
  3 列目 周波数に依らない値 / 4 列目以降 周波数）。
  `吸音率と理論値.csv` / `spl.csv` / `sti.csv` がこの形
- `--redraw` は既存の CSV を書き換えないが、**無い CSV は作る**。
  あとから足した指標を過去のプロジェクトに反映する唯一の道なので

**研修室・視聴覚室は計算し直さずに**音圧レベル・STI・条件表・Excel を生成した
（どれもパルス列から出るため）。テストは 323 → **391 件**、全部 OK。

---

## 7. 依頼 4 件（条件の切り替え・一括計算・経路の使い回し・数値判定）

### 条件はファイルで選び、名前もファイル名から取る（G-29）

> 同一フォルダ内に、複数条件のファイルを入れる予定です。おそらく、DXF のファイル名は
> 物件名や部屋名、条件表のファイル名は条件名にすると思うので、それらを保存する
> 結果ファイルなどの名前に使ってください。

    結果ファイル名の頭 ＝ 対象室名 ＋ 条件名
                          ↑            ↑
                          DXF のファイル名  材料条件表のファイル名

    研修室.dxf ＋ 吸音追加案.csv → 研修室_吸音追加案_rt.csv

- 条件入力に**「材料条件表（CSV）」の選択欄**を追加。同じフォルダに
  `現状.csv` `吸音追加案.csv` … と並べて選び替える
- 対象室名の欄は**空欄なら DXF のファイル名**を使う（書けばそちらが優先）
- ★**条件に依らないファイルには条件名を付けない**（経路の npz と音線軌跡）

### 一括計算（G-29）

「全条件を一括 ▶▶」ボタン、または `python run_project.py <フォルダ> --conditions`。
条件表は**中身で判別**して探すので（1 行目が `区分,レイヤー名,…`）、
吸音率表や結果 CSV を拾わない。

最後に**条件を横に並べた比較表**（`<室>_まとめ_条件比較.csv`。EDT / T30 / C50 /
D50 / Lp / STI の全点平均）を作り、条件ごとの Excel に「条件比較」シートを入れる。

### ★経路の使い回し（F-9）― 指摘のとおりだった

> 音線ループからバックトレースまでは、とくに吸音の違いは関係なく、最後の虚像法による
> 計算の時にようやく吸音の効果が反映されるのかなと思っています。つまり、わざわざ
> 最初から計算し直さなくても、最後の部分だけデータを残しておけば、データの流用および、
> 再計算時に、そこから再開できませんか？

そのとおり。整理すると吸音が効くのは**バックトレースのエネルギー計算だけ**。

| 段階 | 吸音に依存するか |
|---|---|
| ①音線追跡 | **しない**（鏡面反射だけ。受音しても打ち切らない） |
| ②重複削除 | しない |
| ③経路の検証（虚音源が成立するか） | **しない**（純粋に幾何） |
| ③'エネルギー | **する**（各反射で `|R(θ,α)|²`） |

だから経路ごとに**反射面の並び**と**各反射の cosθ**（＋時刻・距離・方向）を
`結果/recN/<室>_経路.npz` に残せば、吸音材を変えた再計算は

    E = Π_k |R(cosθ_k, α_面k)|²

の**配列の掛け算だけ**で済む。元のエネルギーと **1e-15 で一致**することを確認。

★**落とし穴を 1 つ見つけた。**交差判定のパッチは「同一平面＋辺で連結＋**同じ材料**」で
まとめているので、材料の**分け方**が変わるとパッチの切れ目が動き、見つかる経路も変わる
（吸音率の**値**を変えるだけなら変わらない）。そこで**指紋**（頂点・法線・
パッチの分け方・音源・受音点・音線数・反射回数・受音球・両面）を保存して突き合わせ、
食い違えば理由を告げて①からやり直す。**吸音率の値は指紋に入れない。**

`clear_results()` はこの npz を消さない（作り直すのに音線追跡が丸ごと要るうえ、
古いかどうかは指紋で分かる）。`--no-reuse` で使い回しを切れる。

### Excel の数値判定（ユーザー指摘）

> 面積が数字として認識されていない、他のシートも同じように数値判定されていないのが
> あるので、チェックして修正して。

`SHEET_LAYOUT[シート]["text"]`（名前の列の数）を決め、**そこより右は全部**
数値に直すようにした。**周波数に依らない列（面積・総合・音源距離・面数）も対象**。
整数は書式 General、小数は 0.000。
「数値になるのに文字列のままのセルが無い」ことを**全シート走査でテスト**している。

### 音圧レベルの自由音場（ユーザー判断で削除）

> 音圧レベルで、自由音場と自由音場との差はいらないです。
> あくまでも、そういう場合もある、というだけなので。

`spl.csv`・まとめ表・図から外した。`freefield_level()` は残してあり、
**逆二乗則が厳密に成り立つことの検算**にテストで使っている。

### ついでに直したバグ

`MaterialLibrary.to_csv()` が書いた CSV を `from_csv()` が読み戻せなかった
（吸音率のあとの `kind` / `note` 列で行を捨てていた）。数値でないセルが出たら
そこで打ち切るようにした。**GUI で材料を足して保存すると消える**経路だった。

テストは 391 → **441 件**、全部 OK。

---

## 8. 条件表を xlsx に作り直した（依頼）

> DXF ファイルを読み込んでレイヤー名等を抽出して、条件シートを作る仕組みを作って欲しい。
> 条件表に吸音率のデータを載せるのは、ミスリードを誘発するのでやめて欲しいです。
> 材料番号入力列を作って、その番号をもってくるようにしてください。
> 同ファイルで、条件違いをシートを分けて記載したいのと、「吸音率」シートを一緒に同梱して…

    プロジェクトフォルダ/条件表.xlsx

      シート「吸音率」    番号・材料名・吸音率・種類（★PJ 固有の吸音データをここに）
      シート「現状」      区分 / レイヤー名 / 材料番号 / 材料名（参考）/ 面数 / 面積
      シート「吸音追加案」 ← シートを複製して番号を書き換えるだけ

- **「条件表を作成」ボタン**（設定画面）で DXF のレイヤから作る。
  レイヤ名の先頭番号から材料番号を推定して入れる（研修室 9 レイヤ・材料 19 件）。
  用意してあるときは押さずに「参照…」で選び、条件は「条件（シート）」で選ぶ
- ★**条件シートに吸音率を載せない**（ミスリード防止。吸音率シートにだけ置く）
- ★**入力は材料番号の列**。材料名は隣に `VLOOKUP` の**参考**として出るので、
  番号を入れれば Excel 上で名前が出る。**読むときは番号の列しか見ない**
  （全角・半角のミスタイプを避けるため）
- ★**「吸音率」シートを材料一覧として読む**。`absorption.csv` を別に持ち回らなくてよい
  （材料一覧は「吸音率シート → 吸音率 CSV」の順で探す）
- **シート名が条件名**になり結果ファイル名に入る。一括計算はシートを 1 件ずつ回す

### 互換のために入れた仕掛け

- 昔の `材料条件表.csv` も読める。ただし**既定名の CSV は xlsx があれば条件に数えない**
  （移行後に同じ内容が 2 条件として並ぶのを防ぐ）
- ★読むときのファイル名候補に**「対象室名だけ」を足した**。条件表を作った瞬間に
  条件名が付いて、**前の結果が読めなくなる**（`--redraw` が落ちる）のを防ぐ
- 条件（シート）を**選ばなければ結果ファイル名に条件名を付けない**。
  既存プロジェクトの名前が勝手に変わらないようにするため（選んだときだけ付く）

実測（研修室のコピーで確認）：シート「吸音強化案」で反射板を吸音材（番号 14）に
置き換えたら、平均吸音率 1 kHz が 0.539 → 0.728、Lp が −20.7 → −23.5 dB、
STI が 0.899 → 0.885。**2 件目は経路を使い回して一瞬**。

テストは 441 → **464 件**、全部 OK。

---

## 9. 積み残し

- **G-20 CLI の進捗バー**（`progress_window` は GUI 用。CLI は文字だけ）
- **F-7b 複数音源のパイプライン統合** — パルス列の合成方法（非干渉／干渉）が要判断
- **F-8 音線経路の保存** — 受音点の後追いに要る。20 万本×300 反射で 1 GB 近い。要判断。
  いまは「受音点は最初に多めに置く」のがいちばん安い（追跡は受音点数に依らない）
- **G-9b レイヤ→材料の対応の入力 UI**
- **B-20 DWG / REGION の変換スクリプトをリポジトリに入れるか**
- 音線軌跡に受音点の番号を持たせる（いまは「5 点のどれかに届いた」が受音の印）

## 逐次ログ

- 混合モデル（REGION＋3DSOLID）を検証 → 全リージョン版と完全一致
- 研修室の材料リスト更新・レイヤ改名に追随、統計残響式の判定不具合を修正
- 両室 5 受音点で計算（1 時間 45 分）→ 高速化を実装 → 条件を 1.9 倍にして 39 分で再計算
- バックトレースの配列演算化（28.7 倍）／追跡の共有（通し 10.7 倍）／複数音源の追跡
- 顔の向きを受音点ごとに、パネルの並び、設定画面の総表面積
- 説明資料（`docs/高速化の説明.md` ＋ Web 版）
- 両室の音線・音粒子を画面で確認
- 全測定点のまとめ表（`summary.py`。理論値も同じ表に）を新設（G-21）
- 結果フォルダの構成を `結果/recN/` に統一、既存 3 プロジェクトを移行（G-22）
- 結果ファイル名と図に「対象室＋条件名」を付ける。条件入力に名前欄を新設（G-23）
- `rt_statistical.csv` ＋ `surface.csv` を『吸音率と理論値.csv』に統合し、
  **平均吸音率**と等価吸音面積を追加（G-24）
- 材料条件表（レイヤー名 → 吸音材の CSV）を新設。CAD を触らずに材料を変えられる（G-25）
- 結果一式の Excel 出力（7 シート・グラフ付き・雛形対応）。CSV は機械読み用に残す（G-26）
- STI（IEC 60268-16。パルス列から m(F) を出す）。研修室の平均 0.852（G-27）
- 帯域別の音圧レベル。**逆二乗則が厳密に成り立つことを確認**（G-28）
- 条件（吸音材）を条件表のファイルで切り替え、一括計算できるようにした（G-29）
- 結果ファイル名を「DXF 名＋条件表名」に。条件に依らないものは条件名を付けない
- **経路の幾何を保存して吸音材だけ変えた再計算に使う**（F-9）。指紋で古さを判定
- Excel の数値判定を全シート直した（面積が文字列のままだった）
- 音圧レベルから自由音場の値と差を外した（ユーザー判断）
- `MaterialLibrary.to_csv` → `from_csv` の往復が壊れていたのを修正
- 条件表を **xlsx** に作り直し（材料番号入力・吸音率シート同梱・条件はシートで分ける）。
  「条件表を作成」ボタンで DXF のレイヤから作れる
