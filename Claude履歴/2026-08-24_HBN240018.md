# 2026-08-24（HBN240018）

## 本日のやり取り

### 0. 記録の約束（冒頭のお願い）

> 随時ログと技術資料等への変更内容の反映を忘れないようにしてください。

以後、**1 機能ごとに「コード → 資料 → 履歴」を揃えてコミットする**（この日から徹底）。

### 1. 虚音源の可視化（`view_images.py`。G-31）

> 虚音源を可視化して見れるようにしたい。
> ・虚音源から指定した受音点（画面上で指定）を結ぶ線を描く。
> ・音線と同じように、N 回反射まで表示。N 回目反射のみを表示というのも欲しいので、
>   反射回数の開始と終了を設定できるようにすれば良いかと思います。

#### ★新しいデータの蓄積は要らなかった

到来方向を**単位ベクトルにして距離を別に持つ**という決め
（`loop_noredundancy` 冒頭。元コードは `-vtgt` の 1 つに混ぜていた）がここで効いた。

    虚音源 = 受音点 + 到来方向 × 距離        （`PulseList.image_sources`）

`結果/recN/<室>_pulses.csv` を読むだけなので**計算はやり直さない**。
そのために `PulseList.from_csv`（`save_csv` の裏返し）も足した。

虚音源と受音点を結ぶ直線が「折れた経路を伸ばしたもの」で、長さが経路長
（音速 × 到来時刻）。**壁を貫いて見えるのが正しい姿**で、壁との交点が反射点。

#### 決めごと

- **反射回数は「開始」と「終了」で絞る**（要望どおり上限 1 つではなく範囲）。
  同じ値にすれば「N 回目のみ」。既定は 0〜2 回で開く
- **受音点はスライダと `w` キー**で選ぶ。選んでいる点だけ濃い水色。
  モデル上のクリックは難しいというユーザー指摘があるので、
  操作はスライダとキーで完結させる方針（`ray_filter` と同じ）
- 本数で絞るときは**エネルギーの大きい順**（適当に間引くと効いている虚音源が落ちる）
- 色は 反射回数 / エネルギー / 到来時刻（`m` で切り替え）

#### 作りながら直したところ

- ★★**カメラを虚音源に合わせると室が点になる**（研修室で経路長 3.4 km）。
  室の範囲を覚えておき、`h` で室に寄る／`r`（VTK のキー）で全部が入る。
  目盛り（`show_bounds`）も描いたもの全部に合わせて伸びるので、室の範囲で引き直す
- ★「N 回目のみ」にすると色の値が全部同じになり、カラーバーが
  「1.0 1.0 1.0 1.0」と並んで意味が無い。**そのときは 1 色で塗る**
- 題名を自分で出すと `build_plotter` のぶんと二重になるのでやめた
- `pyvista` の `show()` は `reset_camera` を取らない（この版）

#### 検算（テスト [41]）

| 見るもの | 結果 |
|---|---|
| 虚音源までの距離 = 経路長 | 差 0（厳密） |
| 直接音（0 回反射）の虚音源 = 音源そのもの | 差 0 |
| 1 回反射の虚音源 = 壁について音源を鏡像にした点 | 差 0（6 面ぶん全部） |
| 開始＝終了＝N で N 回目のみ | 0 / 1 / 2 回で確認 |
| CSV を経由しても同じ | 一致 |

#### 入口

- 設定画面の**「虚音源を見る」**ボタン（結果があればいつでも開ける）
- `python view_images.py <プロジェクト> --start 2 --end 2`（2 回目反射のみ）

#### 反映した資料

`docs/技術説明書.md` 7.1 節（★虚音源を目で見る）／
`docs/出力・可視化方針.md`（出力⑩ と、そのためのデータの節）／
`README.md`（モジュール表・CLI の例・テスト件数）／`CLAUDE.md`／
`PROGRAM_STRUCTURE.md`（ファイル一覧）／`TODO.md`（G-31 完了）／`docs/pdf/`（作り直し）

## テスト

535 → **558 件**、全部 OK。

## 次にやると良さそうなこと（メモ）

- 虚音源の画面で**折れた実際の経路**も重ねられると、伸ばした線との対応が一目で分かる。
  反射面の並びは経路の npz（`path_cache`）にあるので、反射点を出せば描ける
- 面の確認画面の操作性（TODO G-30。提案済み・判断待ち）

### 2. 研修室で確認（依頼「確認させて／研修室を例にして」）

★**確認中にバグ 1 件と足りない機能が出たので直した。**

- ★★**`h`（室に寄る）のキー登録が抜けていた。**操作の一覧には書いてあるのに
  **効かなかった**（前の編集が assert 失敗で保存されず、説明文だけ入っていた）。
  → 登録した。**説明文を先に書いて実装を後から入れる進め方は危ない**という教訓
- ★**`show()` は最初の描画でカメラを引き直す**（描いてあるもの全部が入るように）。
  起動前に室へ寄せても効かないので、`plotter.camera_set = True` で
  「もう合わせた」と伝える必要がある（`--fit room` が効かなかった原因）
- 資料用に図を書き出せるよう `--receiver`（1 始まり）と `--fit`（all / room）を追加

#### 研修室 rec1（音源 (7.5, 7.0, 1.5) / rec1 (4.5, 7.0, 1.2)）の確認結果

| 反射回数 | 虚音源の数 | 経路長 | 到来時刻 |
|---|---|---|---|
| 0（直接音） | 1 | 3.01 m | 8.8 ms |
| 1 | 7 | 4.04〜15.57 m | 11.7〜45.3 ms |
| 2 | 16 | 5.68〜29.43 m | 16.5〜85.6 ms |
| 3 | 34 | 8.03〜44.66 m | 23.3〜129.9 ms |
| 4 | 54 | 12.04〜58.64 m | 35.0〜170.6 ms |
| 5 | 80 | 14.97〜73.90 m | 43.6〜214.9 ms |
| 合計 | 8,825（最大 300 回） | 〜3,389 m | |

★**1 回反射の 7 個が、室の実際の面と 1 対 1 で対応していた**（強い検算）。

| 虚音源 | 鏡像にした面 | 室の何か |
|---|---|---|
| (7.5, 7.0, −1.5) | Z = 0.000 | 床 |
| (7.5, 7.0, 4.48) | Z = 2.990 | **天井の反射板**（吊り物） |
| (7.5, 7.0, 7.5) | Z = 4.500 | 天井 |
| (−7.5, 7.0, 1.5) | X = 0.000 | 壁 |
| (9.5, 7.0, 1.5) | X = 8.500 | 壁 |
| (7.5, −7.0, 1.5) | Y = 0.000 | 壁 |
| (7.5, 22.28, 1.5) | Y = 14.640 | 壁 |

読み込み時のシェル寸法 **[8.5, 14.64, 4.5]** と完全に一致する。

### 3. 画面の手直し 8 件（実際に動かしてのご指摘）

| # | ご指摘 | どう直したか |
|---|---|---|
| 1 | 虚音源が最初の画面にある。**音線確認画面と並列**の想定 | 設定画面のボタンを外し、可視化ウィンドウの **Tab の輪**に入れた（音線 → 音粒子 → 虚音源）。`view_images.add_controls()` を切り出して共用 |
| 2 | **法線確認へのボタンが消えている**。初期サイズが小さい。ボタンと左のログは行を分けても良い | 初期 1180x840・`minsize` を入れ、**状態の文とボタンで行を分け**、ボタンは `grid` で**足りなければ 2 段に折り返す** |
| 3 | 虚音源の画面で**壁が見えない** | 不透明度 0.10 → **0.30**、さらに**同一平面パッチの外周を線で重ねた** |
| 4 | 数字の設定を**スピンボックス**（手入力あり）に | `ControlPanel.spinbox()`。**`slider()` の中身を差し替え**て全画面を一度に切り替え |
| 5 | 受音点の向きを**一括**（音源側／四方） | 面の確認画面に 5 ボタン。音源側は水平面へ投影して受音点ごとに算出 |
| 6 | 画像保存時に「**保存しました**」の表示 | `vg.notice()`（画面下・3 秒で消える。VTK のタイマー） |
| 7 | 動画保存時に「**保存中**」の表示 | 同上を出しっぱなしにして、終わってから消す |
| 8 | GIF の**画質が粗い**／**長さと再生速度が反映されない**／**再生・停止が押せて iPad で見られる形式** | **MP4（H.264）**に（`imageio` + `imageio-ffmpeg` を追加）。横 1280・色数の制約なし |

#### 作りながら踏んだ落とし穴

- ★★スピンボックスを**`_panel()` を呼ぶ前に作って**しまい、**モデルの上に四角が
  並んだ**（CLAUDE.md に書いてある落とし穴を自分で踏んだ）
- ★★動画で**再生速度をそのまま fps にすると 1 fps の紙芝居**になる。
  同じ絵を `hold` 枚並べて速さを合わせ、**fps は速さの整数倍**にする
  （30 固定だと割り切れず、20 コマ/秒・10 秒が 13.3 秒になった）
- `pyvista` の `show()` は `reset_camera` を取らない（この版）
- VTK に「ボタン」が無いのでチェックボックスを流用（`color_on` と `color_off` を
  同じ色にして latch して見えないようにする）

#### 新しい依存（約束どおり requirements を更新）

`imageio` / `imageio-ffmpeg`（ffmpeg の実行ファイル同梱。約 31 MB）。
**無くても止まらない**（GIF に落ちて知らせる）。

テスト 558 → **577 件**。

### 4. 動画が紙芝居になっていた（保存ログで判明）

実際に保存してもらったログ：

    [view_rays] 指定の長さに収めるため 11394 → 10 コマに間引きます

★★離散化時間 1 ms・応答 11.4 秒で全 **11,394 コマ**。再生速度 1 コマ/秒
（ユーザー指定の既定）をそのまま守ると 10 秒の動画に **10 コマ**しか入らない。
**「再生速度を動画にも反映する」という直し方そのものが間違いだった**
——画面で全部見るには 3 時間かかる速さなので、動画に持ち込めない。

考え方を入れ替えた：**動画は「指定した長さの中に全コマを詰める」**。
再生速度で見終わるほうが早ければ、そこで終わる（速度が効く）。
長さ = `min(指定の長さ, 全コマ ÷ 再生速度)`。何倍速かを表示する。

| | 前 | 後 |
|---|---|---|
| 研修室・1 ms・10 秒 | 10 コマ（紙芝居） | **300 コマ / 10.0 秒 / 30 fps / 9.6 MB** |

あわせて ffmpeg の警告 2 つ（16 の倍数でない／`-pix_fmt` の二重指定）も消した。

**教訓**：要望どおりに直したつもりでも、**実データで一度出してみないと
使えるかどうか分からない**。「反映する」の意味を取り違えていた。

### 5. 画面の手直し（2 巡目・画像つきのご指摘 5 件）

| # | ご指摘 | どう直したか |
|---|---|---|
| 1 | スピンボックスが**数値とボタンが離れている**／`…` を押さないと入力できない。**Excel と同じ形に** | **枠の右に ▲▼ を縦に重ねてくっつけ、枠を押せば手入力**（`…` は廃止）。1 行 18 → 22 px |
| 2 | 向きの一括が**チェックボックスで複数選択可**に見える | `ControlPanel.button()` を作って**押すだけのボタン**に |
| 3 | 虚音源で**受音点を大きくしないで良い** | 半径を揃え、色だけで見分ける |
| 4 | 音線・音粒子・虚音源が**一画面に全部出ていて、どれが何の設定か分からない**。左の欄もタブで。Tab キーでも | 欄に「どのタブのものか」を印付け（`begin_group`）、**選んだタブ＋共通だけ**出す。パネル上端に見出し（`tab_strip`）。Tab キーでも同じ |
| 5 | **動画がおかしい**。1 ms・1 コマ/秒・10 秒なら **10 コマ（10 ms まで）**の想定 | ★私の取り違え。**「画面をそのまま長さぶん録る」**に戻した |

#### ★★動画の件（2 回間違えた）

1. 最初：再生速度をそのまま fps に → **1 fps の紙芝居**
2. 次：長さの中に全コマを詰める → **勝手に早送り**（ユーザーの意図と違う）
3. 正解：**コマ数 = 再生速度 × 長さ**（先頭から順、間引かない）。
   全部を早送りで見たいなら**再生速度を上げる**のが筋。
   なめらかさは別物として 30 fps を保つ（同じ絵を並べる）

**教訓**：「反映されていない」の意味を勝手に補完せず、**どういう出力が欲しいのかを
先に確認する**。今回はユーザーが具体例（10 ms まで）で示してくれて確定した。

#### 落とし穴

- ★★スピンボックスは**四角を先に、文字をあとに作る**。VTK は後に作ったものが
  上に描かれるので、逆だと**枠が数字を覆って読めない**
- タブで隠す欄は `Off()` だけでなく**画面外へ置き直す**（稀に四角が残った）
- VTK のボタンの色は**状態ごとの画像**（`SetButtonTexture`）。
  `GetNumberOfStates()` は無い（setter だけ）

テスト 577 → **578 件**。

### 6. 操作ログで見つけた 2 件（研修室で触ってもらった記録から）

- ★★**`p`（点を拾う）で例外**。`PyVistaAttributeError: Attribute 'pickpoint'
  does not exist and cannot be added to class 'Plotter'`。
  **pyvista 0.48 系の取り合わせの問題**で、`Plotter.left_button_down` が
  `self.pickpoint = …` と代入するのに `Plotter` が新しい属性の追加を禁じている。
  → `enable_point_picking` の前に `pv.set_new_attribute(plotter, "pickpoint", None)`
  で**属性を先に作る**ようにした
- 動画の案内が「動画の長さを 11394 秒にするか」と助けにならなかった。
  長さで足りるなら長さを、途方もない長さなら**再生速度**を勧めるようにした
- 動画そのものは意図どおりになっていることをログで確認
  （10 コマ / 0〜9 ms / 10.0 秒 / 30 fps）

**気づき**：閉じたあとの実行ログを読むのが、いちばん確実な確認になっている
（今回も前回も、これで実害のあるバグが 1 件ずつ出た）。

### 7. 数値の枠を「その場で」打てるようにした

> あくまでも、任意入力する場合は別ウィンドウが開くのではなく、
> その中で完結させたい。難しいですか？

できた。VTK に文字入力の部品は無いので、**キー入力を横取りして自前で組んだ**。

- `KeyPressEvent` を**優先度 10** で見張り（pyvista のキー処理より先）、
  編集中だけ受け取って **`AbortFlagOn()` で先へ流さない**
- 数字・`.`・`-` を溜め、**Enter 確定・Esc 取消・BackSpace で 1 文字**
- 打ち込み中は**枠を明るく**する

★安全側にした点：
- **関係ないキーが来たら編集をやめて素通し**（でないと打ち込み中に `q` で閉じられない）
- **編集していないときは何もしない**（数字は従来どおりレイヤ選択に使える）
- 見張りを仕込めない環境では従来の別窓に落ちる

テスト 578 → **587 件**。

### 8. ★★実行ログに 24,000 行の VTK エラー（テクスチャ上限）

閉じたあとのログを見たら、こうなっていた。

    vtkOpenGLState: Hardware does not support the number of textures defined.（391 回）
    vtkShaderProgram: …（1,038 行）
    Couldn't build the shader program for depth blits

**原因は私が増やしたボタンのテクスチャ**。スピンボックス 1 つに 3 個、タブに 3 個と
`vtkTexturedButtonRepresentation2D` を並べた結果、この画面だけで 80 枚近くになり
GPU の上限を超えていた（**前々回までのログには 1 件も出ていない**ので、原因が
自分の変更だとすぐ分かった）。

**テクスチャを使わない作りに変えた。**

- 見た目 … **文字の背景と枠**（`_label`。`SetBackgroundColor` / `SetFrame`）
- 押した判定 … **クリック座標の当たり判定**（`_hit_area`。
  `LeftButtonPressEvent` を 1 つだけ見張り、登録した四角と突き合わせる）
- 3D 側のクリックは拾わない／タブで隠した欄も押せない
- 使わなくなった `_flat_button` / `_colour_texture` / `_place_button` は消した

テクスチャ警告は **391 → 0**。見た目もむしろ Excel に近くなった。

### 9. 画面に渡す吸音率が計算と食い違っていた

ログの「吸音率が未指定のレイヤ → 0.1 を使用」から。材料は条件表の「吸音率」シートで
決まるので `absorption_csv` は空で、**画面側だけ既定値に落ちていた**（壁の色が
計算と食い違う）。`run_project._absorption_table_for()` を面の確認と可視化の
両方から呼ぶようにした。研修室で 0.005〜0.951 と実際の値が入ることを確認。

テスト 587 → **595 件**。

**気づき（再確認）**：閉じたあとの実行ログを毎回読むのが効いている。
今回は**自分の変更が GPU の上限を踏んでいた**という、画面を見ただけでは
気づけない類のものが出た。

### 10. 触ってもらった結果（実行ログ）

**きれいに動いた**：274 行 / **例外 0** / **テクスチャ警告 0**（前回は 24,025 行・391 件）。
左パネルのはみ出しもタブ分けで **598 px → 92 px** に減った。

直したのは 1 点だけ。操作の一覧に**数値入力の案内が 3 行**並んでいた
（虚音源側・音線側・古い `t` の行）。同居するときは音線側にまとめた。
