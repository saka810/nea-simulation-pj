# 作業一覧（TODO）

最終更新: 2026-08-23（HBN240018）

Fortran → Python 移植の残作業リスト。着手・完了したら本ファイルを更新すること。

**このファイルの構成**：まず「やることリスト」で残作業だけを一覧する。
そのあとの「詳細」に、経緯・判断の根拠・検証結果を節ごとに置く。
各モジュールの詳細は `PROGRAM_STRUCTURE.md`、経緯は `Claude履歴/` を参照。

---

# やることリスト

## 判断待ち（打合せ・情報待ち。こちらでは進められない）

- [ ] **E-12 鏡像の式の `abs()` をどうするか** — 暫定で符号付きを採用中。後日打合せ
- [ ] **E-13 `absorption.csv` が残響室法か垂直入射か** — 出典待ち
- [ ] **E-11 インパルス応答の絶対振幅の校正** — 基準音圧・音源パワーの定義が要る
- [ ] **C-2 ベア名インポートをやめるか** — 相対インポートへ一括変更は要相談
- [ ] **B-11 音源の指向性を CAD でどう表現するか** — いまは点音源（無指向）だけ。
      ※**受音点の頭部向きは解決済み**（CAD では表さず、`face_editor` のスライダで
      決める。受音点ごとに持てる。2026-08-20）

## すぐやる

- [ ] **G-30 面の確認画面の操作性** — 2026-08-23 にユーザーから「今の感じでは使い辛い。
      ちょっとやり方を考えます」。提案は出してある（①面をクリックで 1 枚選ぶ
      ②自動判定と CAD が食い違う面だけを順送り ③材料の割り当ては条件表に移ったので
      この画面は法線に絞る ④レイヤはチェックボックスの列でなく 1 行ハイライトのリスト
      ＋「単独表示」⑤断面スライダ）。**どれを入れるかは判断待ち**
- [ ] **E-15 減衰が直線にならない件の確認** — 研修室モデルで低域の二段減衰を確認済み。実測と突き合わせたい
- [ ] **B-12 ブロック参照（INSERT）の展開** — 現状は読み飛ばす
- [ ] **B-20 DWG（3DSOLID）の取り込み手順をリポジトリに入れるか** — 2026-08-19 に
      `ModelTest.dwg` で検証済み。AutoCAD の `accoreconsole` で STLOUT → 面 DXF に
      変換すれば読める。変換スクリプトはまだ scratchpad にあるだけ。**要判断**
- [ ] **B-21 吸音材リストの持ち方** — テンプレートを用意しつつ、プロジェクト作成時に
      フォルダへコピーする案を提示済み（過去案件の再現性のため）。**ユーザー判断待ち**

## GUI（G-7）— 1 ウィンドウ完結が最終形。いまは必要なウィンドウを順に開く形

- [x] G-7a 条件入力ウィンドウ（`setup_window.py`）
- [x] G-7b プロジェクトの保存・読み込み（`project.py`）
- [x] G-8 法線の確認・修正ウィンドウ（`face_editor.py`。2026-08-19 に `normal_editor.py` から改名）
- [x] G-3 インパルス応答のグラフ（`plots.py`。**正規化して PNG 保存**）
- [x] G-4 明瞭度の指標 C50 / C80 / D50 / Ts（`reverberation.clarity_measures()`）
- [x] G-5 伝搬方向の図（`plots.propagation_direction`。頭の向きは GUI で入力）
- [x] G-6 モード分布（`plots.mode_distribution`）
- [x] G-6b 経路差からのモードの積み上げと吸音の効果（`plots.mode_buildup`）
- [x] G-11 保存済みの結果から図だけ描き直す（`run_project.redraw` / `--redraw`）
- [x] G-12 いまの画面を画像・動画で保存（`g` / `b`。`図/画面/` へ連番）
- [x] G-13 ウィンドウのタイトルの文字化け（検証して駄目なら英字の題に落とす）
- [x] G-15 数値入力のキーを `e` → `t` に（`e` は VTK の終了キーだった）
- [x] G-16 **注目したい音線だけを残す**（`ray_filter.py`。`k` 種類 / `j` 基準点 / `0` 解除）
- [x] G-16b **方向は方位角・仰角のスライダで指定**（クリックは任意にした）
- [x] G-14 **表は周波数を横に統一**（`table.py`。共通ルール。CLAUDE.md にも記載）
- [x] E-17 統計残響式を Sabine / Eyring / Eyring-Knudsen の 3 つに（ミリントンは削除）
- [x] G-9a **面ごとの吸音材の割り当て**（`face_editor.py` ＋ `materials.json`）。
      面グループ（同一平面パッチ）単位で選んで、左パネルの材料をクリックで貼る（2026-08-19）
- [x] G-19 **条件入力画面の容積自動算出に総表面積と平均自由行程も出す**（参考表示）（2026-08-19）
- [x] G-16 **容積・総表面積を読み込み時に出す**（`volume_from_normals` / `surface_area` /
      `layer_areas`。`python read_dxffile.py 室.dxf` で幾何だけ確認できる）（2026-08-19）
- [x] G-17 **「容積の拾い方」表示**（`face_editor` の `m` 3 段目。外殻＝青／内側の物体＝橙／
      開いた辺＝赤い線）。どの領域を容積として拾ったかが目で分かる（2026-08-19）
- [x] G-18 **三角形分割の質**（`_ear_clip` が最小角のいちばん大きい耳を切る。
      `triangle_min_angle` / `DxfModel.triangle_quality`）（2026-08-19 ユーザー指摘）
- [ ] **G-9b 材料の編集 UI**（材料そのものの追加・変更・吸音率グラフ） — 土台
      （`MaterialLibrary`）はある。**吸音材リストの持ち方の判断待ち**（下記 B-21）。
      ※「レイヤーにどの材料を割り当てるか」は**材料条件表（G-25）で解決済み**
- [ ] **G-10 温度・湿度のスライダ** — いまは入力欄。動かしながら見たいなら要実装
- [ ] **G-7c 1 ウィンドウに統合** — どんな情報が要るかが固まってから。`pyvistaqt` + `PySide6` の導入判断も

## 性能（必要になったら）

- [x] F-4 **同一平面パッチ単位の交差判定**（`mesh_method.PatchArrays`）。
      速度 2.3〜3.0 倍 ＋ **受音経路が 2.2 倍**（三角形単位の取りこぼしが直った）（2026-08-19）
- [x] F-5 **バックトレースの配列演算化**（`loop_noredundancy.backtrace_batch`）。**28.7 倍**（2026-08-21）
- [x] F-6 **音線追跡を受音点間で共有**（`loop_reflectionmesh.loop` が受音点を複数取る）。
      通しで **10.7 倍**（受音点 5 点）。受音点を増やす費用がほぼゼロに（2026-08-21）
- [x] F-7a **複数音源の追跡**（`loop_reflectionmesh.loop` が音源も複数取る。戻り値は [音源][受音点]）（2026-08-21）
- [ ] **F-7b 複数音源のパイプライン統合** — バックトレースを (音源, 受音点) の組で回し、
      パルス列を音源ごとの遅延・利得を掛けて合成する。PA を想定。`docs/高速化の説明.md` 5 節に設計
- [x] G-21 **全測定点のまとめ表**（`summary.py`。残響時間は理論値も同じ表に）（2026-08-21）
- [x] G-22 **結果フォルダの構成を統一**（`結果/recN/`。受音点に依らないものは `結果/` 直下）
      （2026-08-21 ユーザー指摘）
- [x] G-23 **結果ファイル名に対象室＋条件名を入れる**（`Project.prefixed`。図にも付く。
      読むときは `existing_result_path` で昔の名前も探す）（2026-08-21 ユーザー要望）
- [x] G-24 **『吸音率と理論値.csv』に統合**（`rt_statistical.csv` ＋ `surface.csv`。
      材料別の吸音率 → **平均吸音率** → 残響時間理論値 の順）（2026-08-21 ユーザー要望）
- [x] G-25 **条件表**（`条件表.xlsx`。レイヤー名 → **材料番号** ＋ 「吸音率」シート同梱。
      条件はシートで分ける。DXF のレイヤから「条件表を作成」で作れる）
      （2026-08-21 ユーザー要望。G-9b もこれで解消）
- [x] G-26 **結果一式の Excel 出力**（`workbook.py`。グラフ付き・雛形に流し込める。
      CSV は機械読み用にそのまま残す）（2026-08-21 ユーザー要望）
- [x] G-27 **STI（音声伝送指数）**（`sound_level.py`。IEC 60268-16。
      パルス列から m(F) を出す。解析解と一致を確認）（2026-08-21 ユーザー要望）
- [x] G-28 **帯域別の音圧レベル**（`sound_level.py`。PWL を与えれば絶対値、
      未入力なら相対値。**自由音場＝逆二乗との差**も出す）（2026-08-21 ユーザー要望）
- [x] G-30 **画面の整理**（2026-08-21 ユーザー指摘）：条件入力を縦スクロール＋区分ごとの枠に、
      吸音率表と吸音率の種類と両面の欄を削除、「モデルから見積」で表面積と平均吸音率も表示、
      インパルス応答の長さを理論残響時間から推定。
      面の確認画面は**同一平面を 1 枚として表示**、レイヤを消すと法線も消える、
      ホイールでスクロール、操作の一覧は重ねて表示
- [x] G-29 **条件（吸音材）をファイルで切り替え、一括で回す**（`run_conditions`。
      対象室名＝DXF 名 / 条件名＝条件表名。条件比較表つき）（2026-08-21 ユーザー要望）
- [x] F-9 **経路の使い回し**（`path_cache.py`。吸音材だけ変えた再計算は
      音線追跡とバックトレースを省いて配列の掛け算だけ。指紋で古さを判定）
      （2026-08-21 ユーザー指摘）
- [ ] **B-22 元の DXF（REGION / 3DSOLID）を直接読む** — いまは面に分解した
      `*_faces.dxf` が要る。ユーザー要望「もともと作っているファイルを読み込めるように」。
      ACIS の解釈が要るので、`accoreconsole` で面に分解する変換を**プログラム側から
      呼ぶ**のが現実的（`scratchpad` の `stl2dxf.py` / `lines2faces.py` が土台）。
      2026-08-21 に「時間かかるなら後で良い」と言われて後回し
- [ ] **C-3 両面反射（two_sided）を消す** — 使わない方針になったので
      画面からは外した（2026-08-21）。`mesh_method.FaceArrays` / `procedure` /
      `path_cache` の指紋からも消す。**指紋を変えると経路の使い回しが切れる**ので、
      消すときは一緒に作り直す
- [ ] **G-20 CLI に進捗表示** — `progress_window` は GUI 用。`run_project.py` を直接叩くと文字だけ
- [ ] **F-8 音線経路の保存** — 受音点を後から足すのに要る。20 万本×300 反射で 1 GB 近い。要判断
- [ ] **F-2 空間分割（BVH / 一様格子）** — 面が数千枚を超えると効いてくる。
      パッチ単位になったので、分割の単位もパッチでよい
- [ ] **F-3 numba / マルチプロセス** — F-2 で足りない場合の次の手

## 済んだもの（2026-08-24）

- [x] **G-31 虚音源の可視化** — `view_images.py`。虚音源と受音点を結ぶ線を描き、
      **反射回数の開始・終了**で絞る（同じ値にすれば「N 回目のみ」）。
      受音点はスライダと `w` キーで選ぶ。★**計算はやり直さない**
      （パルス列から `受音点 + 到来方向 × 距離` で復元）。テスト [41]
- [x] **G-32 画角（見る向き）のセーブ・ロード** — `view_camera.py`。
      2026-08-24 ユーザー要望「条件違いで計算した結果を比較したい時に、
      音線の結果など画角などを合わせたい」。`プロジェクトフォルダ/視点.json` に
      **カメラ＋左パネルの数値＋開いていたタブ**を残し、左パネルのボタンで出し入れする。
      面の確認画面と結果画面で同じファイル。テスト [43]
      ★**名前を付けて何本でも**持てる（同日の追加要望「角度は色々保存したい」）。
      保存は名前を聞く窓、読込は選ぶ窓（削除もここ）
- [x] **G-34 操作の一覧を左の欄に戻した** — 2026-08-24 ユーザー要望。
      ページ送りが入って全部載るようになったので、
      「要点の抜粋（欄）＋全部（F1）」の二重持ちをやめ、
      **同じキーの重複した説明も落とす**（`dedupe_help`）
- [x] **B-23 面上の音源（半無響室）** — 2026-08-24 ユーザー要望。
      `source_placement.py`。面・辺・頂点に載った音源を見分け、**半球に放射**して
      載っている面の 1 次反射を起こさない。「面とみなす距離」（既定 0、
      実寸を見込むなら 0.1 m）と放射方向を設定できる。テスト [44]
- [x] **B-24 3DSOLID を直方体に限り復元** — `read_dxffile.box_from_points`。
      ACIS の頂点から 8 隅がそろうことを確かめて面を起こす。
      向きごとに `_床` `_天井` `_壁` とレイヤを分ける。★箱以外は従来どおり読めない
      （B-22 の全面対応は別途）
- [x] **G-35 逆二乗の評価（別ルート）** — `inverse_square.py`。
      受音点のレイヤ（`rec1` `rec2` `rec3`）を測線として、
      理想の 1/r² からのずれを CSV と図にする。**ふだんの出力には入れない**
- [x] **G-33 入力位置（キャレット）を明るい棒にした** — 2026-08-24 ユーザー指摘
      「キャレット？が見えないです。…どこにいるのかが見え辛いです」。
      文字列に `|` を挟む形から、**別の絵（琥珀色の棒）を重ねる**形へ。
      数字が動かなくなり、位置も一目で分かる。テスト [42]

## 小さいもの

- [ ] **E-16 1/3 オクターブへの展開が入力の範囲より外に中身を作る**（2026-08-23 に気づいた）
      — `impulse.band_mapping` が対数軸でいちばん近いオクターブバンドを選ぶので、
      オクターブ 8 バンド（上端 8 kHz ＝ 上限 11.3 kHz）の入力でも 1/3 オクターブの
      **12.7k・16k・20.2k に 8 kHz の値が写る**（低域も 16〜50 Hz に 63 Hz が写る）。
      全エネルギーの **9.4%** がそこに乗る。**元コードの手書き対応表と同じ振る舞い**
      なので今は変えていないが、入力の範囲外に中身を作っていることになる。
      高速版と厳密版の差もほとんどここに乗っている（`docs/技術説明書.md` 8.2.1）。
      → 範囲外を 0 にするか、いまのまま（元コード互換）にするかはユーザー判断
- [ ] **B-19 `test2.dxf` は床も壁もレイヤ `0`** — 吸音材を分けるならレイヤを分ける運用の周知
      （`check_model()` が「吸音率が引けないレイヤ」として指摘するようにはなった）

## 完了（詳細は下の各節）

| 節 | 内容 | 完了 |
|---|---|---|
| A | 音線追跡の完成（traceff の読み解き・番兵の廃止） | 2026-08-12 |
| B | DXF 読み込み（単位換算・多角形分割・シェル診断・音源受音点） | 2026-08-12〜13 |
| C-1/C-3/C-4 | 通し実行の検証・テスト恒久化（93 項目）・実行環境 | 2026-08-12〜14 |
| D | 既知の転記ミス 6 件 ＋ 残響室法吸音率（Paris の式） | 2026-08-12〜14 |
| E | 後半パイプライン（バックトレース〜残響時間）を全段接続 | 2026-08-14 |
| E-9/E-10/E-16 | 大気条件から音速・空気吸収／nref 不足の解消／統計残響式 | 2026-08-14 |
| F-1 | 高速化（NumPy ベクトル化。180〜250 倍） | 2026-08-14 |
| G-0/G-1/G-2 | モデルビューア／音線の可視化／音粒子のアニメーション | 2026-08-14 |
| B-18 | `LWPOLYLINE` の OCS→WCS 変換（鉛直な壁が読めていなかった） | 2026-08-14 |
| B-10 | 作図ミスの自動チェック（`check_model()`） | 2026-08-15 |
| B-9 | 複数受音点（受音点ごとにフォルダを分けて計算） | 2026-08-15 |
| E-14 | バンド数変換を対数軸の補間に | 2026-08-15 |
| G-3/G-4/G-7a/G-7b/G-8 | 図の PNG 保存・明瞭度指標・条件入力・プロジェクト・法線編集 | 2026-08-15 |
| G-5/G-6/G-6b | 伝搬方向・モード分布・経路差からの積み上げと吸音の効果 | 2026-08-15 |

---

# 詳細

以下は経緯・判断の根拠・検証結果。**上のリストで足りるなら読む必要はない。**

---

## A. 音線追跡の完成（ステップ3） → **2026-08-12 完了**

元コード `backtrace.f90` 649〜677 行の traceff の読み解きは 2026-08-12 に完了。
結論は `docs/技術説明書.md` 5.8 節と、下の「traceff の結論」に記載。

- [x] A-1 `if inside:` の中で `history_2dim.append(list(history))` に修正（コピー必須）
- [x] A-2 `if collision:` の中で**無条件に** `history.append(mesh_nearestid)` に修正
- [x] A-3 面ループ内で `soundray_comesfrom` を上書きしているバグの修正
      → 面ループは `mm.collision_distance()` の呼び出しだけにし、交点の算出は
        最寄り面が確定してから 1 回だけ行う形に整理（元コード 680〜694 行と同じ構造）
- [x] A-4 `collision` フラグを面ループの**外**で初期化するよう修正
- [x] A-5 不要な `from numpy.ma.core import count` を削除
- [x] A-6 12/04 メモの疑問コメント（疑問1〜4）を確定した回答に書き換え
- [x] A-7 **反射回数ループの範囲を `range(nref)` → `range(nref + 1)` に修正**（新規発見）
      元コード 545 行は `do k = 0, nref` で nref+1 周する。k=0 が直接音の区間なので、
      `range(nref)` だと反射回数が nref−1 回までしか出ない 1 つ足りない状態だった
- [x] A-8 可視化用の軌跡レコーダ `ray_recorder.py` を新設し、`loop()` にフックを追加
      （`recorder=None` なら無効。本線の計算・速度には影響しない）

### traceff の結論（メモ）

- 受音判定（663行）が `tractmp(k+1) = jtmp`（677行）**より前**にあるのが鍵。
  受音時点の履歴は「そこまでに済んだ k 回の反射」で、これから当たる壁は含めない。
- 疑問1 → k ループの外には**出せない**。1 本の音線が別々の k で複数回受音しうるため。
- 疑問2 → nray×nref×nref には**ならない**。`do j = 0, nref` は受音時のみ（count 回）。
  固定長配列の行まるごとコピーなだけで、Python なら `list(history)` のコピー1回で等価。
- 疑問3 → `do j = 0, k` で**良い**。k+1 以降は必ず 0。
  0 埋めが要るのは Fortran が固定長＋`traceffn`（有効反射回数）で行長管理しているため。
- 疑問4 → inside と collision を**統合してはいけない**。両者は独立。

### A から派生した新規課題

- [x] A-9 反射履歴の先頭にある番兵 `-1`（元コード `tractmp(0) = -1`）の扱い
      → **2026-08-14 に廃止**（E-1 着手時に判断）。
      元コードは `tractmp(k+1) = jtmp` と 1 つずらして詰めており、バックトレース側は
      `tracred(i,k)` を **k = 1 から**読む（元コード 903 行）。つまり添字 0 の -1 は
      **一度も参照されない固定長配列のパディング**でしかない。
      Python は可変長リストなので持つ意味がなく、`mesh[history[0]]` が -1 で
      最後の面を指す事故のもとになるため落とした。**反射回数 = `len(history)`**

---

## B. DXF読み込みの実装（ステップ1） → **2026-08-12 完了**

作図ルールは **`docs/DXFデータの作り方.md`** に別途まとめた。

- [x] B-1 `read_dxffile.read()` / `read_model()` の本実装（元コード 132〜283 行）
      グループコード 70 のフラグで頂点(192)/面(128)を判別、レイヤ名→吸音材、
      単位換算、縮退面の除去、外積による法線計算と正規化
- [x] B-2 `ezdxf` を使うか自前パーサか → **自前パーサを採用**。
      ASCII DXF は「グループコード / 値」が 1 行ずつ交互に並ぶだけなので、
      2 行ずつ読んでタプル列にすれば済む。外部依存を増やす理由がなかった
- [x] B-3 `mesh.py` の `vertexes` が (3,1,3) になっている形状バグの修正
- [x] B-4 吸音率CSV読み込みの実装（`read_absorption_csv()`）。
      サンプルは `data/absorption_sample.csv`。レイヤ名（日本語可）で突き合わせる
- [x] B-5 **単位の自動換算**（新規）。ヘッダ `$INSUNITS` から m へ換算。
      `unit='mm'` のような明示指定で上書きも可（`$INSUNITS=0` の unitless 対策）
- [x] B-6 **法線の扱い**。既定は `'cad'`（CAD の巻き順をそのまま使う）。
      **法線は「音が通る空気側」を向くようにモデル側で作るのが原則**で、その正解を
      持っているのは CAD モデル自身だから。`'flip'`（元コード `ynnmrev='y'` 相当）と
      `'shells'`（シェル単位で空気側へ揃える）も用意。
      ⚠ **`'toward'` / `'inward'` などの「面ごとに向ける」方式は廃止**
      （凸凹の壁や宙に浮いた家具で破綻するため。2026-08-13 のユーザー指摘）
- [x] B-13 **開いた形状（閉じていない形状）での計算を検証**（2026-08-13）。
      一面だけの壁で直接音＋1回反射のちょうど 2 経路になることを確認。詳細は下記
- [x] B-14 **シェル診断の実装**（2026-08-13）。辺の共有で連結成分に分割し、
      シェルごとに 閉/開・体積・法線の向き・外殻かを報告する。巻き順の一貫性も検出。
      → **GUI で法線を調整するための土台（G-8）**
- [x] B-15 **多角形→三角形分割を堅牢化**（2026-08-13）。
      **CAD 側で三角形に割る必要はない**（n 角形 → n−2 枚に自動分割）。
      - 凹んだ四角形 … 内側を通る対角線を自動で選ぶ（素朴な扇状分割は多角形の外に三角形を作る）
      - ねじれた四角形 … 平面からのずれを測って警告（切り方で形が変わるため CAD 側で直す）
      - 5角形以上 … 耳刈り法。**凹んでいても正しく分割できる**
      - 分割の妥当性を面積の照合（靴ひも公式 vs 三角形の合計）で自動チェックし、破綻を検出
- [x] B-16 **閉じたポリラインを面として読む**（2026-08-13、`test2.dxf` 対応）。
      CAD で輪郭だけ描く作り方に対応。`POLYLINE`（`70` に 1）と `LWPOLYLINE`（`70` に 1）。
      **頂点数の制限なし**（`test2.dxf` は 9 角形 → 三角形 7 枚）。
      面がどのエンティティ由来かを `face_sources` で報告。
      読み飛ばした非対応エンティティも種類別に数えるようにした（原因が分かるように）
- [x] B-17 **元コード付属 `absorption.csv`（ID + 材料名 + a1〜a6 の8列）に対応**（2026-08-13）。
      **ID と材料名の両方をキーに登録**するのでレイヤ名がどちらでも引ける。
      CP932 のエンコーディングにも対応（UTF-8 → CP932 → latin-1 の順に試す）
- [x] B-7 **音源・受音点の読み込み**（新規）。`src` / `rec` レイヤの POINT を拾う。
      `procedure.process()` に `None` を渡すと DXF 側の値を使う
- [x] B-8 3DFACE エンティティにも対応（POLYLINE ポリフェイスメッシュに加えて）

### 開いた形状での検証結果（2026-08-13）

**閉じた室でなくても計算できる。**面は `v·n < 0`（法線に向かって進む）のときだけ衝突と判定するので
片側だけ反射し、当たる壁がなくなった音線はそこで打ち切られる。

| ケース | 結果 |
|---|---|
| 一面の壁（法線+z）／音源・受音点とも法線側 | **反射回数 0 と 1 のみ**。直接音 2.000 m と 1回反射 2.828 m（虚音源 (0,0,−1)）。重複削除後ちょうど **2 経路** |
| 同じ壁／音源・受音点を裏側に | 直接音のみ（法線の裏からは反射しない） |
| **法線を逆向きにして音源を法線の反対側に** | **直接音のみ。反射音が消える** |
| 床＋垂直壁の二面 | 0/1/2 回反射（コーナー反射）。重複削除後 4 経路 |
| 3DFACE の一面反射板 DXF | 直接音 4.000 m ＋ 1回反射 4.472 m のちょうど 2 経路 |
| **`test2.dxf`（平面9角形＋壁の2面）** | 三角形 9 枚。床は上向き(+z)、壁は9角形の内側を向く。直接音・床反射・壁反射がすべて検出される |

**教訓**: 開いた形状では**法線の向きが結果を左右する**（3 行目のケース）。
ただしそれを「音源方向に向ける」ことで解決してはいけない（下記）。

### 法線の向きの方針（2026-08-13 のユーザー指摘で確定）

**法線は「音が通る空気側」を向く。その正解を持っているのは CAD モデル自身。**

- 室の外殻 → 室内向き ／ 室内に置く物体（机・什器）→ 外向き ／ 一面反射板 → 反射させたい側
- **両面で反射させたいものは CAD 側で厚みを持たせる**
  （厚みのある机なら上面は上向き法線・下面は下向き法線となり、どちらも空気側）
- **「面ごとに音源方向へ向ける」方式は廃止**。凸凹のある壁や宙に浮いた家具で
  向きが破綻して衝突判定がおかしくなる
- 既定は `'cad'`。読み込み側は勝手な補正をせず、**シェル診断で誤りを可視化する**方向に変更

検証（室 4×5×3 m ＋ 厚みのある机 1.6×0.8×0.05 m）:

| 項目 | 結果 |
|---|---|
| 机の上面 / 下面の法線 | +z / −z（どちらも空気側） |
| 机の上面・下面の両方で反射しているか | 両方 True |
| シェル診断 | 外殻 体積60.0000m³ 法線=inward OK ／ 内側 体積0.0640m³ 法線=outward OK |
| 凸部（角柱）を貼った壁 | 面ごとに ±x, ±y, ±z の 6 方向。`'cad'` はそのまま保持 |
| 机の法線を誤って内向きにした DXF | `'shells'` で上向きに自動補正できる |
| 旧 `'toward'`/`'auto'`/`'inward'` 等 | `ValueError`（廃止） |

### B から派生した新規課題

- [x] B-9 **複数受音点**（2026-08-15）。受音球の位置が変わると音線追跡そのものをやり直す
      必要があるので、**受音点ごとに 1 回ずつ回す**。2 点目以降は `結果/rec2/` `図/rec2/` に書く。
      `run_project.run()` が自動で振り分ける（`project.receiver` を指定すればその 1 点だけ）
- [x] B-10 **作図ミスの自動チェック**（2026-08-15、`read_dxffile.check_model()`）。
      面が読めているか／音源・受音点の有無と室内にあるか／吸音率が引けないレイヤ／
      重なった面／開いた辺／巻き順／ねじれ／極端に小さい面／読み飛ばした種別を見る。
      `procedure.process()` が計算前に必ず呼び、エラーがあれば知らせる
- [ ] B-11 音源の指向性・受音点の頭部向きを CAD でどう表現するか（**G-5 伝搬方向確認に必要**）
- [ ] B-12 ブロック参照（INSERT）の展開。現状は読み飛ばす
- [x] B-18 **`LWPOLYLINE` の OCS→WCS 変換**（2026-08-14）。DXF の Arbitrary Axis Algorithm。
      **鉛直な壁は押し出し方向が水平**になるので変換が要る。未対応だった間は
      実案件モデル（研修室）の壁 19 枚がまるごと読み飛ばされていた
- [ ] B-19 **`test2.dxf` は床も壁もレイヤ `0`** なので同じ吸音材になる。
      吸音材を分けるならレイヤを分ける運用を CAD 側に周知する（`docs/DXFデータの作り方.md` 2節）
- [x] B-20 **ねじれの報告を実寸（mm）化し、影響の目安まで出す**（2026-08-13）。
      相対値だけでは「直すべきか」の判断ができなかった。
      AutoCAD でねじれない面を作る方法も文書化（`docs/DXFデータの作り方.md` **1b 節**）:
      UCS を作業面に合わせて `PLINE`（LWPOLYLINE は高さを1つしか持てないので構造的に平面）／
      直線辺の押し出し（必ず平面）／三角形（原理的に平面）。
      `REGION` が平面でないと失敗する性質を AutoCAD 内での平面性チェックに使える

---

## C. 動作確認の足場

- [x] C-1 直方体など単純形状での通し実行（2→3→4）と結果検証 → 2026-08-12 完了
      6×4×3 m の直方体（三角形12枚・内向き法線）を検証スクリプトで手組みし、
      音線3000本・最大反射8回・受音球 r=0.3m で通した。結果は下記「検証結果」。
      ※ ①DXF読込はスタブのままなので、そこだけメッシュを手組みで代替している
- [ ] C-2 `geosim/` のベア名インポート問題（現状 cwd 依存）— 実行方法を決める
      ※ CLAUDE.md 記載どおり、相対インポートに変えるなら要相談
      ※ 検証は `PYTHONPATH=geosim` を通して実行している
- [x] C-3 検証スクリプトを `tests/` に恒久化 → **2026-08-14 完了**（`tests/test_geosim.py`）。
      pytest は使わず素の Python で走る（依存を増やさないため）。34 項目。
      ```
      .venv\Scripts\python tests\test_geosim.py
      ```
      DXF 読込／音線生成／エネルギー減衰／バックトレース／鏡像／インパルス応答／残響時間を、
      **解析的に答えが分かる問題**で突き合わせる。数式に関わるコードを触ったら必ず走らせる
- [x] C-4 **実行環境の整備**（2026-08-14 完了）。
      **Python 3.10.11**（チーム方針）で `.venv` を作り、numpy / scipy / matplotlib / pyvista を導入。
      - `requirements.txt` … 直接依存（バージョン範囲つき）
      - `requirements-lock.txt` … 動作確認済みの厳密な一式（`pip freeze` 相当）
      - `.python-version` … バージョンの明示
      - `.gitignore` に `.venv/` を追加（端末ごとに作り直す）
      - 手順は README.md「環境構築」に記載
      ※ この端末は既定の `python` が **3.13** を指すので、`py -3.10 -m venv .venv` と
        インタプリタを明示すること（取り違えやすい）

### 検証結果（2026-08-12、6×4×3 m 直方体 / 音線3000本 / 最大反射8回 / 受音球 r=0.3m）

| 項目 | 結果 |
|---|---|
| 内向き法線チェック | 12面すべて正常 |
| 受音イベント数 | 234 |
| 履歴の先頭番兵 `-1` | 保持されている |
| 面 ID の範囲 | すべて 0〜11 に収まる |
| 2次元リストのコピー | 234件すべて別オブジェクト（参照共有なし） |
| 反射回数の分布 | 0〜8 回（上限 nref=8 を超えない） |
| 直接音（反射0回） | 5本検出（音源-受音点距離 3.655 m） |
| 重複削除 | 234 → 214 経路 |
| 軌跡レコーダ | 3000本 → stride 15 で 200本記録 |
| 全節点が室内にあるか | 2000点すべて室内 |
| エネルギーの単調減少 | OK |
| 音粒子の位置補間 | t=0.05s で音源から最大 5.09 m（室内に収まる。直進なら 17.15 m） |
| npz 保存/読み戻し | 67 KB、一致 |

---

## D. 既知の転記ミス修正 → **2026-08-12 完了**

- [x] D-1 `sound_ray.py`：`np.zeros(ray_number, 3)` → `np.zeros((ray_number, 3))`
- [x] D-2 `sound_ray.py`：`sound_rays(i, 2)` → `sound_rays[i, 2]`（2 箇所）
- [x] D-3 `sound_ray.py`：`energy_decay` の反射係数、第2項 `(1+√(1-α))` → `(1−√(1-α))`（2 箇所）
      旧実装は約分により吸音率が効かず、垂直入射で常に完全吸音になっていた
- [x] D-4 `receiver_sphere.py`：条件②の比較対象を垂線距離 → 射影距離（元コード 663 行）
- [x] D-5 `mesh_method.py`：`innerproduct_from3vertexes` の作業配列 (3,2) → (2,3)
- [x] D-6 `mesh_method.py`：`collision_detection` の `< 0` → `<= 0`（元コード 625 行に一致）

元の数式（局所反応性壁面の斜入射反射率）は検証の結果**正しい式**と確認済み。
導出と検証結果は `PROGRAM_STRUCTURE.md` の `sound_ray.py` 節を参照。

### D から派生した新規課題

- [x] D-7 残響室法吸音率への対応 → **2026-08-14 完了**（`geosim/absorption.py`）。
      Paris の式を数値積分し、二分法で逆解きして z → 垂直入射吸音率を求める。
      α_s は z について単調でなく z≈1.567 で最大 0.951 を取るので、
      建築材料が乗る z ≥ z_max の枝を選ぶ。上限を超える値は丸めて警告する。
      CSV には `# kind: normal|random` で種類を宣言する（宣言が無ければ警告）。
      往復変換の誤差 1e-13、解析解との一致 1e-9 を確認済み

---

## E. 後半パイプライン（A が固まってから）

**2026-08-14 に E 節を完了。パイプライン 1〜7 が全部つながった。**

- [x] E-1 `loop_noredundancy.py` の整理 → **全面書き直し**
      虚音源の鏡像生成・バックトレース検算・エネルギー減衰・パルス列出力（`PulseList`）。
      検証: test.dxf で直接音・1次反射6本・2次反射18本すべて解析解と**誤差 0.000e+00**、
      エネルギーも手計算の |R(θ)|² と一致。
      ※ 元コード 911 行の鏡像式にある `abs()` は使わず符号付きにした（凹形状で崩れるため）
- [x] E-2 `procedure.py` のステップ5・6の接続 → **完了**。7（残響時間）も接続。
      あわせて `main()` を追加してコマンドラインから通し実行できるようにした
- [x] E-3 `impulse.py` の転記ミス修正 → **全面書き直し**（9 箇所。詳細は PROGRAM_STRUCTURE.md）
- [x] E-4 `df` の `transfer_function` への受け渡し → 引数化して解決
- [x] E-5 6バンド吸音率 → 32バンド展開表の移植 → `impulse.BAND_EXPANSION_6_TO_32`
- [x] E-6 自作FFT → `np.fft` に置き換え。**バンドパスは自作のまま残す判断**。
      `scipy.signal.firwin(..., scale=False)` と相対誤差 1e-16 で一致することを確認したので、
      依存を増やさず元コードとの対応が見える自作版のほうがよいと判断した
- [x] E-7 バンド数の不整合解消 → **6 バンド（125/250/500/1k/2k/4k Hz）に統一**。
      根拠は make_ipls の 6→32 展開表（94〜125 行）
- [x] E-8 残響時間算出（`ipls2rt_fortran.f90` 相当）→ `reverberation.py` を新設。
      減衰率が既知の合成応答で検証し、誤差の中央値 1.2 %。
      ※ 元コードの**巡回畳み込みは使えない**（プリリンギングが末尾に回り込んで
        減衰曲線に -20 dB の床ができ、T30 が理論値の 5〜19 倍になる）。線形畳み込みに変更した

### E 節で持ち越した課題

- [x] E-9 **音速の既定値** → **2026-08-14 解決**（`geosim/atmosphere.py`）。
      定数をやめ、**温度・湿度・気圧から計算**する形にした（基準 20℃/40%/101.325kPa → 343.81 m/s）。
      空気吸収も同じ大気条件から ISO 9613-1 で計算するので、両者の食い違いが起きない。
      元コードの 340.0 m/s はおよそ 14℃ 相当と判明。
      再現したい場合は `Atmosphere(temperature=14.0)`
- [x] E-10 **後部残響を出すのに必要な `nref`** → **2026-08-14 に F-1 で解消**。
      ベクトル化前は反射回数を増やすと現実的な時間で終わらなかったが、
      test.dxf で **音線 20000 本・最大反射 250 回が 145 秒**で回るようになった。
      `nref` で打ち切られた音がまだ大きいときは `procedure.py` が警告する
- [x] E-16 **統計残響式（Sabine / Eyring / Millington）** → 2026-08-14 追加（ユーザー要望）。
      `reverberation.statistical_reverberation()`。室容積と各面の面積・吸音率から算出。
      **閉じた室が前提**（開いていれば None を返して警告）。
      垂直入射吸音率を Paris の式で乱入射に変換してから使う。空気吸収 4mV も入る。
      `procedure.py` が計算結果の T30 と並べて表示する。
      test.dxf では 125 Hz 以上で T30/Eyring = 0.89〜1.26
- [ ] E-15 **test.dxf の減衰が直線にならない**（曲率が 10〜25%）。
      ※ 統計残響式との比が 125 Hz 以上で 0.89〜1.26 に収まったので、
        **大きな計算誤りではない**ことは確認できた。残るのは多段減衰の解釈。
      nref を 40 → 250 に増やしても T30 が変わらないので、反射回数の不足ではない。
      2×3×1 m という極端に小さい平行面の室で、面ごとに吸音率が大きく違うため
      **音場が拡散せず本当に多段減衰している**と考えられる（EDT と T30 の差にも表れている）。
      不具合ではない見込みだが、**もう少し現実的な寸法のモデルで確認したい**
- [ ] E-11 インパルス応答の**振幅の絶対値**（基準音圧・音源パワーの定義）が未確認。
      元コードも 1/(t·c0) で規格化しているだけなので、実測と比べるなら校正が要る
- [ ] E-12 **鏡像の式の `abs()`** → **暫定として符号付きの式を採用**（2026-08-14 ユーザー判断）。
      **最終方針は後日打合せで決定する。**
      根拠と選択肢は `docs/議論_鏡像の式のabs問題.md` にまとめてある。
      打合せまでは現状（数学的に正しい符号付き。凸な部屋では元コードと完全一致）で進める
- [ ] E-13 `absorption.csv`（元コード付属）が**残響室法か垂直入射か不明**。
      現状は `--absorption-kind` で指定する形にしてある（指定が無ければ警告）。
      出典が分かれば `# kind:` 行を書き足す。**public リポジトリには載せない**（ユーザー判断）
- [x] E-14 **バンド数変換を対数軸の補間に**（2026-08-15）。オクターブバンドは中心周波数が
      2 倍ずつ並ぶので `log2 f` の上で線形補間する。6↔8 は中心周波数が一致するため
      結果は従来と同じ（後方互換）。素性の違う表を混ぜたときに正しくなる。
      ⚠ **範囲外は外挿せず端の値を使う**（吸音率は 0〜1 に収まるので傾きを延長すると外へ出る）。
      6 バンド表を 8 バンドで使えば 63 Hz は 125 Hz のコピーのままで、これは
      「その帯域のデータが無い」という事実の表れなので鵜呑みにしないこと

---

## G. 出力・可視化（方針決定済み、実装は後）

詳細は **`docs/出力・可視化方針.md`** を参照。
モデルの確認用ビューアは **G-0 で先に作成済み**
（`geosim/view_model.py` と `geosim/view_model_gui.py` の 2 本）。
作りたい出力は次の 6 種。

- [x] G-1 **音線の可視化** → **2026-08-14 完了**（`geosim/view_rays.py --mode rays`）。
      反射経路を折れ線で描く。`--color energy|time|reflection|ray` で色分け、
      受音経路は太い黄色で重ね描き、`--received-only` で絞り込み、
      `--max-reflection` で**折れ線を途中で打ち切る**（初期反射だけ見たいとき）
- [x] G-2 **音粒子の可視化（動画）** → **2026-08-14 完了**（`--mode particles`）。
      スペースで再生/停止、矢印キーでコマ送り、スライダで時刻指定。
      `--movie out.gif` で GIF に書き出す（Pillow でコマを合成。追加依存なし）。
      任意時刻の粒子位置は全音線ぶんまとめて計算している（`RayLog.positions_at`）
- [x] G-3 **インパルス応答のグラフ**（2026-08-15、`plots.py`）。**最大値で正規化して PNG 保存**する
      （絶対振幅の校正が未了なので値そのものには意味が無い。E-11）。
      上段が波形、下段がエネルギー時間曲線。バンド別は 5 ms 移動平均の包絡線で重ねる
      （生のままだと反射音の干渉で暴れて読めない）
- [x] G-4 **各種音響指標**（2026-08-15 に明瞭度系を追加）。
      EDT / T20 / T30 は `reverberation.decay_measures()`、
      **C50 / C80 / D50 / Ts は `reverberation.clarity_measures()`**。
      C50・D50 は音声（境目 50 ms）、C80 は音楽（80 ms）。
      **時刻の起点は直接音の到来時刻**にしている（先頭の無音を含めると窓がずれる）。
      ※ STI は未実装（変調伝達関数が要るので別物）
- [x] G-5 **伝搬方向確認（図）**（2026-08-15）。`plots.propagation_direction()`。
      真上から見た人の絵（鼻と耳付き）を受音点に置き、到来方向のエネルギー分布を
      バンド別・反射次数別に重ねる。**直接音は赤い矢印**で別に描く（分布の線に埋もれるため）。
      頭の正面方向は `project.head_azimuth`（**0° = +X、反時計回り**）で、
      `normal_editor` の黄色い矢印を見ながら GUI で決める。CAD 側での表現はやめた（B-11）。
      上下の向きは扱わない（実務では水平面で足りる、というユーザー判断）
- [x] G-6 **モード分布**（2026-08-15）。`plots.mode_distribution()`。書籍 2.1 波動音響理論。
      上段が受音点のスペクトル、下段が外形寸法から出した直方体の固有周波数と累積数。
      **山がなだらかなのは不具合ではない**：モードの半値幅は `2.2/T` で決まり、
      T = 0.45 s の室なら 4.9 Hz にもなる。図の見出しにこの値を書き添えてある。
      山は `scipy.signal.find_peaks` で拾って ▼ を打つ
- [x] G-6b **経路差からのモードの積み上げと吸音の効果**（2026-08-15）。`plots.mode_buildup()`。
      詳細は下の「経路差から見たモード（G-6b）」節
- [ ] G-7 GUI の設計（フレームワーク未定。上記のパラメータを渡せる形にする）
      ※ Web ベースなら `view_model.py`、デスクトップなら `view_model_gui.py` が土台になる。
        デスクトップでスライダやグラフを同居させるなら `pyvistaqt` + `PySide6` の追加導入が必要
- [x] G-0 **モデルビューア**（2026-08-14）。三角形要素・法線の向き・レイヤ別色分けを表示し、
      回転／拡大縮小／平行移動ができる。**法線の裏側を赤で塗る**ので向きの誤りが一目で分かる。
      実装方式の違う 2 本を**並存**させている。
      - `geosim/view_model.py` … 自己完結 HTML（WebGL）を書き出してブラウザで開く。
        依存ライブラリなし。**相手に環境構築を求めずに共有できる**のが強み
      - `geosim/view_model_gui.py` … PyVista のネイティブウィンドウ。
        **Python から直接操作できる**ので G-1 / G-2 の重ね描きはこちらが早い
      ```
      cd geosim
      python view_model.py     ..\test2.dxf --absorption ..\absorption.csv
      python view_model_gui.py ..\test2.dxf --absorption ..\absorption.csv
      python view_model_gui.py ..\test.dxf  --screenshot shot.png   # 画像だけ書き出す
      ```
- [ ] G-9 **GUI で材料を編集できるようにする**（ユーザー要望）。土台は 2026-08-14 に用意済み。
      - `absorption.MaterialLibrary` … 材料の追加 `add()` / 変更 `update()` / 削除 `remove()`、
        CSV・JSON の読み書き。**Excel に無い材料を GUI から足せる**
      - `absorption.LayerAssignment` … レイヤ → 材料の対応表。JSON で保存。
        **CAD を編集せずに材料を差し替えられる**のがねらい
      - 残響室法／垂直入射の切り替えも `kind` で持っている
      GUI 側に要るのは「材料一覧の表」「レイヤ一覧と材料の割り当て」
      「吸音率のグラフ」「種類の選択」「条件の保存/読込」あたり
- [ ] G-10 **GUI で温度・湿度を変えられるようにする**。
      `atmosphere.Atmosphere` にスライダを結ぶだけ。音速と空気吸収の両方が連動する
- [x] G-8 **法線の確認・修正ウィンドウ**（2026-08-15、`normal_editor.py`）。
      緑=内向き（OK）／赤=外向き（要反転）／灰=判定できない、で色分けして表示する。
      枠で囲んだ面を反転、数字キーでレイヤごと反転、`a` 自動 / `c` CAD に戻す / `x` 全反転。
      `s` で `normals.json` に保存し、次回以降 `read_model(flip_faces=...)` で適用される。
      **自動補正のあとに重ねる**ので、人が直したぶんが最後の決定になる

### 経路差から見たモード（G-6b、2026-08-15）

**きっかけ**：G-6 のスペクトルが滑らかで山が読み取りにくい、という話から
「エネルギー減衰をいったん忘れて、**経路差から決まるモードが何回現れたかを数えて
積み上げられないか**。そのうえで吸音と距離減衰を入れ、さらに**設計通りの吸音**と
**完全反射（距離減衰のみ）**を比べれば吸音の効果が目で分かるのでは」という提案。

**やっていること**（`plots.pulse_spectrum` / `plots.mode_buildup`）

パルス列を位相込みで足す。`H(f) = Σ a_n exp(-i 2π f t_n)`。
到来時刻の差がそのまま経路差なので、`exp` の中身は経路差ぶんの位相。
**`a_n = 1`（減衰なし）で計算すると `|H(f)|` は「その周波数で同位相に重なった
経路の本数」そのもの**になる。1 本なら 1、2 本重なれば 2。提案どおりの「積み上げ」。

*経路差から固有周波数が出る理由*：往復の経路差 Δ で戻ってきた音は、Δ が波長の
整数倍のとき元の音と強め合う。つまり `f = m·c/Δ`。直方体の x 方向の往復は
Δ = 2Lx なので `f = m·c/(2Lx)` となり、軸モード `f = (c/2)(m/Lx)` に一致する。
`room_modes()` の式そのもの。

重みを変えて 3 本描く。
- ①減衰なし `a = 1` … 室形状だけで決まる。吸音材を貼っても変わらない
- ②完全反射 `a = 1/d` … 距離減衰のみ
- ③設計の吸音 `a = √E/d` … `E` は面の吸音だけが入ったパルスのエネルギー

②と③の差が**吸音の効果**。パルスのエネルギーには距離減衰も空気吸収も入って
いない（`impulse.py` 側で掛ける約束）ので、この 3 本はきれいに分離できる。

**処理は増えない**。素直に書くと「パルス本数 × 周波数点数」の掛け算になって重いが、
到来時刻を細かい格子に**投げ込んで（ヒストグラムにして）から FFT** すれば O(n log n)。
要はインパルス応答を作るのと同じ手順。実測で 3 本ぶん描いて 1 秒弱（うち大半は
matplotlib の描画）。研修室 3901 本のパルスで確認。

**検証**（`tests/test_geosim.py` [20]）
- 平行 2 面（L=4 m、音源 x=1、受音 x=3）の軸モード 42.9 / 128.6 / 171.5 Hz を検出
- **2 次（85.8 Hz）は出ない**。`cos(nπx/L)` が音源・受音点の両方で 0 になる、
  つまり節に乗っているため。**モード形状まで再現できている**証拠
- 同時刻に k 本置けば値がちょうど k になる／半波長ずらせば打ち消し合う

**図の読み方の補助**
- 位相がばらばらなら和はランダムウォークで √N 程度にしかならない。
  **その線を引いてあり、超えている山だけが本当に強め合っている周波数**
- 0 Hz では全経路が同位相になり左端に巨大な山が立つので、**いちばん低い
  固有周波数から下は描かない**
- 周波数の刻みは **1 Hz**（`frequency_step`。ユーザー判断）。FFT の刻みは応答長で
  決まる（3 秒なら 0.333 Hz）ので細かすぎる。**間引かず二乗平均**でまとめ直す
  （減衰なしの①は櫛の歯が鋭く、飛ばすと山を跨いで見落とすため）。
  √N の目安が帯域幅に依らず保たれるので基準線はそのまま使える
- 吸音率はオクターブバンドしか無いので各周波数点には最も近いバンドの値を当てる。
  6 バンドだと 88 Hz より下は全部 125 Hz の値になる（低域を見るなら 8 バンドに）
- パルス列は受音球が拾えた経路の**標本**。本数の絶対値は音線数と受音球半径で変わる。
  読むべきは**山と谷の位置**と**2 本の線の差**

### G-11 保存済みの結果から図だけ描き直す（2026-08-15）

`run_project.redraw(project)` / `python run_project.py <フォルダ> --redraw`。

**音線追跡はやり直さない。** `pulses.csv` と `ir.csv` を読み、そこから先だけを
計算して `図/` を作り直す。研修室（パルス 3901 本）で数秒、図 9 枚。

きっかけ：`direction.png` に直接音の赤い矢印を足したのに、**既存プロジェクトを
開いても古い図のまま**だった（GUI の「前回の結果を見る」は文字の要約だけ出していた）。
図の描き方を直すたびに全部回し直すのは現実的でない。

- パルス列とインパルス応答は**そのまま使う**（再合成しない）ので前回と食い違わない
- 残響指標・明瞭度・統計残響式は**本番と同じ関数**で計算し直す。
  CSV の読み方をここに別途書かずに済む（保存済み CSV と 1e-12 s 以内で一致を確認）
- **結果 CSV は書き換えない**（テストで確認）
- GUI の「前回の結果を見る」も内部でこれを呼ぶ

### G-12 / G-13 画面の保存とタイトルの文字化け（2026-08-17）

**G-12 いまの画面をそのまま保存する。** `g` で画像、`b`（音粒子のとき）で動画。
角度も、絞った本数も、不透明度も、見えているままが残る。
置き場は **`図/画面/`**（`図/` 直下だと `clear_results()` に巻き添えで消される。
テストで確認済み）。`音粒子_0123ms_01.png` のように**止めた時刻**も名前に入る。
動画は GIF（依存を増やさないため。幅 720 px・128 色・コマ数 400 で頭打ち）。

**G-13 ウィンドウのタイトルが化ける。** VTK のウィンドウは Windows では
ANSI ウィンドウで、日本語が**文字によって**化ける。W 系 API でも CP932 でも直らない
（かえって全部 `?` になる）ことを確認した。設定後に**読み戻して検証**し、
駄目なら英字の題に落とす。

**タイトルバーには画面の種類だけを出す。物件名は入れない**（ユーザー判断）。
題は `view_model_gui.WINDOW_TITLES` に集約：
`法線の確認` / `音線・音粒子` / `音線の飛び方` / `モデルビューア`、
英字はそれぞれ `geosim - normals` / `- rays and particles` /
`- ray directions` / `- model viewer`。
画面の中の見出し（物件名入り）とは別物で、`make_plotter(title, screen=...)` で分ける。

★踏んだ落とし穴 2 つ：
  - **描画イベントの中でタイトルを設定すると落ちる。** `SetWindowName` が
    `WM_SETTEXT` を同期で送るため描画中に再入し、OpenGL のコンテキストごと死んだ
    （segfault、しばらくそのマシンで新しいウィンドウが開けなくなった）。
    `show()` の直前に呼ぶ形（`finish_window`）に直した
  - **決めた題は `plotter.title` にも書き戻す。** pyvista の `show()` が
    `self.title` を貼り直すので、書き戻さないと日本語（化ける方）に戻る

実機で確認済み：4 画面ともタイトルが英字に差し替わること、`g` の画像が
ウィンドウと同じ寸法で出ること、`b` の GIF が書けて再生位置が戻ること。

### G-14 表の並べ方 ― 周波数は横（2026-08-17）

**「グラフにしたときの横軸が周波数なので、表形式も横方向周波数に統一してください。
これからも何か作る時は，基本方針は変わらないので，共通ルールとしてください。
何かの都合で縦になる場合は，確認してください。」**（ユーザー判断）

`geosim/table.py` に共通ルールとして置き、CLAUDE.md にも書いた。

    ○ 項目,125,250,500,1000,2000,4000
      EDT_s,0.945,0.571,0.459,0.295,0.267,0.284

    × frequency_hz,EDT_s,T20_s
      125,0.945,0.633

- 1 行が指標なら `write_frequency_table()`（`rt` / `clarity` / `まとめ_*`）
- 1 行が周波数以外（時刻・材料・経路）なら列名を `band_column()` で作る。
  **番号（`energy_1`）にしない**。バンド数 6 と 8 で意味が変わって読み違えるため。
  `pulses.csv` の `energy_1..6` を `energy_125Hz..` に直した
- 読むのは `read_frequency_table()`。**古い縦向きの CSV もそのまま読める**
- **画面に出す表も同じ向き**にした（`procedure` / `app` / `reverberation`）

### E-17 統計残響式を 3 つに（2026-08-17）

**Sabine / Eyring / Eyring-Knudsen**（ユーザー判断）。

- アイリングの式は `-S ln(1-ᾱ)` まで。**空気吸収 `4mV` を足した形はヌードセンの寄与**。
  2 つを別の列にしたので、**差がそのまま空気吸収の効き**になる
- **Sabine と Eyring には空気吸収を入れない**。素の形にしないと差の意味が濁る
- **ミリントンは削除**。面ごとに対数を取るので α→1 の面が 1 枚でもあると T→0 になり
  （開口が 1 つで残響ゼロ）、実務では使われない

### G-15 数値入力で画面が落ちた（2026-08-17）

**「条件設定を数値で指定したら、画面が落ちて、元のプロジェクト選択画面等に戻った」**

原因：**`e` は VTK の終了キー**（`q` と同じ）。そこに数値入力を割り当てていたので、
押すと入力ダイアログが出ると同時に **`ExitEvent` も飛んでいた**。
`run_animation` はそれを見てループを抜けるので、値を入れて OK を押した瞬間に
ウィンドウが閉じ、条件入力の画面に戻る。**例外は出ないのでログにも残らなかった。**

直し方：数値入力を **`t`** に変更（`view_model_gui.VALUE_INPUT_KEY`）。
あわせて `VTK_RESERVED_KEYS` を明文化し、予約キーを渡したら警告を出すようにした。
テスト [22] で「割り当てているキーが予約キーでないこと」を押さえてある。

★ 最初は tkinter と VTK のイベントループの入れ子を疑って 2 通り再現を試したが、
  どちらも落ちなかった。**`InvokeEvent('CharEvent')` でキーを総当たり**して
  ようやく `e` と `q` だけが `ExitEvent` を出すと分かった。
  「症状から犯人を推測する」より「全部試して観測する」ほうが早かった。

### G-16 注目したい音線だけを残す（2026-08-19）

**「音線・音粒子ともに、この経路もっとみたい、この辺りの粒子もっとみたい、
この方向に飛ぶ音線・粒子に注目したい。そんな機能を追加できないですかね？」**

`geosim/ray_filter.py` を新設（純粋な計算）＋ `view_rays.RayFocus`（画面側）。
3 つとも「表示する音線の添字を絞る」話なので、既存の
`RayDisplay` / `ParticleAnimation` にそのまま乗った。

操作は **`p` で点を拾う → `k` で種類を選ぶ → 範囲はスライダ**、`0` で解除。

| 種類 | 何が残るか |
|---|---|
| 近くを通る | 折れ線が基準点から半径以内を通った音線 |
| この方向に飛ぶ | 音源から見て基準点の向きへ出た音線（円錐） |
| 1 本だけ | いちばん近い 1 本（反射面の並びをコンソールに出す） |

踏んだ点・気をつけた点は PROGRAM_STRUCTURE.md の該当節にまとめてある
（出射方向で見る／描いている反射回数までで測る／線分まで測る／
トポロジを変えずに隠す／本数が戻るようにする）。

検証：立体角の期待値と一致（半角 5° で 4 本、15° で 34 本、30° で 131/134 本）。
実ウィンドウで 4 種類の切り替えと本数の追随を確認。テスト 240 件。

### G-16b 絞り込みをスライダで指定する（2026-08-19）

**「スライダーの方では無く、モデル上で `p` を押して、という操作が難しい。
どの方向へ、というのも平面方向と縦方向のスライダーを用意するなどで
指定出来た方がやり易い」**

- 方向を**「方位角 [°]」「仰角 [°]」スライダ**で指定できるようにした
  （約束は `project.head_azimuth` と同じ：0° = +X で反時計回り、仰角 0 が水平）
- 基準点は **`j` で 受音点 → 音源 → 拾った点**。**既定は受音点**なのでクリック不要
- 方向は**音源から伸びる黄色い矢印**で描く（何を指定しているか見えるように）
- `p` は任意。使えば基準点になり、方向モードならスライダの値もその向きに合わせる

**パネルに入りきらなくなった**（スライダ 9 本、レイヤ 8 つで 458 px はみ出し）。
スクロールは試したが**VTK のウィジェットが行ずれする**ので取り下げ、
載せる内容を削って 268 px に減らし、入りきらないぶんはコンソールに出す形にした。
詳細は PROGRAM_STRUCTURE.md の該当節。

**実装時の原則**（新しい計算段階を書くたびに確認する）

1. 時刻を保持する（G-2 の動画も G-5 の時間区切りも時刻がないと作れない）
2. バンド別のまま保持する（合算は最後の描画段階で）
3. 受音しなかったデータが要るかを考える（G-1・G-2 は要る、それ以外は不要）
4. 間引き・保存は本線の速度に影響しない副チャンネルにする
5. GUI から渡せるパラメータにする（ハードコードしない）

---

## F. その他

- [x] F-1 **高速化** → **2026-08-14 完了（NumPy ベクトル化）**。
      `mesh_method.FaceArrays` に面をまとめ、**音線の束 × 全面**の交差判定を
      1 回の配列演算で行う形にした。`loop_reflectionmesh` は音線ごとの Python ループを廃止し、
      反射回数ループの中で生きている音線を配列でまとめて進める。
      `receiver_sphere.inside_sphere_batch` / `loop_noredundancy` も同じ仕組みを使う。
      - **結果は scalar 版とビット単位で一致**（面 ID・距離・受音判定すべて。テストで確認）
      - 最寄り面探索の単体で **約 250 倍**
      - 処理の順序（受音判定 → 打ち切り → 壁 ID 追記 → 反射）は元コードのまま
      ※ scalar 版（`collision_distance` など）は**消していない**。読みやすい参照実装として、
        また一致確認の基準として残してある
- [ ] F-2 **空間分割（BVH / 一様格子）の検討**。
      現状は「全音線 × 全面」を総当たりするので、面数に比例して重くなる。
      面が数千枚を超える実モデルでは効いてくる見込み。
      ベクトル化と両立させるなら、格子で候補面を絞ってから配列演算する形になる
- [ ] F-3 **numba / マルチプロセスの検討**。
      ベクトル化で足りない場合の次の手。ただし依存が増えるので必要になってから
