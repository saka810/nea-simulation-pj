# 不具合報告 2026-09-06（WIN240377 / Claude）

実際に計算を回して見つかったもの。**この端末ではコードを直さず報告だけ**にしてある
（修正案が要るようなら `fix/WIN240377_...` のブランチで出します）。

対象コミット：`6c2a03c`（2026-09-06 時点の `main`）
環境：Windows 11 / Python 3.10.11 / `requirements-lock.txt`／`tests\test_geosim.py` 829 件 OK

---

## ① `inverse_square.py` が「計算済みなのに結果を見つけられない」

**症状**

既定の手順（条件入力の「条件（シート）」を選ばずに計算 → `inverse_square.py`）で、
計算は正常に終わっているのに次のように出て、**何も書き出されない**。

```
[逆二乗] 条件『現状』
[逆二乗] 音圧レベルのまとめが見つかりません（先に計算してください）
[逆二乗] 0 条件ぶん書き出しました
```

「先に計算してください」と言われるが、`結果\<室>_まとめ_音圧レベル.csv` は実在する。

**再現手順**

1. `project.json` の `condition_sheet` が空のまま計算する
   （★これは**異常な状態ではなく GUI の既定**。`setup_window._refresh_sheets`
   がコンボボックスの先頭に空欄を置き、選ばなければ空のままにする。
   コメントにも「選ばなければ結果ファイル名に条件名を付けない」と意図が書いてある）
2. プロジェクト直下に `条件表.xlsx` がある（計算時に無ければ自動で作られるので、
   ふつうは必ずある）
3. `python inverse_square.py "<プロジェクト>"` を実行する

`test.dxf` で実際に踏んだ。

**原因（見立て）**

書く側と読む側で、ファイル名に付く**条件名の有無が食い違う**。

- **書く側**：`run_project` は `project.condition_sheet`（空）をそのまま使うので、
  `condition_label` が空 → `test_まとめ_音圧レベル.csv`
- **読む側**：`inverse_square.main()` が条件表のシートを全部数え上げ、
  `project.condition_sheet = "現状"` を**自分で立てる**
  （`geosim/inverse_square.py:484-491`）→ `test_現状_まとめ_音圧レベル.csv` を探す

このリポジトリには本来この食い違いを吸収する仕組みがある。
`Project.name_candidates()` が ①室名＋条件名 → ②室名だけ → ③頭なし の順に探し、
コメントにも「★②があるのが大事。条件表を作った瞬間に条件名が付くので、
それ以前の結果が読めなくなってしまう」と書いてある。

ところが `read_levels()` だけ、この仕組みを通さず `prefixed()` で 1 通りだけ組んでいる。

```python
# geosim/inverse_square.py:107-114
def read_levels(project):
    """`まとめ_音圧レベル.csv` から (周波数, {受音点名: Lp[バンド]})。"""
    import summary as sm

    path = os.path.join(project.folder, pj.RESULT_DIR,
                        project.prefixed(sm.LEVEL_FILE))   # ← ここ
    if not os.path.exists(path):
        return None, {}
```

すぐ上の `read_points()`（`geosim/inverse_square.py:75-79`）は
`project.existing_result_path("points")` を使っていて、**同じファイルの中で流儀が割れている**。

**確認**

`project.json` の `condition_sheet` を `"現状"` にして計算し直すと、
まとめ表が `test_現状_まとめ_音圧レベル.csv` になり、`inverse_square.py` が通る。
→ 診断のとおり、**名前の食い違いだけが原因**。

```
[逆二乗] 表を書き出しました: …\結果\test_現状_逆二乗.csv
[逆二乗] 1 条件ぶん書き出しました
```

**修正案（未適用）**

`read_levels()` を `name_candidates()` 経由にすれば、他の読み口と揃う。

```python
for name in project.name_candidates(sm.LEVEL_FILE):
    path = os.path.join(project.folder, pj.RESULT_DIR, name)
    if os.path.exists(path):
        break
else:
    return None, {}
```

**当座の回避**

条件入力の「条件（シート）」で**シートを明示的に選んでから**計算する。
あるいは `python inverse_square.py "<フォルダ>" --sheet ""` … は不可
（`--sheet` を省略すると全シートを数え上げる作りなので、「条件名なし」を指定する道が無い）。

**影響の範囲**

- `report_inverse_square.py` は `inverse_square.run()` を呼ぶので**同じ症状**になるはず（未確認）。
- 逆二乗の評価は半無響室・無響室の確認に使う「別ルート」なので、通常の
  残響時間・音圧レベルの計算結果そのものには影響しない。

---

## ② 条件名の付け方が、ツールによって食い違う（軽微・要判断）

①と同じ根の話。`condition_sheet` が空のとき、**同じプロジェクトの同じ条件**なのに
出てくるファイル名が割れる。

| ツール | 条件シートの扱い | 出るファイル名 |
|---|---|---|
| `run_project.py` | `project.condition_sheet` のまま（空） | `test_まとめ_音圧レベル.csv` |
| `mode_shape.py` | 同上（空） | `test_音圧分布_01_….csv` |
| `frequency_response.py` | **全シートを数え上げて立てる** | `test_現状_伝達関数.csv` |
| `inverse_square.py` | **全シートを数え上げて立てる** | `test_現状_逆二乗.csv` |

実際に 1 つの `結果\` フォルダの中で、`test_…` と `test_現状_…` が混在した。
どちらに揃えるかは方針の話なので、**判断をお願いします**（勝手に寄せません）。

- 案 A … 数え上げる側（`frequency_response` / `inverse_square`）を
  「プロジェクトが条件シートを持っていなければ立てない」に揃える
- 案 B … `run_project` 側で、条件シートが空でも先頭のシート名を条件名として採用する
  （※ただし `Project.condition_label` のコメントにある
  「既定名は条件を分けていないということなので空にする」という設計判断と衝突する）

---

## ③ 一括実行のあと `project.json` の条件シートが最後の条件に書き換わる（軽微）

**症状**

「全条件を一括 ▶▶」（`run_project.py --conditions`）で回すと、
条件入力で選んでいたシート（例：`現状`）に関わらず、
`project.json` の `condition_sheet` が**最後に回した条件**（例：`対策案`）になる。
次に画面を開くと、選んでいたつもりの条件と違うものが選ばれている。

**原因（見立て）**

`run_conditions()` は条件ごとに `sub`（`Project` の複製）を作るが、
`folder` は同じなので、`run(sub)` の中の
`project.save()`（`geosim/run_project.py:34`）が
**その条件の設定で `project.json` を上書きする**。ループの最後の条件が残る。

```python
# geosim/run_project.py:219-222（条件ごとの複製。folder は同じ）
sub = pj.Project(project.folder, **{k: getattr(project, k) for k in pj.DEFAULTS})
sub.condition_csv = file_name
sub.condition_sheet = sheet or ""
```

**影響**

計算結果そのものは正しい（条件ごとに正しい名前で出る）。
気づかずに次の単発計算をすると、意図と違う条件で回ることになる。
`mode_shape.py` / `frequency_response.py` / `inverse_square.py` も
`condition_sheet` を自分で立てるので（②参照）、同じ道筋で書き換わりうる。

**当座の回避**

一括実行のあとに単発で回すときは、条件入力の「条件（シート）」を選び直す。

**★①②の修正（`fdc57fe`）を取り込んだあとも再現します**（2026-09-06 夕に確認）。

`_freeze_condition_name()` は「`project.json` には書き戻さない」ようになっていますが、
一括実行の経路は**そこを通りません**。`run_conditions()` が
`sub.condition_sheet = sheet`（`run_project.py:222`）で先に立ててから `run(sub)` を呼び、
`run()` の中の `project.save()`（`run_project.py:82` と `:743`）が
そのまま `project.json` へ書き込みます。

実測（例題・2 条件）:

```
実行前: "condition_sheet": ""
  [run] 条件 1/2: 現状   → 結果の頭 '五角形の部屋_現状'
  [run] 条件 2/2: 対策案 → 結果の頭 '五角形の部屋_対策案'
実行後: "condition_sheet": "対策案"
```

なお **① は修正で直っています**。条件シートが空のままでも
`inverse_square.py` が結果を見つけ、「2 条件ぶん書き出しました」と通りました。
テストも 844 / 844 件 OK です。

---

## ④ README のコマンドが 1 か所欠けている（誤記）

`README.md` の「資料を PDF で読む」節。

```
.venv\Scripts\python docsuild_pdf.py
```

正しくは `docs\build_pdf.py`（`docs\b` の `\b` が消えている）。
`CLAUDE.md` の該当箇所も同じ誤記になっている。

→ **`docs\使い方マニュアル.md` には正しい形で書いた**が、README と CLAUDE.md 側は
触っていない（コード・共有ルールの改変にあたるため）。直してよければ直します。

---

## ⑤ `dxf_faces.py` が画層名を壊す（**実案件で踏んだ／実害あり**）

**症状**

REGION の DXF を `dxf_faces.py` で面に直すと、**日本語の画層名が全部文字化けする**。

```
変換後:  ��_1F     �V��{�[�h    ���˔�    �J��_���C
本来:    床_1F     天井ボード    反射板    開口_研修
```

**画層名は吸音材の割り当てに使う**ので、これが崩れると条件表が読めない。
`dxf_faces.py` の決めごとに「★レイヤ名は保つ（吸音材の割り当てに使うので、
これが崩れると意味が無い）」と書いてあるが、日本語では保てていない。

**再現手順**

実案件のモデルで踏んだ。

```
260908_階段教室.dxf（REGION 195 個 / 3DSOLID 7 個・画層 35 種）
python dxf_faces.py "…\260908_階段教室.dxf"
→ 773 三角形 / 画層 20 種。うち日本語の 19 種すべてが文字化け
```

**原因**

`dxf_faces.py:139` が accoreconsole の出力を **UTF-8** として読んでいる。

```python
# geosim/dxf_faces.py:137-139
def _read_dump(path):
    """AutoCAD が書いた辺の一覧を読む。→ (グループ→辺, 落ちたもの)"""
    groups, dropped = {}, []
    with io.open(path, encoding="utf-8", errors="replace") as handle:   # ← ここ
```

accoreconsole は**システムのコードページ（日本語 Windows なら CP932）**で
書き出すので、日本語のバイト列が `errors="replace"` で U+FFFD に置き換わり、
そのまま `_write_dxf`（`:237`、UTF-8 で書く）に渡って**永久に失われる**。

★元の DXF 自体は UTF-8（ヘッダは `$DWGCODEPAGE ANSI_932`）で、
**読み込み側（`read_dxffile`）は正しく日本語を読めている**。
壊れるのは accoreconsole の出力を経由するここだけ。

**★★もっと悪いこと：画層が統合される**

化け方が同じになる画層は**1 つにまとめられてしまい、名前だけでは分離できない**。

| 統合された組 | 面積 |
|---|---|
| `PHP_階段裏` ＋ `PHP_階段下` | 合計 60.08 m² |
| `開口_2F小` ＋ `開口_2F大` | 合計 80.24 m² |

この 4 画層に別々の吸音材を割り当てることは**できない**。

**修正案（未適用）**

CP932 で読んでみて、駄目なら UTF-8 に落とす（逆順でもよいが、
`errors="replace"` は**最後の手段にする**。黙って壊れるのを避けるため）。

```python
def _read_dump_text(path):
    raw = io.open(path, "rb").read()
    for encoding in ("cp932", "utf-8"):     # accoreconsole はシステムのコードページ
        try:
            return raw.decode(encoding)
        except UnicodeDecodeError:
            continue
    return raw.decode("utf-8", errors="replace")
```

`locale.getpreferredencoding(False)` を先に試す形でもよいと思います。

**当座の回避（この端末で実施済み）**

化け方は決定的（`名前.encode('cp932').decode('utf-8','replace')`）なので、
元の DXF の画層名から逆引きして書き戻せる。階段教室では **17 / 19 件**を復元し、
統合された 2 件は `PHP_階段裏＋階段下` / `開口_2F小＋2F大` という名前にした。
復元スクリプトが要るなら出します（`fix/WIN240377_dxf_faces_の文字コード` で
本体の修正案ごと出すこともできます）。

---

## ⑥ 「条件表を作成」が、選択したプロジェクトフォルダに作られない（**実害あり**）

**症状**

条件入力の「条件表（xlsx）」の欄に**別のフォルダのファイル**が入っていると、
「条件表を作成」を押しても**選択したプロジェクトフォルダには何も作られず、
その別フォルダのファイルが書き換わる**。

**再現手順**（`ct.create()` を直接呼んで確認）

```
① 条件表の欄が空                    → プロジェクト直下に作られる（正しい）
② 欄に前の案件の条件表が残っている  → ★そちらが更新され、
                                       新しいプロジェクトの中身は ['project.json'] のみ
```

**原因**

`condition_table.create()` が `path(project)` ＝ `Project.condition_path` を使う。
`condition_path` は**欄の指定があればそれを返す**ので、別フォルダを指していれば
そこが対象になる。既にファイルがあるので `update()` に回る。

```python
# geosim/condition_table.py:549-555
file_name = path(project)
if not is_book(file_name):
    file_name = os.path.join(os.path.dirname(file_name) or project.folder,
                             CONDITION_BOOK)
if os.path.exists(file_name):
    return update(project, model, library, verbose=verbose)
```

**なぜ実害か**

`update()` は材料番号と安全率は守るが、**面数・面積・参考の式は書き換える**。
つまり**別案件の条件表の面数・面積が、いま開いているモデルの値に置き換わる**。
気づかないまま他案件の入力ファイルが変わってしまう。

**ユーザーの要望（2026-09-06）**

> 条件表の作成は、選択したプロジェクトフォルダ内にしてほしい

**修正案（未適用）**

`create()` は**プロジェクト直下に固定**するのが素直だと思います
（「参照…」で選んだ既存の表を使うのは「読む」ときの話で、「作成」は別）。
あるいは、プロジェクトフォルダを選び直したときに欄をクリアする。

---

## ⑦ 要望：レイヤの全選択・全解除（面の確認画面）

**ユーザーの要望（2026-09-06）**

> レイヤー全選択全解除ほしい

**背景（実案件の数字）**

階段教室のモデルは**画層が 20 種**ある。左パネルのチェックは 1 つずつしか
切り替えられないので、「1 つだけ見る」「全部戻す」に 20 回のクリックが要る。

**あわせて気づいた制限**

- **数字キーでレイヤを選べるのは 1〜9 まで**（`face_editor.py:847-849` の
  `for k in range(9)`）。20 画層のモデルでは**10 個目以降が選べない**。
  画面のレイヤ一覧も、10 個目以降は番号が付かない
- `m`（表示 ON/OFF）はモデル全体を切るもので、**チェックの状態とは別管理**。
  「全解除」の代わりには使いづらい

**あると良さそうなもの**（判断はお任せします）

- 「全表示」「全非表示」のボタン（`ControlPanel.button` があるので置けるはず）
- 「選んでいるレイヤだけ表示」（1 つだけ見る使い方が多いので）

---

## 追記（2026-09-06 夕）

③ は、GUI 付きマニュアルを作るために例題（`docs\例題\五角形の部屋.dxf`、2 条件）を
一括実行したときに実際に踏んだもの。①②③とも**同じ根**（`condition_sheet` を
誰が決めるか／結果ファイル名の頭をどう合わせるか）から出ているので、
直すなら 3 件まとめて見るのがよいと思います。

⑤⑥⑦ は、実案件（`260908_階段教室.dxf`。REGION の DXF・画層 35 種）を
読ませたときに出たもの。**⑤と⑥はどちらも「他のファイルを壊す」向きの不具合**なので、
優先度は高いと思います。

---

## ⑧ 要望：`dxf_faces.py` が元の図面の POINT（src / rec）を引き継がない

**ユーザーの要望（2026-09-10）**

> POINT も引き継ぎたいですね

**症状**

`dxf_faces.py` は面（閉じた 3D ポリライン / 3DFACE）だけを書き出すので、
**元の図面にあった `POINT` が消える**。音源・受音点は `src` / `rec` 画層の POINT で
渡す決めなので、変換を通したモデルは**毎回、音源も受音点も無い状態から始まる**。

実案件で踏んだ。`260908_階段教室.dxf` には利用者が `Src` 画層に音源 2 点を
置いてくれていたが、変換後のファイルには残らず、`read_dxffile` は
「音源が DXF にありません」と言う。今回はこちらで座標を計算して書き戻した。

**原因**

`_write_dxf()`（`geosim/dxf_faces.py:235-`）が `polygons` しか書かないため。
読み取り側の `_read_dump()` も、AutoCAD に出させているのが**辺の一覧**なので
POINT はそもそも拾っていない。

**あるとよい形（判断はお任せします）**

元の DXF は**そのままテキストとして読める**（ACIS が読めないだけで、
`POINT` のグループコードは普通に取れる。実際にこちらの台本では拾えた）。
accoreconsole を通さず、**元の DXF から `POINT` を直接拾って書き足す**だけで済むと思います。

- 拾う対象は `src` / `rec` 系の画層に限らず、**POINT 全部**でもよいかもしれません
  （`read_dxffile` 側が画層名で選ぶので、余分に入っていても害はない）
- 画層名もそのまま保つ（`Src` / `Rec` の大文字小文字は `_layer_matches` が
  小文字化して見るので問題ない）
- ★同じ考えで、`$INSUNITS` は既に引き継いでいるので、そこに並べる形になると思います

**当座の回避（この端末で実施済み）**

変換後の DXF に `POINT` を書き足す台本を作って使っている。

### ★修正案をブランチで出しました（2026-09-11）

ユーザー指示により、修正案をブランチにしました。**merge はこちらで判断しません。**

```
ブランチ  fix/WIN240377_dxf_faces_のPOINT引き継ぎ
PR        https://github.com/saka810/nea-simulation-pj/pull/1
コミット  0810966
```

- `read_points()` を追加。**accoreconsole は通さず元の DXF から直接拾う**
  （読めないのは ACIS だけで、POINT のグループコードはテキストとして取れる）
- ★**モデル空間（`ENTITIES`）だけ**を見る。`BLOCKS` の中の POINT は
  ブロック定義の座標系なので、そのまま置くと位置が合わない
- ★**画層で絞らない**。`src` / `rec` の判定は `read_dxffile` の役目なので、
  ここで決め打ちすると命名の揺れ（`Src` / `音源`）に追随できなくなる
- `write_faces_dxf(points=…)` で書き戻し、画層テーブルにも足す
- `convert()` が引き継いだ点数を画層ごとに報告する

**検証**

- `tests/test_geosim.py` **891 / 891 件 OK**（[48] に ⑨ として 10 件追加）
- 実案件 `260908_階段教室.dxf` で通し確認

```
[面に分解] 点（POINT）を 2 個引き継ぎました（画層: Src 2）
[read_dxffile] 音源: [[-43.9583, -8.5056, 1.5], [-34.9583, -8.5056, 1.5]]
[面に分解] 読み込み確認: 三角形 779 枚 / レイヤ 22 種
```

三角形 779 枚・レイヤ 22 種は従来どおりで、**音源だけが復活**している。

`PROGRAM_STRUCTURE.md` と `docs/DXFデータの作り方.md`（＋PDF）も同じブランチで更新した。

**★ついでに気づいた点（このブランチでは直していない）**

`docs/DXFデータの作り方.md` の「穴のある面（外周だけを面にするので、開口が
塞がっているかもしれない）」という記述が、2026-09-09 の修正（キーホール法で
穴を開けたまま割る）以降**実態と合っていない**。1 ブランチ 1 件の約束なので
触っていないが、直したほうがよいと思います。

---

## ⑨ 音源が 2 点以上あっても**先頭の 1 点しか計算しない**（黙って捨てる。実害あり）

**症状**

`src` 画層に音源を 2 点置いたモデルで計算したところ、**2 点目が丸ごと無視された**。
しかも**警告が一切出ない**。読み込み時には 2 点とも表示されるので、
利用者からは「両方使われた」ようにしか見えない。

```
[read_dxffile] 音源: [[15.0, 1.0, 1.5], [6.0, 1.0, 1.5]]   ← 2 点あると表示される
（以降、2 点目に触れる出力は一切なし）
```

★実案件（JRE東北研修施設・階段教室）で、利用者が CAD 側に S1・S2 の 2 点を
作図したうえで計算を依頼した。**3 回（音線 10 万 / 50 万 / 100 万本、延べ 4 時間）
回したあとで、利用者が 3D 画面を見て「片方の音源からしか出ていない絵になっている？」
と気づいた**。気づかなければそのまま報告書になっていた。

**再現手順**

`src` 画層に POINT を 2 つ以上置いた DXF で `run_project.py <プロジェクト>`。

**原因**

パイプラインが**単一音源前提**になっている。

```python
# geosim/run_project.py:230-234
def _source_of(project, model):
    if project.source is not None:
        return np.asarray(project.source, dtype=float)
    if model.source_points:
        return np.asarray(model.source_points[0], dtype=float)   # ← 先頭だけ
    return None
```

```python
# geosim/procedure.py:42
def process(soundsource_point, receiver_point, dxf_filename, ...)   # ← 単数

# geosim/procedure.py:229-232
if soundsource_point is None:
    if not model.source_points:
        raise ValueError("音源が指定されておらず、DXF に src レイヤの POINT もありません")
    soundsource_point = model.source_points[0]                    # ← 先頭だけ
```

★**`loop_reflectionmesh.loop` は音源を複数取れる**（戻り値が `[音源][受音点]`。
`CLAUDE.md` にもそう書いてある）。**その口を `procedure` / `run_project` が
使っていない**だけ。

**確かめ方（残っている痕跡）**

- `結果\<室>_測定点.csv` に音源が 1 点しか載らない。
  列名まで `src1からの距離_m` になる
- `結果\<室>_raylog.npz` の軌跡の始点を数えると、**全部が 1 点目**
  （階段教室では 2000 本すべてが S1）

**影響**

- 受音点が音源の片側に寄っていると、**直接音の距離も残響への寄与も丸ごと変わる**。
  階段教室では S1・S2 が 9 m 離れており、rec2 までの距離は S1 から 9.29 m、
  S2 から 5.69 m。**どちらを使うかで結果が変わる**
- ISO 3382 は**音源位置ごとに測って平均する**ので、2 点あるなら 2 回ぶんの
  結果が要る。いまの作りではそれが得られない

**当座の回避**

`project.json` の `source` に座標を直接書けば音源を指定できるので、
**音源ごとにプロジェクトを分けて回し、あとで組み合わせる**。
（ISO 3382 の作法どおりなので、回避というより本来の形に近い）

**直し方の案（判断が要るので手を付けていません）**

1. **最低限**：音源が 2 点以上あったら**警告を出す**
   （「先頭の 1 点だけ使います。音源ごとに分けて回してください」）。
   これだけでも「黙って捨てる」状態は無くなる
2. **本筋**：受音点と同じように**音源もループする**。
   `loop_reflectionmesh.loop` は既に対応しているので、
   `procedure` / `run_project` を通すだけ。ただし
   **出力の置き場が `結果\srcM_recN\` のように変わる**ので、
   ファイル名の決め方（`Project.result_path`）とまとめ表の作り、
   経路キャッシュの指紋にも影響する。★**設計の判断が要るので開発者にお任せします**

★この端末はコードを改変しない約束なので報告のみ。
①のときのようにブランチで出すこともできますが、②の本筋は影響範囲が広いので、
まず方針を決めていただくほうがよいと思います。

---

## ⑩ 古い `project.json` の `source` が、音源 2 点対応を**黙って無効にする**

**症状**

⑨ の修正（`5c05164`）を取り込んだあと、音源 2 点のモデルで計算したのに
**また 1 点目だけの結果**になった。`結果/src1` `結果/src2` も作られない。
**警告は出ない。**

★実案件（階段教室）で、**5 時間の計算を丸ごと無駄にした**。
気づいたのは終わったあとに `結果/` の構成を見たときで、
計算中は単一音源で走っていることを知る手がかりが無い。

**原因**

音源の決め方が **`project.json` の `source` を最優先**する。

```python
# geosim/run_project.py:108-124
def _sources(project, model=None):
    if project.source is not None:
        return [np.asarray(project.source, dtype=float)]   # ← 1 点返して終わり
    ...
    return [np.asarray(p, dtype=float)
            for p in (getattr(model, "source_points", None) or [])]
```

その `source` は**計算のたびに書き戻される**。

```python
# geosim/run_project.py:401-405
# 設定を 1 回だけ残す。★条件は**呼ばれたときのまま**（…）
#   音源だけは DXF から取った値を書き戻しておく
if results and results[0].get("soundsource_point") is not None:
    project.source = np.asarray(results[0]["soundsource_point"]).tolist()
project.save()
```

つまり **1 度でも計算したプロジェクトは、その後ずっと音源 1 点に固定される**。
⑨ より前に回したプロジェクトは全部この状態なので、**修正を入れても直らない**。

★皮肉なことに、`_run_sources()` の説明文には対策が書いてある。

> ★**`project.json` に音源を書き戻さない**（`save_settings=False`）。
> 書き戻すと `source` が 1 点に固定され、**次回から 1 点目しか回らなくなる**

受音点は同じ理由で書き戻しを止めてある（`write_back=False`）のに、
**音源は外側の経路（`:404`）に書き戻しが残っている**。

**再現手順**

1. 音源が 2 点ある DXF のプロジェクトを 1 回計算する（⑨ 以前のコードでも可）
2. `project.json` に `"source": [x, y, z]` が書かれる
3. ⑨ の修正を入れて計算する → **`結果/src1` `src2` が作られず、1 点目だけの結果**

**当座の回避**

`project.json` の `"source"` を `null` に戻す。
★**回す前に音源の数を確かめるのが確実**：

```python
import project as pj, run_project as rp
prj = pj.Project.load(r"<プロジェクト>")
print(len(rp._sources(prj)))     # 2 になっていること
```

**直し方の案**

1. **書き戻しをやめる**（受音点と同じ扱いにする）。`:403-404` を消すだけ。
   ただし**既存のプロジェクトは `source` が残ったままなので救済にならない**
2. ★**DXF の音源が `project.json` の `source` より多いときは知らせる**。
   「DXF に音源が 2 点ありますが、設定で 1 点に固定されています」と出せば、
   既に固定されてしまったプロジェクトも気づける。**こちらを併せて入れてほしい**

★この端末はコードを改変しない約束なので報告のみ。

**あわせて（軽微）**

単一音源で回したあとに音源 2 点で回し直すと、`結果/` に**古い `rec1`〜`rec5`
フォルダが残る**（新しい結果は `src1/rec1` 側に入る）。どちらが最新か分からないので、
`clear_results()` が構成の変わった古い棚も片付けるとよい。

---

## ⑪ 音源が 2 点以上あると「前回の結果を見る」が開けない（**実害あり**）

**症状**

階段教室（音源 2 点・100 万本）の計算が終わったあと、条件入力の
**「前回の結果を見る」を押すと「計算結果がありません」**と言われる。

> `C:\…\NEA_階段教室` にまだ計算結果がありません。
> 「計算する ▶」を先に実行してください。

結果は `結果/src1/rec1/` 以下にちゃんとある。条件入力画面の
**「計算結果あり」の表示も出ない**（同じ関数を見ているため）。

★**計算をやり直す以外に画面から結果を見る方法が無くなる**。
100 万本で 2.5 時間かかるので、知らずに押すと実害が大きい。

**原因**

`project.has_results()` が **`結果/` 直下と `結果/rec1/` しか見ていない**。

```python
# geosim/project.py:1109-1125
def has_results(project):
    saved = project.receiver_index
    try:
        for index in ((None, 1) if saved is None else (saved,)):
            project.receiver_index = index          # ← 受音点は回すが
            if any(os.path.exists(project.existing_result_path(key))
                   for key in ("rt", "pulses")):     # ← 棚（src1）は見ていない
                return True
```

⑨ の対応で、音源が 2 点以上あると結果は **`結果/srcM/recN/`** に入るように
なった。`project.source_folders()` という棚の一覧を返す関数もあるのに、
ここから呼ばれていない。**⑨ の直し漏れ**。

**再現（実機で確認）**

```
has_results(そのまま)   = False     ← ボタンが止まる
source_folders          = ['src1', 'src2', '合成_平均', …]
has_results(src1 の棚)  = True      ← 結果はある
```

**当座の回避**

棚を指して同じ画面を直接開く。

```python
import app, project as pj, source_mix as sx
prj = pj.Project.load(r"<プロジェクト>")
app._visualise(sx.tagged(prj, tag="src1"))     # src2 なども同様
```

**直し方の案 → ブランチで出しました**

`fix/WIN240377_source_shelf`（`09c0b22`）。★**棚を指している `project` は
その棚だけを見る**（指定を無視して他の棚で True を返さない）ようにしてあります。

---

## ⑫ 音源が 2 点以上あると虚音源のタブが出ない

**症状**

⑪ を回避して結果画面を開くと、**虚音源のタブだけが出ない**。
音線・音粒子は正常。端末にこう出る。

```
[虚音源] rec1 のパルス列がありません: …\結果\rec1\階段教室_現状_pulses.csv
                                         ↑ src1 が抜けている
[app] 虚音源は開けません（ValueError: パルス列（pulses.csv）が見つかりません。
                          先に計算してください）
```

**原因**

`view_images.load_sets()` が受音点ごとに `Project` を組み直すとき、
`pj.DEFAULTS` からしか値を写していない。

```python
# geosim/view_images.py:380-382
sub = pj.Project(project.folder,
                 **{key: getattr(project, key) for key in pj.DEFAULTS})
sub.receiver_index = k + 1
```

`source_tag` と `source_index` は「**保存する条件ではない**」ので意図的に
`DEFAULTS` に入っていない。そのため**組み直すと棚が落ちて** `結果/recN/` を
見にいく。`_visualise` 側は自分で `default_shelf()` を呼び直しているので、
音線・音粒子だけ無事だった。

★**同じ形が他にも残っています**（未検証）。

| 場所 | 見に行く先 |
|---|---|
| `inverse_square.read_levels()` :227 | 受音点ごとに組み直し。棚が落ちる |
| `summary` の条件比較 :367 | 同上 |

`source_mix.tagged()` を通せば棚ごと作れるので、そちらに寄せるのが安全だと思います。

**直し方の案 → ブランチで出しました**

⑪ と同じ `fix/WIN240377_source_shelf`（`09c0b22`）。根が同じ
「**棚を持ち回っていない**」なので 1 本にまとめました。
`PROGRAM_STRUCTURE.md` にも落とし穴として書き足してあります。

### 同型の対応（2026-09-16 / HBN240018）

「未検証」として挙げた 2 か所を直したところ、**同じ形が 3 か所**見つかったので
まとめて片付けた（`inverse_square.read_levels()` / `summary.write_condition_summary()` /
`frequency_response` の 3 か所 ＝ 残響時間の読み出し・受音点ごとのパルス列・`write_csv`）。
組み直しは **`source_mix.tagged()`** に通す形に揃えた。

★**棚を引き継ぐだけでは足りなかった。**`inverse_square` / `frequency_response` /
`report_inverse_square` は `Project.load()` から始まるので、**そもそも棚を
選んでいない**（＝棚が落ちる以前に、`結果/` 直下を見て「先に計算してください」で
止まる）。`main()` で `source_mix.default_shelf()` を通し、1 番目の音源を開いて
そう言うようにした（`app.py` と同じ作法）。

まだ残っているもの：`run_project.run_conditions()` の Excel 作り直し（:422）が
棚を選ばない。**条件の一括実行を 2 音源で回したとき**に出る。
`mode_shape` は結果を読まず計算し直すので、この形ではない。

検算：音源 2 点 × 受音点 2 点の棚を作り、5 か所すべてが `結果/src2/recN/` を
指すことと、**単一音源のプロジェクトが従来どおり `結果/recN/`** であることを確認。
`tests	est_geosim.py` … **946 / 946 件 OK**（935 → 946）

確認：階段教室（音源 2 点）で `rec1`〜`rec5` すべて読めるようになった
（rec1 で虚音源 6798 個）。`tests\test_geosim.py` は **925 / 925 件 OK**。

---

## ⑬ 条件表の「面数」が三角形の枚数で、モデルが細かすぎるように見える（表示）

**症状**

階段教室の `条件表.xlsx` の面数が、**他ソフトで数えた面数の 3 倍**になっている。
モデルを作った人から「やけに多い」と指摘を受けた。

| | 枚数 |
|---|---:|
| 他ソフトの面数 | **241** |
| 条件表の「面数」 | **779** |
| このプログラムが実際に 1 枚として扱う数（同一平面パッチ） | **223** |

**原因**

条件表に書いているのは `model.layer_counts` ＝ **三角形に割ったあとの枚数**。

```python
# geosim/condition_table.py:776-781
layer_counts = getattr(model, "layer_counts", {}) or {}   # ← 三角形の枚数
...
rows.append((SECTION_LAYER, layer, key, layer_counts.get(layer, 0),
             layer_areas.get(layer, 0.0)))
```

n 角形は必ず n−2 枚の三角形になるので、ここは**必ず増える**。
一方、交差判定が 1 枚として扱うのは `mesh_method.coplanar_patches()` の
**同一平面パッチ**で、そちらは他ソフトとほぼ一致する。

**数え直した結果（階段教室・22 画層）**

| レイヤ | 他ソフト | 三角形 | パッチ | 面積 m² |
|---|---:|---:|---:|---:|
| ガラス | 31 | 191 | 28 | 298.5 |
| 反射板 | 42 | 84 | **42** | 456.3 |
| ボード | 33 | 66 | 32 | 92.2 |
| GW | 22 | 63 | 18 | 647.3 |
| PHP_階段下 | 2 | 54 | 7 | 43.5 |
| 床_階段 | 24 | 48 | **24** | 188.2 |
| PHP_2F側壁 | 21 | 42 | 20 | 88.2 |
| 天井ボード | 8 | 39 | **8** | 174.2 |
| スチールドア | 18 | 38 | 14 | 42.4 |
| 防煙垂壁_2F | 10 | 35 | 7 | 192.3 |
| 床_1F | 2 | 31 | **2** | 418.7 |
| 床_2F | 1 | 22 | **1** | 125.3 |
| PHP_1F側壁 | 8 | 20 | 6 | 50.1 |
| PHP_後壁 | 6 | 12 | 1 | 23.8 |
| PHP_バルコニー | 5 | 10 | **5** | 55.1 |
| PHP_階段裏 | 1 | 10 | **1** | 16.6 |
| 開口 4 種・ガラス 2 種 | 各 1〜2 | 2〜4 | **一致** | — |
| **合計** | **241** | **779** | **223** | **3031.9** |

半数の画層はパッチ数が**ぴたりと一致**する。ずれている分は 2 種類。

- **こちらが少ない側** … 同一平面で隣り合う面をまとめた分。`PHP_後壁` は
  **6 面が 1 枚（23.84 m²）**になる。同じ平面・同じ材料の壁を 6 分割して
  描いてあるだけなので、**まとめるのが正しい**（三角形単位だと「同じ壁の
  隣の三角形に当たった」だけでバックトレースが経路を却下してしまう）
- **こちらが多い側**（`PHP_階段下` の +5）… 中身は
  **21.75 m² と 21.21 m² の 2 枚が実体**で、他ソフトの 2 面と一致する。
  残りは 0.18 m² × 3 と **0.00 m² × 2** の細片

**あわせて：潰れた三角形が出ている**

| | 枚数 |
|---|---:|
| 面積 < 1e-6 m²（実質 0） | 4 |
| 面積 < 0.01 m² | 47 |
| 最小角 < 1° | 113 |
| 最小角 < 5° | 251 |

最小角の中央値 10.9°・最悪 0.0°。出どころは**穴あき面のキーホール分割**
（穴と外周を橋でつなぐので橋の根元に潰れた三角形が出る）だと思われます。
`ガラス` に 27 三角形のパッチが 4 つあるのも同じ理由。

計算への影響は小さいはずです（交差判定はパッチの外周で行うので、パッチ内部の
細片は効かない。0 面積のものは独立したパッチになるが外周が潰れていて当たらない）。
ただし丸めに弱い形なので、`dxf_faces.py` 側で**面積が 0 の三角形を落とす**
余地はあると思います。

**直し方の案（コードは出していません）**

1. **見出しを「三角形数」にする**か、**「面数（パッチ）」の列を足す**。
   利用者が見たいのは後者のはず
2. 面の確認画面はすでに**パッチの外周**を描いているので、そこで数えた値を
   そのまま書けば一致する

★条件表の列構成は「**利用者が作った体裁を壊さない**」約束があり、
列を足すか見出しを変えるかは**書式の決めごと**なので、ここは提案のみとしました。
どちらにするか決まれば、その形でブランチを出します。

### 対応（2026-09-16 / HBN240018）→ **後者（列を足す）で入れた**

ユーザー判断「⑬は提案の通りにして」を受けて、**`面数（パッチ）` の列を足した**。
既存の `面数`（三角形の枚数）は**意味も値も変えていない**。

- 数えるのは `condition_table.layer_patch_counts()`。
  `mesh_method.coplanar_patches()` を**本番と同じ呼び方**（材料も渡す）で呼ぶので、
  表の値と計算が 1 枚として扱う数が必ず一致する
- ★**前からあるプロジェクトの条件表にも次の計算で列が増える**
  （`_ensure_columns` が空いている列に足す。材料番号・安全率・利用者が足した
  セルと列幅はそのまま）。作り直さなくてよい
- ★**同じパッチが 2 つのレイヤにまたがることがある**（同一平面で隣り合い材料も
  同じとき）ので、レイヤ別の合計は室全体のパッチ数より多くなりうる。
  レイヤごとに並べる表なので、これが素直な数え方と判断した
- ★**数えられなければ列は空のまま**（0 と書くと「面が無い」と誤読される）
- 昔の形（パッチ列が無い xlsx / CSV）も従来どおり読める
- 検算：`test.dxf`（2×3×1 m の箱）で 三角形 12 枚 → **パッチ 6 枚**
  （床 1・天井 1・壁 4）。レイヤ別も `{1:1, 2:4, 3:1}` で形と合う。
  `tests	est_geosim.py` … **935 / 935 件 OK**（925 → 935。⑬ のぶん 10 件）

残っている宿題（このときは手を付けていない）:

- **面積 0 の三角形が 4 枚・最小角 1° 未満が 113 枚**（キーホール分割の橋の根元）。
  `dxf_faces.py` 側で落とす余地がある
- 見出しを `面数` → `三角形数` に変える案は**採らなかった**（前からある表の
  見出しを書き換えることになり、「体裁を壊さない」約束に触れるため）

---

## ⑭ 条件を変えて回すと、**前の条件の図（PNG）が全部消える**（実害あり）

**症状**

条件を変えて計算し直すと、**前の条件で作った図が残らない**。
CSV は条件名でファイルが分かれているので残るのに、図だけが消える。

★**一括実行（`--conditions`）だと、最後の条件の図しか残らない**はず
（条件ごとに `_run_one` が走り、その頭で毎回消えるため）。

**原因**

`clear_results()` が、図フォルダの **PNG を名前も見ずに全部消している**。

```python
# geosim/project.py:668-673
figures = self.figure_dir()
if os.path.isdir(figures):
    for name in os.listdir(figures):
        if name.lower().endswith(".png"):     # ← 条件名を見ていない
            os.remove(os.path.join(figures, name))
```

すぐ上の CSV 側は `result_candidates(key)` を通していて、
**いまの条件の名前（＋昔の名前）だけ**を消している。図だけ扱いが違う。

★ファイル名の頭に条件名を付けるようにした時点（2026-08-21）で、
「古いファイルが混ざる」問題は**名前で解決している**。
図をまとめて消す必要はもう無いはず。

**実測（階段教室で踏んだ）**

条件0（＝従来の「現状」）の結果がある状態で条件1 を回したところ、

| | 回す前 | 回したあと |
|---|---:|---:|
| 条件0 の PNG（`図/src1/recN/` など） | 110 | **0** |
| 条件1 の PNG | 0 | 110 |

**110 枚が消えた。**回す前に `図/` を控えに退避しておいたので戻せた。

**救い**

`run_project.redraw()` が CSV から図を作り直せる（音線追跡はやり直さない）ので
**復旧はできる**。ただし消えたこと自体は知らされない。

**直し方の案 → ブランチで出しました（2026-09-18）**

`fix/WIN240377_figure_clear`（`5add9e7`）。
CSV 側（`name_candidates`）と同じ「対象室名＋条件名」の考え方で、
**いまの条件の図だけ**を消すようにしました（`Project.owns_figure`）。

| ファイル名（条件1 を回すとき） | どうするか |
|---|---|
| `階段教室_条件1_decay.png` | **消す**（いまの条件） |
| `階段教室_条件0_decay.png` | **残す**（別の条件） |
| `階段教室_points.png` | **残す**（条件に依らない図。毎回書き直される） |
| `decay.png` | **消す**（頭を付ける前の図） |

- ★`file_prefix` が空（対象室名も条件名も無い）のときは**従来どおり全部消す**
- ★**手で撮った画像（`図/画面/`）は頭が付かないので巻き添えのまま**。
  子フォルダに分けておく必要は変わらない（`PROGRAM_STRUCTURE.md` に書き足した）

確認：実案件と同じ 121 枚（条件0〜10）を並べて条件1 の掃除 →
**11 枚だけ消えて 110 枚が残る**。`tests\test_geosim.py` は
**1001 / 1001 件 OK**（996 → 1001。⑭ のテストを 5 件足した）。

---

## ⑮ 音源を合成するとき、表の見出しを数値として平均している（軽微・出力は正しい）

**症状**

音源が 2 点以上あるプロジェクトを回すと、毎回この警告が出る。

```
geosim/source_mix.py:353: RuntimeWarning: overflow encountered in power
  mean = 10.0 * np.log10(np.mean(np.power(10.0, np.array(values) / 10.0)))
```

**原因**

`_average_rows()` が**見出しの行も数値として平均**している。
見出しは周波数（`63, 125, …, 8000`）なので、エネルギー平均にすると

```
10^(8000/10) = 10^800  →  オーバーフローして inf
```

実際に見出しが壊れることを確認した。

```
入力ヘッダー : ['受音点', '項目', '音源距離_m', '63', …, '2000', '4000', '8000']
出力ヘッダー : ['受音点', '項目', '音源距離_m', '63', …, '2000', 'inf', 'inf']
```

**★出力は正しい。**呼び出し側が

```python
# geosim/source_mix.py:419-424
rows        = _average_rows(tables, energy=False)   # ← 見出しはこちらが使われる
energy_rows = _average_rows(tables, energy=True)
for r, use_energy in enumerate(flags):
    if use_energy:
        rows[r] = energy_rows[r]                     # ← dB の行だけ差し替え
```

と、**dB の行だけ**差し替えるため、壊れた見出しは捨てられる。
実際、`結果/合成_*/recN/*_spl.csv` に `inf` は 1 つも入っていない（確認済み）。

**それでも直したほうがよい理由**

1. 毎回警告が出るので、**本物の異常が埋もれる**
2. 捨てる行まで計算している（全行 × 2 回）
3. ★`_energy_columns()` の判定が変わって見出し行が dB 扱いになった瞬間、
   **黙って `inf` が書かれる**

**直し方の案 → ブランチで出しました（2026-09-18）**

`fix/WIN240377_mix_header`（`c943b0c`）。
`_average_rows(tables, energy=…)` が**行ごとの真偽のリスト**も取れるようにして、
丸ごと 2 通り作ってから差し替えるのをやめました。

```python
# 直す前：表を丸ごと 2 通り平均してから dB の行だけ差し替え（見出しも計算していた）
rows        = _average_rows(tables, energy=False)
energy_rows = _average_rows(tables, energy=True)
for r, use_energy in enumerate(flags):
    if use_energy:
        rows[r] = energy_rows[r]

# 直したあと：行ごとに平均のとり方を渡す（見出し行に触らない）
rows = _average_rows(tables, energy=_energy_columns(key, tables[0]))
```

確認：
- **実データ 220 表**（11 条件 × 5 受音点 × 4 種）で**直す前と 1 文字も変わらない**
- `-W error::RuntimeWarning` で走らせても止まらない（警告が消えた）
- `tests\test_geosim.py` … **1006 / 1006 件 OK**（1001 → 1006。⑮ のテストを 5 件追加）

★捨てる行を計算しなくなるので、高速化の提案 ③（合成に 25 秒）も少し軽くなります。

---

## 修正の確認（2026-09-10 / WIN240377）

`32a08d5`（WIN250046 の修正 `7622ed8` / `90ae4ad` を含む）を取り込んで実機確認した。
**報告した ③⑤⑥⑦ はすべて直っています。**

- `tests\test_geosim.py` … **881 / 881 件 OK**（844 → 881 に増えている）

### ③ 一括実行で `project.json` の条件シートが書き換わる → **直った**

例題（`docs\例題\五角形の部屋.dxf`・2 条件）で `--conditions` を実行。

```
実行前: "condition_sheet": "現状"
  [run] 条件 1/2: 現状   → 結果の頭 '五角形の部屋_現状'
  [run] 条件 2/2: 対策案 → 結果の頭 '五角形の部屋_対策案'
実行後: "condition_sheet": "現状"     ← 以前は "対策案" に化けていた
```

### ⑤ `dxf_faces.py` が画層名を壊す → **直った（統合も解消）**

実案件（`260908_階段教室.dxf`）を変換し直した。

```
[面に分解] 読み込み確認: 三角形 779 枚 / レイヤ 22 種
★文字化けが残っている画層: なし
```

22 画層すべて正しい日本語で出た。**統合されていた 2 組も分離**されている。

| 修正前 | 修正後 |
|---|---|
| `PHP_�K�i��`（1 画層・60.08 m²） | `PHP_階段下`（54 枚）と `PHP_階段裏`（10 枚）に分離 |
| `�J��_2F��`（1 画層・80.24 m²） | `開口_2F大`（4 枚）と `開口_2F小`（2 枚）に分離 |

あわせて**穴のある面の対応**（`90ae4ad`）も効いている。
面の元になったエンティティが `閉じたポリライン 240 ＋ 3DFACE 17` になり、
三角形が 773 → **779 枚**（`床_1F` が 25 → 31 枚）に増えた。

### ⑥ 条件表が選択したプロジェクトフォルダに作られない → **直った**

前と同じ試験（`ct.create()` を直接呼ぶ）を回した。

```
② 欄に前の案件の条件表が残っている
   ★作られた場所 : …\新しいプロジェクト\条件表.xlsx     ← 以前は別フォルダ側
     プロジェクト直下か: True
   別のフォルダの中身: ['project.json', '条件表.xlsx']   ← 触られていない
```

### ⑦ レイヤの全選択・全解除 → **入った**

面の確認画面の左パネルに次が追加されているのを確認した。

- **「レイヤを全表示」「レイヤを全非表示」「この番号のレイヤだけ表示」**
- **「レイヤ番号」のスピンボックス** ＋「この番号のレイヤを選択に足す」
- レイヤ一覧の**番号が 10 個目以降にも振られた**（階段教室では 1〜22）。
  数字キーが 1〜9 までという制限は、この欄で回避できるようになっている

階段教室（779 面 / 186 グループ / 22 画層）で、法線は `auto` が **375 面を反転**して
全 779 面が内向き（OK）になった。

### 小さな気づき（不具合というほどではないもの）

面の確認画面の見出しが **「— 面の確認（法線・吸音材）」**と、頭にダッシュだけが
残った形で出る。`face_editor.edit()` が `project.name`（対象室名の欄。空欄可）を
使っているため。`display_name`（空欄なら DXF 名）を使えば
「260908_階段教室_faces_修正後 — 面の確認…」になると思います。急ぎではありません。

---

## 参考：この端末で確認できた範囲（正常だったもの）

`test.dxf`（2×3×1 m・音線 5 万本・最大反射 50 回・受音球 0.5 m）で通し実行し、
以下は問題なく動いた。

- `run_project.py`（通し計算 → CSV・図 11 枚・Excel 8 シート）
- 経路の使い回し（2 回目は `結果\rec1\test_経路.npz` から復元）
- `summary.py` / `workbook.py` / `open_edges.py` / `section.py --create`
- `frequency_response.py`（20〜250 Hz）／`mode_shape.py`（`box` 法）
- `read_dxffile.py` / `dxf_faces.py`（`accoreconsole` は AutoCAD 2025 で検出済み）
- `tests\test_geosim.py` 829 / 829 件

3D 画面（`app.py` / `face_editor.py` / `view_rays.py`）は**未確認**（対話操作が要るため）。

---

## ⑯ DXF を作り直すたびに**音源・受音点の並びが変わる**（実害あり）

**症状**

形状をまったく変えず、**画層名を 1 つ変えただけ**の DXF を作り直したところ、
`read_dxffile` が返す音源・受音点の**並びが入れ替わった**。

| | 旧 260914 | 新 260918（画層名だけ変更） |
|---|---|---|
| src1 | S1 (15, 1, 1.5) | **S2 (6, 1, 1.5)** |
| src2 | S2 | **S1** |
| rec1〜5 | R1, R2, R3, R4, R5 | **R1, R2, R5, R3, R4** |

★**三角形は 779 枚で頂点の並び順まで完全一致**（`np.array_equal` で確認）。
変わったのは画層名だけなのに、`POINT` の出てくる順番が変わっている。

**何が起きるか**

1. ★**`結果/src1/` に S2 の結果が入る。**過去の条件と比較できなくなる
2. ★**`結果/rec1/` が R1 でなくなる**（A-3 で直したのと同じ症状の再発）
3. ★★**経路キャッシュが無効になる。**指紋の `source` が合わないため
   音線追跡からやり直しになる（実案件で **50 分回してから気づいて中止**。
   最後まで行けば 100 万本 × 音源 2 点で 2.5 時間）

```
[経路] 保存した経路は使えません: 音源の位置が違います
       （source: 保存 [15.0, 1.0, 1.5] / いま [6.0, 1.0, 1.5]）
```

**原因**

`read_dxffile` は DXF に**出てきた順**に番号を振る。`dxf_faces.py` が
`accoreconsole` で作り直すと `POINT` の並びが変わりうるので、
**同じモデルでも作り直すたびに番号が入れ替わる**。

**当座の回避（実案件でやったこと）**

`測定点順.json`（`point_order.py`）で並べ直す。**音源にも受音点にも使える**。

```python
po.save(project, sources=[1, 0], receivers=[0, 1, 3, 4, 2], model=model)
```

★これは既にある仕組みで、よく出来ている（点の数を控えて食い違えば使わない）。
**次に DXF を差し替えても点の数が同じなら自動で当たる**ので、いったん作れば安心。

**直し方の案**

1. ★**座標で照合して警告する。**`測定点順.json` が無いとき、前回の計算結果
   （`結果/<室>_測定点.csv`）と座標を突き合わせ、**並びが変わっていたら知らせる**。
   いまは黙って番号が振り直されるので、結果を見るまで気づけない
2. ★★**経路キャッシュの「合わない理由」に対処法を添える。**
   「音源の位置が違います」と出たとき、**保存側の座標がいまの別の音源と一致する**なら
   「音源の順番が入れ替わっているようです。`測定点順.json` で直せます」と言えるはず。
   50 分の無駄打ちはこれで防げた
3. `dxf_faces.py` が `POINT` を**座標順に並べて**書き出す（作り直しても順番が動かない）

★この端末はコードを改変しない約束なので報告のみ。

---

## ⑰ `decay_floor_db` がシミュレーションでは意味を成さない（誤読を招く）

**症状**

`rt.csv` の `decay_floor_db`（減衰の底）が **−347〜−375 dB** と出る。
ユーザーから「シミュレーションだとしても −100 もいくかな？」と指摘された。

**何のための値か**

★**「どこまで読めるか」を示すため**に置かれた値。`PROGRAM_STRUCTURE.md` にも
こう書いてある。

> ★**ISO 3382 の余裕は評価区間の下端 + 10 dB**（T30 なら −45 dB）。
> 実測では暗騒音の話だが、シミュレーションでは**反射回数の打ち切り**と
> **インパルス応答長の打ち切り**が同じ役目の床を作る。`decay_floor_db` で残す

つまり「打ち切りが作る床」を検出するのが狙い。

**なぜ意味を成さないか**

実装が**曲線の最小値を返すだけ**になっている。

```python
# geosim/reverberation.py:529-533
def _decay_floor_db(decay_db):
    """減衰曲線がどこまで下がったか [dB]（見えている範囲）。"""
    finite = np.asarray(decay_db, dtype=float)
    finite = finite[np.isfinite(finite)]
    return float(finite.min()) if len(finite) else np.nan
```

**実測なら暗騒音が床を作る**のでこれで正しい。しかし**シミュレーションには
暗騒音が無い**ので、シュレーダー積分 `E(t) = ∫ₜ^∞ p²dτ` は応答の終端で
ゼロに向かい、**float64 の精度限界（−350 dB 前後）まで落ちるだけ**になる。

**実測（階段教室・条件1・R1・63 Hz）**

| 到達 dB | −20 | −40 | −60 | −80 | −100 | −200 | −300 |
|---|---:|---:|---:|---:|---:|---:|---:|
| 時刻 [s] | 0.51 | 1.13 | 2.05 | 3.54 | **4.16** | **4.39** | **4.62** |

**−80 dB までは 3.5 秒かけて素直に落ちるのに、そこから 1 秒足らずで
−300 dB まで垂直に落ちる。**パルス列の最後の到来が **4.11 s**
（応答長 5.0 s）なので、それ以降は積分に残す音が無いだけ。

★**実質的に読めるのは −80 dB 程度**。−347 dB という報告は
「余裕たっぷり」という**誤った安心**を与える。

**直し方の案**

1. ★**曲線の傾きが急変する点を床とみなす**。打ち切りが効き始めると
   傾きが桁で変わるので、局所の傾きが評価区間の傾きの N 倍を超えた点を返す
2. あるいは**最後のパルスの到来時刻における曲線値**を返す
   （そこから先は「音が無い」ので読めない、と明示できる）
3. 少なくとも、**暗騒音を入れていない計算では「床は打ち切りで決まる」と
   但し書きを出す**（数値をそのまま出すと誤読される）

★この端末はコードを改変しない約束なので報告のみ。

**あわせて（この室で分かったこと）**

30 dB 幅の評価区間を −5〜−35 から −30〜−60 までずらして読むと、
残響時間は**単調に伸び続け**（500 Hz で 1.62 → 3.03 s）、
ξ も**単調に悪化し続ける**（9.9 → 60.4）。
★**「二段階減衰」ではなく「ずっと曲がり続けている」**のがこの室の実態で、
どこを 30 dB で切っても直線には乗らない。**ξ を併記する意味が大きい**。

## ⑱ 条件表の「安全率」が**計算に一切効かない**（実害あり・危険側・警告なし）

**症状**

条件表の `安全率` 列に値を入れても、**結果がまったく変わらない**。
警告も出ない。

階段教室で `条件13`（安全率なし）を複製して `条件15` を作り、
`GW ×0.7` / その他 `×0.8` / 開口はそのまま、と入れて回したところ——

| 突き合わせたもの | 条件13 | 条件15（安全率入り） |
|---|---|---|
| `pulses.csv` バンド別エネルギー（src1/rec3） | 911.7 / 442.4 / 366.2 / 244.4 … | **完全に同じ** |
| `吸音率と理論値.csv` の GW の α | 0.113 / 0.227 / 0.693 / 0.951 … | **完全に同じ** |
| 平均吸音率 | 0.149 / 0.233 / 0.315 … | **完全に同じ** |

**★1 ビットも違わない。**安全率は読めている（`condition_table.safety_factors()`
は `{'GW': 0.7, 'PHP_1F側壁': 0.8, …}` を正しく返す）のに、計算に届いていない。

**原因**

**同じ 1 回の実行の中で、モデルが 2 回・別々の作り方で読まれている。**

1. `run_project._model_for()` … `_absorption_table_for()` を通す。
   **ここは安全率が効く**（`condition_table.absorption_table(..., factors=…)`）。
   使い道は**経路キャッシュの指紋づくりと作図チェックだけ**
2. `procedure.process()` … `run_project.py:800` から
   **`material_library` と `layer_assignment` を渡される**ので、
   `procedure.py:210-224` で**自前に**吸音率表を組み立てて `read_model` する。

```python
# geosim/procedure.py:214-224
if material_library is not None:
    print(f"[procedure] {material_library.summary()}")
    absorption_table = material_library.absorption_table(layer_assignment,
                                                         ...)
...
model = rd.read_model(dxf_filename, unit=unit, absorption_table=absorption_table,
                      ...)
```

★**この経路に安全率が入る余地が無い。**そして**本計算で使われるのは 2 の方**。

**実測での裏取り**（`pc.reuse()` に渡る mesh の GW の垂直入射 α）

| どのモデルか | 63 | 125 | 250 | 500 |
|---|---:|---:|---:|---:|
| `_model_for()` が作るモデル（安全率あり） | 0.043 | 0.090 | 0.327 | **0.526** |
| **実際に計算に渡るモデル**（`procedure` が読んだもの） | 0.062 | 0.133 | 0.526 | **0.951** |

`run_project.py:972` には

> `# 吸音率の作り方は procedure.process() と同じ手順に揃える`

とコメントされているが、**揃えたつもりで揃っていない**（`--redraw` の側だけ
`_absorption_table_for()` を使っている）。

**なぜ実害が大きいか**

★安全率は「吸音を見過ぎないように間引く」**安全側**の係数。
効かないと**残響時間が短めに・STI が良めに出る＝危険側**へ倒れる。
しかも数字は一見まともなので、**出力を見ても気づけない**。
`PROGRAM_STRUCTURE.md` にも「安全率（例 0.8）はカタログ値に掛けてから
垂直入射へ変換する」と書いてあるので、**書いてあるとおりに使うと嘘の結果が出る**。

**直し方の案**

1. ★**`procedure.process()` に `absorption_table=` を渡せるようにし、
   `run_project` は `_absorption_table_for(project)` の結果をそのまま渡す。**
   いちばん素直で、2 つのモデルが 1 つになる
2. あるいは `procedure.process()` に `absorption_factors=` を足し、
   `procedure.py:216` のあとに掛ける（★掛ける順序は**カタログ値に掛けてから
   垂直入射へ変換**を守ること）
3. 最低限、**安全率が入っているのに使われない経路では警告を出す**

**★あわせて（派生してもう 1 つ）**

`condition_table.absorption_table()` は**安全率があるときだけ「レイヤ名をキーにした
エントリ」を足す**ので、`_model_for()` が作るモデルは
**`Mesh.material` がレイヤ名になる**（安全率が無いときは材料名）。

同一平面パッチは「同じ**材料**」でまとめるので、
★**安全率を足しただけでパッチの切れ目が変わりうる**。
経路キャッシュの指紋はこのモデルで作るため、
**吸音率の値しか変えていないのに経路が使い回せなくなる**恐れがある
（実案件では 2.5 時間の再追跡になる）。案 1 で直せばこの食い違いも消える。

**この端末で採った回避策**（参考）

安全率の列は使わず、**安全率を掛けた材料を「吸音率」シートに作って**
条件シートからその材料番号を指した（`GW 32K-50t(GC薄手) ×0.7` など 11 材料）。
library の経路を通るので確実に効く。★**安全率の列は空にしてある**
（不具合が直ったときに二重に掛からないように）。

★この端末はコードを改変しない約束なので報告のみ。


### 対応（2026-09-20 / WIN250046）→ **3 つとも直した**

ユーザー指示「⑯〜⑱対応して」。

#### ⑯ 測定点の並び → **気づける形にし、変換の出口も決め打ちにした**

報告の 3 案を**全部**入れた。

1. **前回と突き合わせて知らせる**（`point_order.check_against_previous`）。
   前回書いた `結果/<室>_測定点.csv` と座標を突き合わせ、**座標の集合は同じで
   順番だけ違う**ときに、何がどう入れ替わったかと戻し方を出す。
   `run_project._ordered` がモデルを読んだ直後に 1 回だけ呼ぶので、
   **回し始める前に気づける**
   - ★**位置そのものが変わっているときは何も言わない**（点を動かしたなら当然）
   - ★**`測定点順.json` は書き換えない**（知らせるだけ）。直すのは利用者の判断
   - ★戻す並びは**必ず「DXF に出てきた順の番号」**。すでに `測定点順.json` が
     あるときは**合成して**返す（当たっている分は挙げない）
2. **経路キャッシュの理由に対処法を添える**（`path_cache.compare(points=…)`）。
   「音源の位置が違います」で止まったとき、保存した位置が**いまの別の測定点**と
   一致するなら「★保存した位置はいまの音源の 2 番目です。順番が入れ替わって
   いるだけかもしれません」と出す。**50 分の無駄打ちはこれで防げる**
3. **変換の出口で並びを決め打ちにする**（`dxf_faces.sort_points`）。
   `POINT` を**画層 → 座標**の順に並べ直すので、CAD 側で `ENTITIES` の並びが
   変わっても**何度変換しても同じ番号**になる。並べ直したことは実行ログに出す

戻し方も用意した: `python point_order.py <プロジェクト> --match-previous`

#### ⑰ `decay_floor_db` → **崖の手前を返す**

`_decay_floor_db()` が**曲線の最小値**を返すのをやめ、**傾きが急変する点**を
探すようにした。基準は「0 → −20 dB まで落ちるのに要した平均の傾き」で、
その 5 倍を超えたところを崖の入口とみなす。

- ★**崖が無ければ従来どおり最小値**（解析的な直線の減衰では意味が変わらない）
- ★**基準の深さより手前は見ない**（立ち上がりの急降下を崖と取り違えないため）
- ★**「最後に緩やかだった点」ではなく「最初に急になった点」**を採る。
  崖の先は数値の底で**平らになる**（傾き 0）ので、後ろから探すと底を拾う
- 実測（報告と同じ形の曲線）：最小 −350 dB → 床 **−78 dB**。
  打ち切った応答のシュレーダー積分では 最小 −3000 dB → 床 **−96.7 dB**
- ★★**副産物**：`decay_curves` の「ISO 3382 の余裕（評価区間の下端 + 10 dB）まで
  見えているか」の見張りは `floor_db` を使うので、**最小値のままでは一度も
  出なかった**（−347 dB は常に −45 dB より下）。これで**見張りが効くようになった**

#### ⑱ 安全率 → **出来あいの表を本計算へ渡す**（報告の案 1）

`procedure.process()` に **`absorption_table=`** を足し、`run_project` が
`_absorption_table_for()`（＝安全率を掛けた表）を渡すようにした。
渡されたときは `material_library` から組み立て直さない。

- ★これで**同じ 1 回の実行の中でモデルが 2 通りの吸音率で読まれる**状態も消えた
  （指紋づくり・作図チェックと本計算が同じ表を見る）
- ★**材料一覧が無いのに安全率が書いてあるときは警告する**（黙って捨てない）。
  吸音を見過ぎる＝危険側に外れるため
- 検算：`test.dxf` に安全率 0.5 を入れて、**実際に読まれたモデルの吸音率**が
  0.431 → 0.183（カタログ 0.60 × 0.5 を垂直入射に変換した値と一致）。
  直す前は 1 ビットも変わらなかったことをテストで押さえてある
- ★**報告が挙げていた「派生してもう 1 つ」（パッチの切れ目が動く）は起きない**。
  `read_dxffile` は `Mesh.material` に**レイヤ名**を入れる（材料名ではない）ので、
  安全率の有無でパッチの分け方は変わらない。テストでも面の枚数が変わらないことを
  確認した。報告のこの一節だけは事実と違った

★この端末（WIN250046）はコードを直す側。回帰はテストで押さえてある。
